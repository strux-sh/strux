import{_ as t,c as d,a as o,o as r}from"./app-CBOFuHUC.js";const c={};function a(s,e){return r(),d("div",null,[...e[0]||(e[0]=[o('<h1 id="environment-variables" tabindex="-1"><a class="header-anchor" href="#environment-variables"><span>Environment Variables</span></a></h1><p>Every environment variable Strux sets for BSP scripts, plus the variables the <code>strux</code> CLI itself reads from your shell. For when scripts run, see <a href="/bsp/reference/build-steps.html" target="_blank" rel="noopener noreferrer">Build Steps &amp; Lifecycle Hooks</a>.</p><h2 id="build-script-variables" tabindex="-1"><a class="header-anchor" href="#build-script-variables"><span>Build script variables</span></a></h2><p>Build-time lifecycle scripts (every step except <code>flash_script_tool</code> / <code>flash_script</code>) run inside the <code>strux-builder</code> Docker container with the project mounted at <code>/project</code>. They receive:</p><h3 id="identity-and-target" tabindex="-1"><a class="header-anchor" href="#identity-and-target"><span>Identity and target</span></a></h3><table><thead><tr><th>Variable</th><th>Value</th><th>Description</th></tr></thead><tbody><tr><td><code>BSP_NAME</code></td><td>e.g. <code>qemu</code></td><td>Name of the BSP being built.</td></tr><tr><td><code>PRESELECTED_BSP</code></td><td>same as <code>BSP_NAME</code></td><td>Alias for <code>BSP_NAME</code>.</td></tr><tr><td><code>HOST_ARCH</code></td><td><code>arm64</code>, <code>x86_64</code>, or <code>armhf</code></td><td>Architecture of the build machine.</td></tr><tr><td><code>TARGET_ARCH</code></td><td><code>arm64</code>, <code>x86_64</code>, or <code>armhf</code></td><td>Architecture of the target device (resolved from <code>bsp.arch</code>).</td></tr><tr><td><code>STEP</code></td><td>e.g. <code>make_image</code></td><td>The lifecycle step the script is running at.</td></tr><tr><td><code>STRUX_VERSION</code></td><td>e.g. <code>0.3.0</code></td><td>Version of the Strux CLI.</td></tr><tr><td><code>PROJECT_NAME</code></td><td>from <code>strux.yaml</code></td><td>The project name.</td></tr><tr><td><code>PROJECT_VERSION</code></td><td>from <code>strux.yaml</code></td><td>The project version.</td></tr><tr><td><code>STRUX_UPDATE_ENABLED</code></td><td><code>true</code> / <code>false</code></td><td>Whether the update system is enabled in <code>strux.yaml</code> (<code>update.enabled</code>).</td></tr></tbody></table><h3 id="paths" tabindex="-1"><a class="header-anchor" href="#paths"><span>Paths</span></a></h3><p>Inside the container the project root is <code>/project</code> (when the CLI itself runs inside the builder container, e.g. in CI, these resolve to the real project path instead).</p><table><thead><tr><th>Variable</th><th>Value</th><th>Description</th></tr></thead><tbody><tr><td><code>PROJECT_DIR</code></td><td><code>/project</code></td><td>Project root directory.</td></tr><tr><td><code>PROJECT_FOLDER</code></td><td><code>/project</code></td><td>Alias for <code>PROJECT_DIR</code>.</td></tr><tr><td><code>PROJECT_DIST_DIR</code></td><td><code>/project/dist</code></td><td>The <code>dist/</code> directory.</td></tr><tr><td><code>PROJECT_DIST_FOLDER</code></td><td><code>/project/dist</code></td><td>Alias for <code>PROJECT_DIST_DIR</code>.</td></tr><tr><td><code>PROJECT_DIST_ARTIFACTS_FOLDER</code></td><td><code>/project/dist/artifacts</code></td><td>Shared, user-editable build artifacts. See <a href="/concepts/artifacts.html" target="_blank" rel="noopener noreferrer">Artifacts</a>.</td></tr><tr><td><code>SHARED_CACHE_DIR</code></td><td><code>/project/dist/cache</code></td><td>Shared cache root, for artifacts reused across BSPs.</td></tr><tr><td><code>BSP_CACHE_DIR</code></td><td><code>/project/dist/cache/{bsp}</code></td><td>BSP-specific cache directory.</td></tr><tr><td><code>PROJECT_DIST_CACHE_FOLDER</code></td><td><code>/project/dist/cache/{bsp}</code></td><td>Alias for <code>BSP_CACHE_DIR</code>.</td></tr><tr><td><code>PROJECT_DIST_OUTPUT_FOLDER</code></td><td><code>/project/dist/output/{bsp}</code></td><td>BSP-specific output directory — where final images go.</td></tr><tr><td><code>BSP_FOLDER</code></td><td><code>/project/bsp/{bsp}</code></td><td>The BSP&#39;s own directory.</td></tr></tbody></table><h3 id="splash-and-display-conditional" tabindex="-1"><a class="header-anchor" href="#splash-and-display-conditional"><span>Splash and display (conditional)</span></a></h3><p>Set only when the corresponding configuration exists.</p><table><thead><tr><th>Variable</th><th>Value</th><th>Description</th></tr></thead><tbody><tr><td><code>SPLASH_ENABLED</code></td><td><code>true</code> / <code>false</code></td><td>From <code>strux.yaml</code> <code>boot.splash.enabled</code>. Only set when <code>boot.splash</code> is configured.</td></tr><tr><td><code>SPLASH_LOGO</code></td><td>path</td><td>From <code>strux.yaml</code> <code>boot.splash.logo</code>. Only set when a logo is configured.</td></tr><tr><td><code>SPLASH_COLOR</code></td><td>hex color</td><td>From <code>strux.yaml</code> <code>boot.splash.color</code>. Only set when a color is configured.</td></tr><tr><td><code>DISPLAY_WIDTH</code></td><td>e.g. <code>1920</code></td><td>From <code>bsp.yaml</code> <code>display.resolution</code>. Only set when <code>display</code> is configured.</td></tr><tr><td><code>DISPLAY_HEIGHT</code></td><td>e.g. <code>1080</code></td><td>From <code>bsp.yaml</code> <code>display.resolution</code>. Only set when <code>display</code> is configured.</td></tr></tbody></table><h2 id="flash-script-variables" tabindex="-1"><a class="header-anchor" href="#flash-script-variables"><span>Flash script variables</span></a></h2><p><code>flash_script_tool</code> and <code>flash_script</code> scripts are run by <a href="/guide/flashing.html" target="_blank" rel="noopener noreferrer"><code>strux flash</code></a> directly on your <strong>host machine</strong> (not in Docker), with the flash workspace as the working directory. They inherit your shell environment plus <code>BSP_NAME</code>, <code>PRESELECTED_BSP</code>, <code>HOST_ARCH</code>, <code>TARGET_ARCH</code>, <code>STRUX_VERSION</code>, and the splash/display variables above (<code>PROJECT_NAME</code>, <code>PROJECT_VERSION</code>, and <code>STRUX_UPDATE_ENABLED</code> are <strong>not</strong> set for flash scripts). The path variables point at real host paths:</p><table><thead><tr><th>Variable</th><th>Value</th><th>Description</th></tr></thead><tbody><tr><td><code>PROJECT_DIR</code> / <code>PROJECT_FOLDER</code></td><td>host project path</td><td>Project root on your machine.</td></tr><tr><td><code>PROJECT_DIST_DIR</code> / <code>PROJECT_DIST_FOLDER</code></td><td><code>{project}/dist</code></td><td>The <code>dist/</code> directory.</td></tr><tr><td><code>PROJECT_DIST_ARTIFACTS_FOLDER</code></td><td><code>{project}/dist/artifacts</code></td><td>Shared artifacts directory.</td></tr><tr><td><code>SHARED_CACHE_DIR</code></td><td><code>{project}/dist/cache</code></td><td>Shared cache root.</td></tr><tr><td><code>BSP_CACHE_DIR</code> / <code>PROJECT_DIST_CACHE_FOLDER</code></td><td><code>{project}/dist/cache/{bsp}</code></td><td>BSP-specific cache.</td></tr><tr><td><code>PROJECT_DIST_OUTPUT_FOLDER</code></td><td><code>{project}/dist/output/{bsp}</code></td><td>BSP-specific output — where the image to flash lives.</td></tr><tr><td><code>BSP_FOLDER</code></td><td><code>{project}/bsp/{bsp}</code></td><td>The BSP directory.</td></tr><tr><td><code>FLASH_DIR</code></td><td><code>{project}/dist/flash/{bsp}</code></td><td>Flash workspace — the script&#39;s working directory; put downloaded tools here.</td></tr><tr><td><code>PROJECT_DIST_FLASH_FOLDER</code></td><td>same as <code>FLASH_DIR</code></td><td>Alias for <code>FLASH_DIR</code>.</td></tr><tr><td><code>STEP</code></td><td><code>flash_script_tool</code> or <code>flash_script</code></td><td>Which flash stage is running.</td></tr></tbody></table><p>See <a href="/bsp/guide/flash-scripts.html" target="_blank" rel="noopener noreferrer">Flash Scripts</a> for how to structure them.</p><h2 id="variables-the-cli-reads" tabindex="-1"><a class="header-anchor" href="#variables-the-cli-reads"><span>Variables the CLI reads</span></a></h2><p>These are read from <strong>your</strong> environment when you run <code>strux</code>:</p><table><thead><tr><th>Variable</th><th>Value</th><th>Description</th></tr></thead><tbody><tr><td><code>STRUX_IN_CONTAINER</code></td><td><code>1</code></td><td>Tells Strux it is already running inside the builder container (CI). Build scripts execute directly instead of spawning Docker, and verbose output is auto-enabled when there is no TTY.</td></tr><tr><td><code>STRUX_VERSION</code></td><td>version string</td><td>Overrides the CLI&#39;s reported version (set at compile time in CI; local builds fall back to <code>package.json</code>).</td></tr><tr><td><code>STRUX_DEV_SERVER_URL</code></td><td>URL</td><td>Default dev server URL for <code>strux update</code> when <code>--server</code> is not passed. Falls back to <code>http://127.0.0.1:8000</code>.</td></tr><tr><td><code>STRUX_DEV_NO_UI</code></td><td><code>1</code></td><td>Disables the interactive terminal UI in <a href="/guide/dev-mode.html" target="_blank" rel="noopener noreferrer"><code>strux dev</code></a>, logging plainly instead.</td></tr><tr><td><code>STRUX_GL</code></td><td><code>1</code> or <code>0</code></td><td>Forces GL acceleration on (<code>1</code>) or off (any other value) for <a href="/guide/running-qemu.html" target="_blank" rel="noopener noreferrer"><code>strux run</code></a> QEMU. Unset, Strux auto-detects from the GPU vendor (Intel/AMD on, NVIDIA and unknown off).</td></tr></tbody></table><div class="hint-container warning"><p class="hint-container-title">Values must be shell-safe</p><p>Strux validates script environment values before injecting them into the container; values that could break shell quoting are rejected. Keep paths and config values free of quotes and shell metacharacters.</p></div>',20)])])}const n=t(c,[["render",a]]),l=JSON.parse('{"path":"/bsp/reference/environment-variables.html","title":"Environment Variables","lang":"en-US","frontmatter":{},"headers":[{"level":2,"title":"Build script variables","slug":"build-script-variables","link":"#build-script-variables","children":[{"level":3,"title":"Identity and target","slug":"identity-and-target","link":"#identity-and-target","children":[]},{"level":3,"title":"Paths","slug":"paths","link":"#paths","children":[]},{"level":3,"title":"Splash and display (conditional)","slug":"splash-and-display-conditional","link":"#splash-and-display-conditional","children":[]}]},{"level":2,"title":"Flash script variables","slug":"flash-script-variables","link":"#flash-script-variables","children":[]},{"level":2,"title":"Variables the CLI reads","slug":"variables-the-cli-reads","link":"#variables-the-cli-reads","children":[]}],"git":{"updatedTime":1781317241000,"contributors":[{"name":"Miguel Medeiros","username":"","email":"miguel@medeirosconsulting.ca","commits":1}],"changelog":[{"hash":"e32366d86296aed0c45cf8447d8ea8147c1d03da","time":1781317241000,"email":"miguel@medeirosconsulting.ca","author":"Miguel Medeiros","message":"add docs"}]},"filePathRelative":"bsp/reference/environment-variables.md"}');export{n as comp,l as data};
