import{_ as l,c as r,b as s,d as n,e as t,w as i,a as o,r as d,o as c}from"./app-nwDDctZf.js";const p={};function u(h,e){const a=d("RouteLink");return c(),r("div",null,[e[43]||(e[43]=s("h1",{id:"custom-kernels",tabindex:"-1"},[s("a",{class:"header-anchor",href:"#custom-kernels"},[s("span",null,"Custom Kernels")])],-1)),s("p",null,[e[1]||(e[1]=n("This page covers the ",-1)),e[2]||(e[2]=s("code",null,"boot.kernel",-1)),e[3]||(e[3]=n(" block of ",-1)),e[4]||(e[4]=s("code",null,"bsp.yaml",-1)),e[5]||(e[5]=n(": choosing a kernel source, configuring it with a defconfig and fragments, applying patches, building device trees, and the ",-1)),e[6]||(e[6]=s("code",null,"strux kernel",-1)),e[7]||(e[7]=n(" commands for iterating on configuration. It assumes you have a building BSP — if not, start with ",-1)),t(a,{to:"/bsp/guide/writing-a-bsp.html"},{default:i(()=>[...e[0]||(e[0]=[n("Writing a BSP",-1)])]),_:1}),e[8]||(e[8]=n(".",-1))]),e[44]||(e[44]=o(`<h2 id="when-you-need-a-custom-kernel" tabindex="-1"><a class="header-anchor" href="#when-you-need-a-custom-kernel"><span>When you need a custom kernel</span></a></h2><p>You don&#39;t always. With <code>custom_kernel: false</code> (the default in the qemu BSP), the image uses the stock Debian kernel that comes with the root filesystem — which is exactly right for QEMU and for x86 hardware that generic Debian already boots.</p><p>You need a custom kernel when:</p><ul><li><strong>The board needs a vendor kernel tree.</strong> Most ARM single-board computers (Rockchip, Allwinner, and friends) have drivers that never made it upstream; the vendor or a community fork (Armbian, for example) maintains a tree that supports the SoC.</li><li><strong>The board needs a custom device tree</strong> describing its specific panel, touch controller, PMIC, and wiring.</li><li><strong>You need kernel options Debian doesn&#39;t enable</strong> — the USB gadget Ethernet support that <code>strux dev</code> over USB requires is a common one.</li></ul><h2 id="the-kernel-block" tabindex="-1"><a class="header-anchor" href="#the-kernel-block"><span>The kernel block</span></a></h2><p>This is the real configuration from the <code>hd215-rk3576</code> BSP:</p><div class="language-yaml line-numbers-mode" data-highlighter="prismjs" data-ext="yml"><pre><code><span class="line">  <span class="token key atrule">boot</span><span class="token punctuation">:</span></span>
<span class="line">    <span class="token key atrule">kernel</span><span class="token punctuation">:</span></span>
<span class="line">      <span class="token key atrule">custom_kernel</span><span class="token punctuation">:</span> <span class="token boolean important">true</span></span>
<span class="line">      <span class="token key atrule">source</span><span class="token punctuation">:</span> https<span class="token punctuation">:</span>//github.com/armbian/linux<span class="token punctuation">-</span>rockchip.git<span class="token comment">#rk-6.1-rkr6.1</span></span>
<span class="line">      <span class="token key atrule">version</span><span class="token punctuation">:</span> <span class="token string">&quot;6.1&quot;</span></span>
<span class="line">      <span class="token key atrule">defconfig</span><span class="token punctuation">:</span> rockchip_linux_defconfig</span>
<span class="line">      <span class="token key atrule">fragments</span><span class="token punctuation">:</span></span>
<span class="line">        <span class="token punctuation">-</span> <span class="token punctuation">|</span><span class="token scalar string"></span>
<span class="line">          CONFIG_CPU_RK3576=y</span>
<span class="line">          CONFIG_CLK_RK3576=y</span>
<span class="line">          CONFIG_ARM_ROCKCHIP_CPUFREQ=y</span></span>
<span class="line">        <span class="token punctuation">-</span> <span class="token punctuation">|</span><span class="token scalar string"></span>
<span class="line">          CONFIG_USB_GADGET=y</span>
<span class="line">          CONFIG_USB_LIBCOMPOSITE=y</span>
<span class="line">          CONFIG_USB_CONFIGFS=y</span>
<span class="line">          CONFIG_USB_CONFIGFS_ECM=y</span>
<span class="line">          CONFIG_USB_CONFIGFS_NCM=y</span>
<span class="line">          CONFIG_USB_CONFIGFS_RNDIS=y</span></span>
<span class="line">      <span class="token key atrule">patches</span><span class="token punctuation">:</span></span>
<span class="line">        <span class="token punctuation">-</span> ./patches/kernel<span class="token punctuation">-</span>aic8800<span class="token punctuation">-</span>makefile<span class="token punctuation">-</span>fix.patch</span>
<span class="line">        <span class="token punctuation">-</span> ./patches/kernel<span class="token punctuation">-</span>hd215<span class="token punctuation">-</span>mcu<span class="token punctuation">-</span>poweroff.patch</span>
<span class="line">      <span class="token key atrule">device_tree</span><span class="token punctuation">:</span></span>
<span class="line">        <span class="token key atrule">dts</span><span class="token punctuation">:</span> ./dts/rk3576<span class="token punctuation">-</span>hd215<span class="token punctuation">-</span>linux.dts</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>When <code>custom_kernel: true</code>, the kernel step runs as part of <code>strux build</code>: fetch source → apply patches → (your <code>after_kernel_extract</code> scripts) → configure → compile → install artifacts. Everything happens inside the builder container with the cross-compiler matching your <code>arch</code> (<code>aarch64-linux-gnu-</code> for <code>arm64</code>, <code>arm-linux-gnueabihf-</code> for <code>armhf</code>, native for <code>x86_64</code>).</p><h2 id="source-and-version" tabindex="-1"><a class="header-anchor" href="#source-and-version"><span>Source and version</span></a></h2><p><code>source</code> accepts two forms:</p><ul><li><strong>A git URL</strong>, with an optional ref after <code>#</code> — a branch (<code>#rk-6.1-rkr6.1</code>), a tag, or an exact commit hash (<code>#b4ef083dc0c3...</code>). Strux tries a shallow clone first and falls back to a full clone when the ref demands it.</li><li><strong>A tarball URL</strong> ending in <code>.tar.gz</code>, <code>.tar.xz</code>, <code>.tar.bz2</code>, or <code>.tgz</code>, e.g. <code>https://github.com/torvalds/linux/releases/download/v6.1/linux-6.1.tar.gz</code>.</li></ul><p>The source is checked out to <code>dist/cache/&lt;bsp&gt;/kernel-source/</code> and reused across builds; on later builds Strux just switches the existing checkout to the requested ref.</p><div class="hint-container tip"><p class="hint-container-title">Pin your ref</p><p>The <code>ht109-rk3576s</code> BSP pins an exact commit (<code>...kernel.git#b4ef083dc0c3608e744deabb43dc6b781aadbe6e</code>) rather than a branch, with a comment explaining the patches were generated against that tip. Branches move; your patches don&#39;t. Pin a commit once the BSP works, and bump it deliberately.</p></div><p><code>version</code> is a declarative label for the kernel version (<code>&quot;6.1&quot;</code>). What actually gets fetched is determined entirely by <code>source</code> and its <code>#ref</code>.</p><h2 id="configuration-defconfig-fragments" tabindex="-1"><a class="header-anchor" href="#configuration-defconfig-fragments"><span>Configuration: defconfig + fragments</span></a></h2><p>Kernel configuration is the <code>.config</code> file — thousands of <code>CONFIG_*</code> options. Strux builds it in layers:</p><ol><li><strong>Base config.</strong> If <code>bsp/&lt;bsp&gt;/configs/kernel.config</code> exists (a full saved config — see menuconfig below), it is used and refreshed with <code>make olddefconfig</code>. Otherwise Strux runs <code>make &lt;defconfig&gt;</code> with the <code>defconfig</code> value from <code>bsp.yaml</code>; if you omit it, the kernel&#39;s generic <code>defconfig</code> target is used.</li><li><strong>Fragments.</strong> Each entry in <code>fragments</code> is merged on top using the kernel&#39;s own <code>scripts/kconfig/merge_config.sh</code>, in order — later fragments win.</li></ol><div class="hint-container tip"><p class="hint-container-title">What&#39;s a defconfig? What&#39;s a fragment?</p><p>A <strong>defconfig</strong> is a named configuration preset shipped inside the kernel tree (in <code>arch/*/configs/</code>) — <code>rockchip_linux_defconfig</code> enables everything Rockchip boards commonly need. A <strong>fragment</strong> is a small list of <code>CONFIG_*</code> lines layered on top, so you only state your deltas instead of maintaining a full multi-thousand-line config.</p></div><p>A fragment can be either of:</p><ul><li><strong>An inline block</strong> — a YAML multiline string of <code>CONFIG_*</code> lines, like the examples above. Great for small, self-documenting deltas.</li><li><strong>A file path</strong> — <code>./configs/kernel-usb.config</code> (relative to the BSP folder) or a bare filename ending in <code>.config</code>. Use files when a fragment gets long.</li></ul><p>To <em>disable</em> an option from the defconfig, set it to <code>n</code> — the <code>hd215-rk3576</code> BSP does <code>CONFIG_LOGO=n</code> to suppress the Tux boot logo. The kernel&#39;s comment syntax (<code># CONFIG_FOO is not set</code>) also works inside fragments.</p><div class="hint-container warning"><p class="hint-container-title">Strux dev over USB needs gadget support</p><p>The USB fragment in the example above is not optional decoration: <code>strux dev</code> over a USB cable requires USB gadget Ethernet in the kernel. Enable ECM/NCM for macOS and Linux hosts, RNDIS for Windows hosts. Every hardware example BSP carries this fragment.</p></div><h2 id="capturing-config-interactively-menuconfig" tabindex="-1"><a class="header-anchor" href="#capturing-config-interactively-menuconfig"><span>Capturing config interactively: menuconfig</span></a></h2><p>Hunting <code>CONFIG_</code> names by hand is painful. <code>make menuconfig</code> is the kernel&#39;s interactive configuration browser, and Strux wraps it:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh"><pre><code><span class="line">strux kernel menuconfig</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>This fetches the kernel source if needed, loads your current config (saved config if present, otherwise the defconfig), and opens menuconfig in the builder container — navigate with arrows, toggle with Space, search with <code>/</code>. When you exit, the full resulting config is saved to <code>bsp/&lt;bsp&gt;/configs/kernel.config</code>, and the next build uses it automatically.</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh"><pre><code><span class="line">strux kernel menuconfig <span class="token parameter variable">--save</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p><code>--save</code> additionally runs the kernel&#39;s <code>savedefconfig</code> to produce a <em>minimal</em> config — only the deltas from defaults — at <code>bsp/&lt;bsp&gt;/configs/kernel.fragment</code>. That file is fragment-shaped: add it to <code>bsp.yaml</code> if you prefer keeping your configuration as defconfig + fragments instead of a full saved config:</p><div class="language-yaml line-numbers-mode" data-highlighter="prismjs" data-ext="yml"><pre><code><span class="line">      <span class="token key atrule">fragments</span><span class="token punctuation">:</span></span>
<span class="line">        <span class="token punctuation">-</span> <span class="token string">&quot;./configs/kernel.fragment&quot;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><div class="hint-container warning"><p class="hint-container-title">A saved kernel.config takes priority</p><p>Once <code>configs/kernel.config</code> exists, it replaces the <code>defconfig</code> from <code>bsp.yaml</code> as the base (fragments still apply on top). That&#39;s convenient for iteration but easy to forget: if changing <code>defconfig</code> in <code>bsp.yaml</code> seems to do nothing, a saved config is why. Delete <code>configs/kernel.config</code> to go back to defconfig-based configuration. (<code>strux kernel menuconfig</code> requires <code>custom_kernel: true</code>.)</p></div><h2 id="patches" tabindex="-1"><a class="header-anchor" href="#patches"><span>Patches</span></a></h2><p><code>patches</code> is a list of patch files applied to the source tree after fetch, in order, with <code>patch -p1</code>. Paths are relative to the BSP folder.</p><div class="language-yaml line-numbers-mode" data-highlighter="prismjs" data-ext="yml"><pre><code><span class="line">      <span class="token key atrule">patches</span><span class="token punctuation">:</span></span>
<span class="line">        <span class="token punctuation">-</span> ./patches/kernel<span class="token punctuation">-</span>aic8800<span class="token punctuation">-</span>makefile<span class="token punctuation">-</span>fix.patch</span>
<span class="line">        <span class="token punctuation">-</span> ./patches/kernel<span class="token punctuation">-</span>hd215<span class="token punctuation">-</span>mcu<span class="token punctuation">-</span>poweroff.patch</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Two behaviors worth knowing:</p><ul><li>Each patch is dry-run first; a patch that no longer applies <strong>fails the build</strong> (rather than silently producing a broken kernel).</li><li>A patch that is <em>already applied</em> (detected via a reverse dry-run) is skipped with a notice — so reusing a cached source tree across builds is safe.</li></ul>`,35)),s("p",null,[e[10]||(e[10]=n("For board files that are additions rather than modifications, you don't need patches at all: the ",-1)),e[11]||(e[11]=s("code",null,"ht109-rk3576s",-1)),e[12]||(e[12]=n(" BSP keeps whole vendor driver directories under ",-1)),e[13]||(e[13]=s("code",null,"drivers/",-1)),e[14]||(e[14]=n(" in the BSP and copies them into the kernel tree with an ",-1)),e[15]||(e[15]=s("code",null,"after_kernel_extract",-1)),e[16]||(e[16]=n(" script, which runs after fetch + patch and before configuration. See ",-1)),t(a,{to:"/bsp/concepts/lifecycle-scripts.html"},{default:i(()=>[...e[9]||(e[9]=[n("Lifecycle Scripts",-1)])]),_:1}),e[17]||(e[17]=n(".",-1))]),e[45]||(e[45]=o(`<h2 id="device-trees" tabindex="-1"><a class="header-anchor" href="#device-trees"><span>Device trees</span></a></h2><p>A <strong>device tree source</strong> (<code>.dts</code>) describes the board&#39;s hardware — peripherals, addresses, pins, clocks — and compiles to a binary blob (<code>.dtb</code>) the bootloader hands to the kernel at boot. ARM boards can&#39;t boot without the right one.</p><div class="language-yaml line-numbers-mode" data-highlighter="prismjs" data-ext="yml"><pre><code><span class="line">      <span class="token key atrule">device_tree</span><span class="token punctuation">:</span></span>
<span class="line">        <span class="token key atrule">dts</span><span class="token punctuation">:</span> ./dts/rk3576<span class="token punctuation">-</span>hd215<span class="token punctuation">-</span>linux.dts</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p><code>dts</code> accepts a single entry or a list, and each entry is interpreted by its path form:</p><ul><li><strong>A name with no leading <code>./</code></strong> (<code>rk3399-rock-pi-4.dts</code>): an <em>in-tree</em> device tree — one that already exists in the kernel&#39;s <code>arch/*/boot/dts/</code>. Strux builds exactly that <code>.dtb</code> make target. Use this when mainline or the vendor tree already supports your board.</li><li><strong>A path starting with <code>./</code> or <code>/</code></strong> (<code>./dts/rk3576-hd215-linux.dts</code>): an <em>external</em> DTS kept in your BSP. Strux preprocesses it with the kernel&#39;s include paths (so <code>#include &lt;dt-bindings/...&gt;</code> and SoC <code>.dtsi</code> includes from the kernel tree resolve), compiles it with the kernel&#39;s own <code>dtc</code>, and drops the <code>.dtb</code> into the output. This is the normal case for custom boards — your DTS lives in the BSP, versioned with everything else, typically <code>#include</code>-ing the SoC dtsi from the kernel tree and describing only your board on top.</li><li>A <code>.dtsi</code> entry in the list isn&#39;t compiled itself; its directory is added to the include path for the other entries.</li></ul><p>Two optional companions:</p><div class="language-yaml line-numbers-mode" data-highlighter="prismjs" data-ext="yml"><pre><code><span class="line">      <span class="token key atrule">device_tree</span><span class="token punctuation">:</span></span>
<span class="line">        <span class="token key atrule">dts</span><span class="token punctuation">:</span> ./dts/rk3576<span class="token punctuation">-</span>hd215<span class="token punctuation">-</span>linux.dts</span>
<span class="line">        <span class="token key atrule">overlays</span><span class="token punctuation">:</span></span>
<span class="line">          <span class="token punctuation">-</span> ./dts/overlays/add<span class="token punctuation">-</span>device.dtso</span>
<span class="line">        <span class="token key atrule">include_paths</span><span class="token punctuation">:</span></span>
<span class="line">          <span class="token punctuation">-</span> ./dts/includes</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><ul><li><code>overlays</code> — device tree overlay sources (<code>.dtso</code>), compiled with <code>dtc -@</code> into <code>.dtbo</code> files and copied to the output alongside the DTBs. Overlays describe optional hardware variations applied on top of a base tree.</li><li><code>include_paths</code> — extra directories searched when preprocessing external DTS files, for headers you keep in the BSP.</li></ul><h2 id="build-outputs" tabindex="-1"><a class="header-anchor" href="#build-outputs"><span>Build outputs</span></a></h2><p>Everything lands in <code>dist/cache/&lt;bsp&gt;/kernel/</code>, where your <code>make_image</code> (and bootloader) scripts consume it:</p><table><thead><tr><th>Artifact</th><th>What it is</th></tr></thead><tbody><tr><td><code>Image</code> / <code>zImage</code> / <code>bzImage</code></td><td>The kernel image (arm64 / armhf / x86_64 respectively)</td></tr><tr><td><code>kernel.img</code></td><td>A copy of the kernel image under a uniform name</td></tr><tr><td><code>modules/</code></td><td>Kernel modules, installed via <code>INSTALL_MOD_PATH</code> (i.e. a <code>lib/modules/...</code> tree)</td></tr><tr><td><code>dtbs/</code></td><td>Compiled device tree blobs and overlays</td></tr><tr><td><code>.config</code></td><td>The final merged configuration, for reference</td></tr></tbody></table><p>For example, the <code>hd215-rk3576</code> extlinux boot configuration references <code>dtbs/rk3576-hd215-linux.dtb</code>, and its custom bootloader build lists <code>cache/kernel/rk3576-hd215-linux.dtb</code> as a dependency.</p><h2 id="replacing-the-build-entirely" tabindex="-1"><a class="header-anchor" href="#replacing-the-build-entirely"><span>Replacing the build entirely</span></a></h2>`,13)),s("p",null,[e[19]||(e[19]=n("If the built-in fetch/config/build flow doesn't fit (a kernel with a bespoke build system, or prebuilt vendor kernels), register a script with ",-1)),e[20]||(e[20]=s("code",null,"step: custom_kernel",-1)),e[21]||(e[21]=n(". It replaces the built-in extract and build phases completely — ",-1)),e[22]||(e[22]=s("code",null,"before_kernel",-1)),e[23]||(e[23]=n(" and ",-1)),e[24]||(e[24]=s("code",null,"after_kernel",-1)),e[25]||(e[25]=n(" hooks still run around it. See ",-1)),t(a,{to:"/bsp/concepts/lifecycle-scripts.html"},{default:i(()=>[...e[18]||(e[18]=[n("Lifecycle Scripts",-1)])]),_:1}),e[26]||(e[26]=n(".",-1))]),e[46]||(e[46]=o(`<h2 id="cleaning" tabindex="-1"><a class="header-anchor" href="#cleaning"><span>Cleaning</span></a></h2><p>Kernel builds leave a lot of state in <code>dist/cache/&lt;bsp&gt;/kernel-source/</code>. When configuration changes don&#39;t seem to take effect, or you want a truly fresh build:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh"><pre><code><span class="line">strux kernel clean                    <span class="token comment"># make mrproper: removes .config and all generated files</span></span>
<span class="line">strux kernel clean <span class="token parameter variable">--mode</span> clean       <span class="token comment"># make clean: removes objects, keeps the config</span></span>
<span class="line">strux kernel clean <span class="token parameter variable">--mode</span> full        <span class="token comment"># deletes the source checkout and build output entirely</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>--mode full</code> means the next build re-clones the source — the heavyweight option when the tree is wedged or you&#39;ve changed <code>source</code> and want no leftovers.</p><h2 id="where-to-go-next" tabindex="-1"><a class="header-anchor" href="#where-to-go-next"><span>Where to go next</span></a></h2>`,5)),s("ul",null,[s("li",null,[t(a,{to:"/bsp/guide/bootloader.html"},{default:i(()=>[...e[27]||(e[27]=[n("Bootloaders",-1)])]),_:1}),e[28]||(e[28]=n(" — the other half of the boot story, including how the bootloader finds your kernel and DTB.",-1))]),s("li",null,[t(a,{to:"/bsp/concepts/lifecycle-scripts.html"},{default:i(()=>[...e[29]||(e[29]=[n("Lifecycle Scripts",-1)])]),_:1}),e[30]||(e[30]=n(" — ",-1)),e[31]||(e[31]=s("code",null,"before_kernel",-1)),e[32]||(e[32]=n(", ",-1)),e[33]||(e[33]=s("code",null,"after_kernel_extract",-1)),e[34]||(e[34]=n(", ",-1)),e[35]||(e[35]=s("code",null,"after_kernel",-1)),e[36]||(e[36]=n(", ",-1)),e[37]||(e[37]=s("code",null,"custom_kernel",-1)),e[38]||(e[38]=n(".",-1))]),s("li",null,[t(a,{to:"/bsp/reference/bsp-yaml.html"},{default:i(()=>[...e[39]||(e[39]=[n("bsp.yaml Reference",-1)])]),_:1}),e[40]||(e[40]=n(" — the kernel block, key by key.",-1))]),s("li",null,[t(a,{to:"/concepts/caching.html"},{default:i(()=>[...e[41]||(e[41]=[n("Caching",-1)])]),_:1}),e[42]||(e[42]=n(" — when the kernel step reruns and when it's skipped.",-1))])])])}const m=l(p,[["render",u]]),f=JSON.parse('{"path":"/bsp/guide/kernel.html","title":"Custom Kernels","lang":"en-US","frontmatter":{},"headers":[{"level":2,"title":"When you need a custom kernel","slug":"when-you-need-a-custom-kernel","link":"#when-you-need-a-custom-kernel","children":[]},{"level":2,"title":"The kernel block","slug":"the-kernel-block","link":"#the-kernel-block","children":[]},{"level":2,"title":"Source and version","slug":"source-and-version","link":"#source-and-version","children":[]},{"level":2,"title":"Configuration: defconfig + fragments","slug":"configuration-defconfig-fragments","link":"#configuration-defconfig-fragments","children":[]},{"level":2,"title":"Capturing config interactively: menuconfig","slug":"capturing-config-interactively-menuconfig","link":"#capturing-config-interactively-menuconfig","children":[]},{"level":2,"title":"Patches","slug":"patches","link":"#patches","children":[]},{"level":2,"title":"Device trees","slug":"device-trees","link":"#device-trees","children":[]},{"level":2,"title":"Build outputs","slug":"build-outputs","link":"#build-outputs","children":[]},{"level":2,"title":"Replacing the build entirely","slug":"replacing-the-build-entirely","link":"#replacing-the-build-entirely","children":[]},{"level":2,"title":"Cleaning","slug":"cleaning","link":"#cleaning","children":[]},{"level":2,"title":"Where to go next","slug":"where-to-go-next","link":"#where-to-go-next","children":[]}],"git":{"updatedTime":1782416639000,"contributors":[{"name":"Miguel Medeiros","username":"","email":"miguel@medeirosconsulting.ca","commits":2},{"name":"Claude Opus 4.8 (1M context)","username":"","email":"noreply@anthropic.com","commits":1}],"changelog":[{"hash":"243f80d49c6787743d7d179dc9503bae59a76557","time":1782416639000,"email":"miguel@medeirosconsulting.ca","author":"Miguel Medeiros","message":"docs: fix broken in-page links under base path","coAuthors":[{"name":"Claude Opus 4.8 (1M context)","email":"noreply@anthropic.com"}]},{"hash":"e32366d86296aed0c45cf8447d8ea8147c1d03da","time":1781317241000,"email":"miguel@medeirosconsulting.ca","author":"Miguel Medeiros","message":"add docs"}]},"filePathRelative":"bsp/guide/kernel.md"}');export{m as comp,f as data};
