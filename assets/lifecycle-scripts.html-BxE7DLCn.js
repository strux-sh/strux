import{_ as t,c as n,a as o,o as s}from"./app-CBOFuHUC.js";const a={};function r(i,e){return s(),n("div",null,[...e[0]||(e[0]=[o(`<h1 id="lifecycle-scripts" tabindex="-1"><a class="header-anchor" href="#lifecycle-scripts"><span>Lifecycle Scripts</span></a></h1><p>A BSP customizes the build by attaching shell scripts to <strong>hooks</strong> in the build pipeline — points before and after each built-in step. This page explains the hook model: where the hooks are, what environment your scripts run in, how script caching decides whether to run them, and how paths in their configuration resolve. For a hands-on walkthrough of writing a script, see the <a href="/bsp/guide/scripts.html" target="_blank" rel="noopener noreferrer">scripts guide</a>.</p><h2 id="the-mental-model" tabindex="-1"><a class="header-anchor" href="#the-mental-model"><span>The mental model</span></a></h2><p><code>strux build</code> runs a fixed pipeline of built-in steps: frontend → application → cage → wpe → client → kernel → bootloader → rootfs → bundling. Strux knows how to do each of these generically; it does <strong>not</strong> know how to assemble the final disk image for your specific board, install your vendor drivers, or convert your splash logo into the format your bootloader wants. That&#39;s what lifecycle scripts are for.</p><p>Every script is declared in <code>bsp.yaml</code> under <code>bsp.scripts</code>, with a <code>location</code> (the script file) and a <code>step</code> (the hook to run it at):</p><div class="language-yaml line-numbers-mode" data-highlighter="prismjs" data-ext="yml"><pre><code><span class="line"><span class="token key atrule">scripts</span><span class="token punctuation">:</span></span>
<span class="line">  <span class="token punctuation">-</span> <span class="token key atrule">location</span><span class="token punctuation">:</span> ./scripts/make<span class="token punctuation">-</span>image.sh</span>
<span class="line">    <span class="token key atrule">step</span><span class="token punctuation">:</span> make_image</span>
<span class="line">    <span class="token key atrule">description</span><span class="token punctuation">:</span> <span class="token string">&quot;Create QEMU disk image&quot;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Before and after each built-in step, Strux runs every script registered for that hook, in the order they appear in <code>bsp.yaml</code>. The built-in step in the middle may be skipped by the <a href="/concepts/caching.html" target="_blank" rel="noopener noreferrer">build cache</a>; the hooks around it are still evaluated (each script has its own cache, described below).</p><div class="language-txt line-numbers-mode" data-highlighter="prismjs" data-ext="txt"><pre><code><span class="line">before_build</span>
<span class="line">  before_frontend     → [frontend]    → after_frontend</span>
<span class="line">  before_application  → [application] → after_application</span>
<span class="line">  before_cage         → [cage]        → after_cage</span>
<span class="line">  before_wpe          → [wpe]         → after_wpe</span>
<span class="line">  before_client       → [client]      → after_client</span>
<span class="line">  before_kernel       → [kernel]      → after_kernel        (only if custom_kernel: true)</span>
<span class="line">  before_bootloader   → [bootloader]  → after_bootloader    (only if bootloader.enabled: true)</span>
<span class="line">  before_rootfs       → [rootfs]      → after_rootfs</span>
<span class="line">                        [rootfs post-processing]</span>
<span class="line">  before_bundle</span>
<span class="line">  make_image</span>
<span class="line">after_build</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="the-hooks" tabindex="-1"><a class="header-anchor" href="#the-hooks"><span>The hooks</span></a></h2><p>Most hooks come in <code>before_*</code> / <code>after_*</code> pairs around a pipeline step, plus a handful of special ones. The complete list with exact ordering is in the <a href="/bsp/reference/build-steps.html" target="_blank" rel="noopener noreferrer">build steps reference</a>; here is the shape of it:</p><table><thead><tr><th>Hook</th><th>Runs</th></tr></thead><tbody><tr><td><code>before_build</code> / <code>after_build</code></td><td>Very first and very last hooks of the whole pipeline</td></tr><tr><td><code>before_frontend</code> / <code>after_frontend</code></td><td>Around web frontend compilation</td></tr><tr><td><code>before_application</code> / <code>after_application</code></td><td>Around <code>main.go</code> compilation</td></tr><tr><td><code>before_cage</code> / <code>after_cage</code></td><td>Around the Cage compositor build</td></tr><tr><td><code>before_wpe</code> / <code>after_wpe</code></td><td>Around the WPE WebKit extension build</td></tr><tr><td><code>before_client</code> / <code>after_client</code></td><td>Around the on-device strux client binary build</td></tr><tr><td><code>before_kernel</code> / <code>after_kernel</code></td><td>Around the kernel build — only when <code>boot.kernel.custom_kernel: true</code></td></tr><tr><td><code>after_kernel_extract</code></td><td>Between kernel source fetch+patch and configure+compile</td></tr><tr><td><code>custom_kernel</code></td><td><em>Replaces</em> the built-in kernel build entirely</td></tr><tr><td><code>before_bootloader</code> / <code>after_bootloader</code></td><td>Around the bootloader build — only when <code>boot.bootloader.enabled: true</code></td></tr><tr><td><code>custom_bootloader</code></td><td><em>Replaces</em> the built-in bootloader build entirely</td></tr><tr><td><code>before_rootfs</code> / <code>after_rootfs</code></td><td>Around base root filesystem creation</td></tr><tr><td><code>before_bundle</code></td><td>After rootfs post-processing, right before image creation</td></tr><tr><td><code>make_image</code></td><td>Creates the final disk image — nearly every BSP defines this</td></tr><tr><td><code>flash_script_tool</code> / <code>flash_script</code></td><td><strong>Not run during build.</strong> Run on the host by <a href="/bsp/guide/flash-scripts.html" target="_blank" rel="noopener noreferrer"><code>strux flash</code></a></td></tr></tbody></table><div class="hint-container tip"><p class="hint-container-title">rootfs?</p><p>The <strong>root filesystem</strong> (rootfs) is the Linux file tree the device boots into — <code>/usr</code>, <code>/etc</code>, your app, everything. Strux assembles it as a Debian system inside Docker, then &quot;post-processing&quot; installs your binaries and overlay files into it.</p></div><p>Three details about the kernel and bootloader hooks are worth knowing:</p><ul><li>The entire kernel hook group only exists when <code>boot.kernel.custom_kernel: true</code>; the bootloader group only when <code>boot.bootloader.enabled: true</code>. On a BSP like qemu, where both are false, none of these hooks run.</li><li>A <code>custom_kernel</code> or <code>custom_bootloader</code> script replaces the built-in build, but the surrounding <code>before_*</code> / <code>after_*</code> hooks still run. With <code>bootloader.type: custom</code> (or <code>none</code>), the built-in bootloader build is also skipped, even without a <code>custom_bootloader</code> script — the Rockchip boards use <code>type: custom</code> plus a <code>custom_bootloader</code> script to build a vendor U-Boot fork.</li><li><code>after_kernel_extract</code> only fires when the built-in kernel build actually runs (it sits between the fetch+patch phase and the compile phase). If the kernel step is served from cache, or replaced by <code>custom_kernel</code>, this hook is skipped. The HT109 BSP uses it to drop vendor drivers into the freshly extracted kernel source before compilation.</li></ul><h2 id="execution-environment" tabindex="-1"><a class="header-anchor" href="#execution-environment"><span>Execution environment</span></a></h2><p>Build-time scripts do <strong>not</strong> run on your machine directly. Strux reads the script file and executes its content with <code>/bin/bash -c</code> inside the <code>strux-builder</code> Docker container — the same privileged container all built-in build steps use, with your project mounted at <code>/project</code>. This means:</p><ul><li>The script runs as root inside Debian Linux, regardless of your host OS. Tools like <code>genimage</code>, <code>mkfs.ext4</code>, <code>dtc</code>, and cross-compilers are available there.</li><li>Host paths mean nothing inside the container. Always use the environment variables Strux provides — <code>PROJECT_FOLDER</code> is <code>/project</code>, <code>PROJECT_DIST_CACHE_FOLDER</code> is <code>/project/dist/cache/{bsp}</code>, <code>PROJECT_DIST_OUTPUT_FOLDER</code> is <code>/project/dist/output/{bsp}</code>, and so on. The full list is in the <a href="/bsp/reference/environment-variables.html" target="_blank" rel="noopener noreferrer">environment variables reference</a>.</li><li>Because the container is run with <code>--privileged</code>, scripts can loop-mount images and chroot — that&#39;s how <code>make-image.sh</code> scripts build disk images.</li></ul><p>Strux also passes context as environment variables: <code>BSP_NAME</code>, <code>STEP</code> (the current hook name), <code>HOST_ARCH</code> and <code>TARGET_ARCH</code>, <code>STRUX_VERSION</code>, <code>PROJECT_NAME</code>, <code>PROJECT_VERSION</code>, <code>STRUX_UPDATE_ENABLED</code>, splash configuration (<code>SPLASH_ENABLED</code>, <code>SPLASH_LOGO</code>, <code>SPLASH_COLOR</code>) and the display size (<code>DISPLAY_WIDTH</code>, <code>DISPLAY_HEIGHT</code>).</p><p>The two flash hooks are the exception: they run <strong>on the host</strong>, outside Docker, because they need access to your USB ports and SD card readers. See <a href="/bsp/guide/flash-scripts.html" target="_blank" rel="noopener noreferrer">flash scripts</a>.</p><h2 id="script-caching" tabindex="-1"><a class="header-anchor" href="#script-caching"><span>Script caching</span></a></h2><p>Each script has its own cache entry, independent of the built-in step cache. The model combines an <strong>existence check</strong> on outputs with a <strong>hash check</strong> on inputs:</p><div class="language-yaml line-numbers-mode" data-highlighter="prismjs" data-ext="yml"><pre><code><span class="line"><span class="token punctuation">-</span> <span class="token key atrule">location</span><span class="token punctuation">:</span> ./scripts/build<span class="token punctuation">-</span>bootloader<span class="token punctuation">-</span>rockchip.sh</span>
<span class="line">  <span class="token key atrule">step</span><span class="token punctuation">:</span> custom_bootloader</span>
<span class="line">  <span class="token key atrule">cached_generated_artifacts</span><span class="token punctuation">:</span>     <span class="token comment"># outputs: skip only if these all exist</span></span>
<span class="line">    <span class="token punctuation">-</span> cache/bootloader/u<span class="token punctuation">-</span>boot.bin</span>
<span class="line">    <span class="token punctuation">-</span> cache/bootloader/idbloader.img</span>
<span class="line">  <span class="token key atrule">depends_on</span><span class="token punctuation">:</span>                     <span class="token comment"># inputs: re-run if any of these change</span></span>
<span class="line">    <span class="token punctuation">-</span> ./dts/rk3576<span class="token punctuation">-</span>hd215<span class="token punctuation">-</span>uboot.dts</span>
<span class="line">    <span class="token punctuation">-</span> cache/kernel/rk3576<span class="token punctuation">-</span>hd215<span class="token punctuation">-</span>linux.dtb</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>A script is <strong>skipped</strong> only when all of the following hold:</p><ol><li><code>--clean</code> was not passed. <code>strux build --clean</code> deletes <code>dist/cache/{bsp}/</code> and forces every script to run.</li><li>The script declares at least one entry in <code>cached_generated_artifacts</code>. <strong>A script with no declared artifacts runs on every build</strong> — that&#39;s the right behavior for cheap idempotent scripts, and the reason <code>make_image</code> scripts often declare nothing.</li><li>Every declared artifact exists on disk.</li><li>A cache entry from a previous run exists, and the SHA256 hash of the <strong>script file itself</strong> matches it. Editing the script always triggers a re-run; you never need to list the script in <code>depends_on</code>.</li><li>Every file in <code>depends_on</code> exists and its hash matches the recorded one. A changed, missing, or never-recorded dependency triggers a re-run.</li></ol><p>When a script runs, Strux records the script hash, all dependency hashes, and the artifact list in the cache manifest at <code>dist/cache/{bsp}/.build-cache.json</code>.</p><div class="hint-container warning"><p class="hint-container-title">Dependencies must be files</p><p><code>depends_on</code> entries are hashed as individual files. An entry that points to a directory cannot be hashed, so the script re-runs on every build. List the specific files you depend on.</p></div><p>This two-sided design matters for chaining scripts: declaring another script&#39;s output (for example <code>cache/bootloader/spl/u-boot-spl.bin</code>) as your <code>depends_on</code> makes your script re-run exactly when the upstream script produced something new.</p><h2 id="path-resolution" tabindex="-1"><a class="header-anchor" href="#path-resolution"><span>Path resolution</span></a></h2><p><code>cached_generated_artifacts</code> and <code>depends_on</code> use a small prefix convention, resolved per-BSP:</p><table><thead><tr><th>Prefix</th><th>Resolves to</th><th>Use for</th></tr></thead><tbody><tr><td><code>cache/</code></td><td><code>dist/cache/{bsp}/</code></td><td>Intermediate build artifacts</td></tr><tr><td><code>output/</code></td><td><code>dist/output/{bsp}/</code></td><td>Final build products</td></tr><tr><td><code>./</code></td><td><code>bsp/{bsp}/</code> (the BSP directory) — <code>depends_on</code> only</td><td>Your DTS files, patches, blobs, configs</td></tr><tr><td>anything else</td><td><code>dist/</code></td><td>Rarely needed</td></tr></tbody></table><p>So <code>cache/rootfs-post.tar.gz</code> is the post-processed rootfs tarball in this BSP&#39;s cache, and <code>./boot/extlinux.conf</code> is a file inside the BSP folder. The same rules, with examples, are in the <a href="/bsp/reference/path-resolution.html" target="_blank" rel="noopener noreferrer">path resolution reference</a>.</p><h2 id="where-to-go-next" tabindex="-1"><a class="header-anchor" href="#where-to-go-next"><span>Where to go next</span></a></h2><ul><li><a href="/bsp/guide/scripts.html" target="_blank" rel="noopener noreferrer">Writing lifecycle scripts</a> — the practical guide: a first script, choosing hooks, debugging.</li><li><a href="/bsp/reference/build-steps.html" target="_blank" rel="noopener noreferrer">Build steps reference</a> — every hook, in exact execution order.</li><li><a href="/bsp/reference/environment-variables.html" target="_blank" rel="noopener noreferrer">Environment variables reference</a> — everything available to your scripts.</li><li><a href="/concepts/build-pipeline.html" target="_blank" rel="noopener noreferrer">Build pipeline</a> and <a href="/concepts/caching.html" target="_blank" rel="noopener noreferrer">caching</a> — how the built-in steps and their cache work.</li></ul>`,33)])])}const d=t(a,[["render",r]]),l=JSON.parse('{"path":"/bsp/concepts/lifecycle-scripts.html","title":"Lifecycle Scripts","lang":"en-US","frontmatter":{},"headers":[{"level":2,"title":"The mental model","slug":"the-mental-model","link":"#the-mental-model","children":[]},{"level":2,"title":"The hooks","slug":"the-hooks","link":"#the-hooks","children":[]},{"level":2,"title":"Execution environment","slug":"execution-environment","link":"#execution-environment","children":[]},{"level":2,"title":"Script caching","slug":"script-caching","link":"#script-caching","children":[]},{"level":2,"title":"Path resolution","slug":"path-resolution","link":"#path-resolution","children":[]},{"level":2,"title":"Where to go next","slug":"where-to-go-next","link":"#where-to-go-next","children":[]}],"git":{"updatedTime":1781317241000,"contributors":[{"name":"Miguel Medeiros","username":"","email":"miguel@medeirosconsulting.ca","commits":1}],"changelog":[{"hash":"e32366d86296aed0c45cf8447d8ea8147c1d03da","time":1781317241000,"email":"miguel@medeirosconsulting.ca","author":"Miguel Medeiros","message":"add docs"}]},"filePathRelative":"bsp/concepts/lifecycle-scripts.md"}');export{d as comp,l as data};
