import{_ as n,c as s,a,o as t}from"./app-CBOFuHUC.js";const o={};function i(r,e){return t(),s("div",null,[...e[0]||(e[0]=[a(`<h1 id="running-in-qemu" tabindex="-1"><a class="header-anchor" href="#running-in-qemu"><span>Running in QEMU</span></a></h1><p><code>strux run</code> boots your built image in QEMU — a virtual machine that emulates your target hardware — so you can see exactly what a device will do, from splash screen to app, without flashing anything. This page covers booting, debug and headless modes, configuring QEMU through <code>strux.yaml</code>, and passing real USB devices into the VM.</p><h2 id="booting-the-image" tabindex="-1"><a class="header-anchor" href="#booting-the-image"><span>Booting the image</span></a></h2><p>Build for the qemu BSP, then run:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh"><pre><code><span class="line">strux build qemu</span>
<span class="line">strux run</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>QEMU loads the kernel, initramfs, and root filesystem from your <code>dist/</code> folder and a window opens showing the boot: your splash logo, then your app full-screen. The window matches the resolution from your display configuration, and if <code>strux.yaml</code> defines multiple monitors, QEMU creates one virtual output per monitor.</p><p><code>strux run</code> picks the right QEMU binary for your target architecture (<code>qemu-system-x86_64</code>, <code>qemu-system-aarch64</code>, or <code>qemu-system-arm</code>) and uses hardware acceleration when the host supports it — HVF on macOS, KVM on Linux for x86_64. On Linux, GL acceleration is auto-enabled for Intel and AMD GPUs; NVIDIA and unknown GPUs use software rendering (set <code>STRUX_GL=1</code> to force GL on).</p><div class="hint-container warning"><p class="hint-container-title">Production images only</p><p><code>strux run</code> refuses to boot a development image (one built with <code>--dev</code> or by <code>strux dev</code>) — those expect a dev server to connect to. Rebuild without <code>--dev</code>, or use <a href="/guide/dev-mode.html" target="_blank" rel="noopener noreferrer"><code>strux dev</code></a> for the live development loop.</p></div><p>Press <code>Ctrl-C</code> in the terminal to stop the VM.</p><h2 id="debug-and-headless-modes" tabindex="-1"><a class="header-anchor" href="#debug-and-headless-modes"><span>Debug and headless modes</span></a></h2><table><thead><tr><th>Flag</th><th>Description</th></tr></thead><tbody><tr><td><code>--debug</code></td><td>Show the kernel console and systemd messages in your terminal during boot, instead of the quiet splash</td></tr><tr><td><code>--headless</code></td><td>Run without opening a host display window (the guest still has a virtual GPU and renders normally)</td></tr></tbody></table><p><code>--debug</code> is the first thing to reach for when an image doesn&#39;t boot to your app: it replaces the silent splash boot with the full console stream, so you can see which service failed.</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh"><pre><code><span class="line">strux run <span class="token parameter variable">--debug</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p><code>--headless</code> is useful on CI machines or over SSH, where there&#39;s no display to open a window on.</p><h2 id="configuring-qemu-in-strux-yaml" tabindex="-1"><a class="header-anchor" href="#configuring-qemu-in-strux-yaml"><span>Configuring QEMU in strux.yaml</span></a></h2><p>The <code>qemu</code> section of <code>strux.yaml</code> controls the VM:</p><div class="language-yaml line-numbers-mode" data-highlighter="prismjs" data-ext="yml"><pre><code><span class="line"><span class="token key atrule">qemu</span><span class="token punctuation">:</span></span>
<span class="line">  <span class="token key atrule">enabled</span><span class="token punctuation">:</span> <span class="token boolean important">true</span></span>
<span class="line">  <span class="token key atrule">network</span><span class="token punctuation">:</span> <span class="token boolean important">true</span></span>
<span class="line">  <span class="token key atrule">flags</span><span class="token punctuation">:</span></span>
<span class="line">    <span class="token punctuation">-</span> <span class="token punctuation">-</span>m 2G</span>
<span class="line">  <span class="token comment"># usb:</span></span>
<span class="line">  <span class="token comment">#   - vendor_id: &quot;1234&quot;</span></span>
<span class="line">  <span class="token comment">#     product_id: &quot;5678&quot;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>Memory and custom flags.</strong> The VM gets 2048 MB of RAM by default. <code>qemu.flags</code> is a list of extra QEMU command-line flags appended to the launch command — and if a custom flag matches a built-in one (like <code>-m</code>), the built-in is removed so yours wins. So <code>- -m 4G</code> raises memory to 4 GB. Any QEMU flag works here; each list entry is split on whitespace into flag and value.</p><p><strong>Networking.</strong> The VM uses QEMU user-mode networking with a virtio network card — the guest gets outbound network access through your machine without any host network configuration. From inside the guest, your host machine is reachable at <code>10.0.2.2</code> (QEMU&#39;s built-in gateway address). When the image runs under <a href="/guide/dev-mode.html" target="_blank" rel="noopener noreferrer">dev mode</a> with the WebKit inspector enabled, the inspector ports are additionally forwarded from your host into the guest.</p><h2 id="usb-passthrough" tabindex="-1"><a class="header-anchor" href="#usb-passthrough"><span>USB passthrough</span></a></h2><p>You can hand real USB devices on your machine — a barcode scanner, a serial adapter, a touch controller — through to the VM, so the guest sees them as if they were plugged into the device.</p><p>Devices are listed in <code>strux.yaml</code> under <code>qemu.usb</code> as vendor/product ID pairs (the 4-hex-digit identifiers every USB device carries). You don&#39;t have to find those IDs yourself — let <code>strux usb</code> do it:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh"><pre><code><span class="line">strux usb <span class="token function">add</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>This detects the USB devices currently connected to your machine and shows an interactive checklist (already-configured devices appear pre-selected). Toggle the ones you want and confirm; <code>strux.yaml</code> is updated for you:</p><div class="language-yaml line-numbers-mode" data-highlighter="prismjs" data-ext="yml"><pre><code><span class="line"><span class="token key atrule">qemu</span><span class="token punctuation">:</span></span>
<span class="line">  <span class="token key atrule">enabled</span><span class="token punctuation">:</span> <span class="token boolean important">true</span></span>
<span class="line">  <span class="token key atrule">network</span><span class="token punctuation">:</span> <span class="token boolean important">true</span></span>
<span class="line">  <span class="token key atrule">usb</span><span class="token punctuation">:</span></span>
<span class="line">    <span class="token punctuation">-</span> <span class="token key atrule">vendor_id</span><span class="token punctuation">:</span> <span class="token string">&quot;0c2e&quot;</span></span>
<span class="line">      <span class="token key atrule">product_id</span><span class="token punctuation">:</span> <span class="token string">&quot;0b81&quot;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>To review or remove configured devices later:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh"><pre><code><span class="line">strux usb list</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>It prints the configured devices and offers an interactive removal prompt. (<code>strux usb</code> on its own is a shortcut for <code>strux usb add</code>.)</p><p>On the next <code>strux run</code>, the configured devices are attached to the VM. On Linux this uses QEMU&#39;s <code>usb-host</code> device directly. On macOS it uses <code>usbredir</code> (a USB-over-socket protocol), which requires a QEMU built with usbredir support — <code>strux run</code> checks and tells you if yours isn&#39;t.</p><div class="hint-container tip"><p class="hint-container-title">Device busy?</p><p>If passthrough fails, make sure nothing on the host has claimed the device exclusively — close any app that&#39;s actively using it and run again.</p></div><h2 id="where-to-go-next" tabindex="-1"><a class="header-anchor" href="#where-to-go-next"><span>Where to go next</span></a></h2><ul><li><a href="/guide/dev-mode.html" target="_blank" rel="noopener noreferrer">Dev Mode</a> — don&#39;t rebuild-and-run by hand; get hot reload in the same VM.</li><li><a href="/guide/flashing.html" target="_blank" rel="noopener noreferrer">Flashing</a> — when QEMU looks right, put the image on hardware.</li><li><a href="/reference/strux-yaml.html" target="_blank" rel="noopener noreferrer">strux.yaml reference</a> — every key in the <code>qemu</code> section and beyond.</li></ul>`,32)])])}const l=n(o,[["render",i]]),c=JSON.parse('{"path":"/guide/running-qemu.html","title":"Running in QEMU","lang":"en-US","frontmatter":{},"headers":[{"level":2,"title":"Booting the image","slug":"booting-the-image","link":"#booting-the-image","children":[]},{"level":2,"title":"Debug and headless modes","slug":"debug-and-headless-modes","link":"#debug-and-headless-modes","children":[]},{"level":2,"title":"Configuring QEMU in strux.yaml","slug":"configuring-qemu-in-strux-yaml","link":"#configuring-qemu-in-strux-yaml","children":[]},{"level":2,"title":"USB passthrough","slug":"usb-passthrough","link":"#usb-passthrough","children":[]},{"level":2,"title":"Where to go next","slug":"where-to-go-next","link":"#where-to-go-next","children":[]}],"git":{"updatedTime":1781317241000,"contributors":[{"name":"Miguel Medeiros","username":"","email":"miguel@medeirosconsulting.ca","commits":1}],"changelog":[{"hash":"e32366d86296aed0c45cf8447d8ea8147c1d03da","time":1781317241000,"email":"miguel@medeirosconsulting.ca","author":"Miguel Medeiros","message":"add docs"}]},"filePathRelative":"guide/running-qemu.md"}');export{l as comp,c as data};
