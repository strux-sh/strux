import{_ as n,c as a,a as s,o as t}from"./app-B4LHn44G.js";const o={};function r(i,e){return t(),a("div",null,[...e[0]||(e[0]=[s(`<h1 id="bsp-development" tabindex="-1"><a class="header-anchor" href="#bsp-development"><span>BSP Development</span></a></h1><p>This track is for developers bringing Strux up on new hardware — taking a board that ships with a vendor Linux image (or nothing at all) and turning it into a supported Strux target. If you&#39;ve never done embedded board bring-up before, start here: each page explains the embedded concepts as you meet them.</p><h2 id="what-is-a-bsp" tabindex="-1"><a class="header-anchor" href="#what-is-a-bsp"><span>What is a BSP?</span></a></h2><p>A <strong>Board Support Package (BSP)</strong> is a folder in a Strux project that tells the build pipeline everything device-specific about one hardware target: which CPU architecture to compile for, which kernel and bootloader to build, which firmware files the board needs, which Debian packages to install, and how to assemble the final disk image.</p><p>Everything that is <em>not</em> board-specific — your frontend, your Go backend, the Cage compositor, WPE WebKit, the Debian root filesystem — is built by Strux the same way for every board. The BSP supplies the rest.</p><div class="hint-container tip"><p class="hint-container-title">New to these terms?</p><p>The <strong>kernel</strong> is the core of Linux — it talks to the hardware. The <strong>bootloader</strong> is the small program that runs first when the board powers on and loads the kernel. The <strong>root filesystem (rootfs)</strong> is everything else: the <code>/usr</code>, <code>/etc</code>, <code>/home</code> tree your OS runs from. These docs explain each one in depth when you get to it.</p></div><p>A project can hold any number of BSPs side by side, one folder each under <code>bsp/</code>. The <code>bsp</code> field in <code>strux.yaml</code> selects the default, and <code>strux build &lt;bsp&gt;</code> builds a specific one. Every project starts with a <code>qemu</code> BSP for local development; you add hardware BSPs next to it.</p><h2 id="what-s-in-a-bsp-folder" tabindex="-1"><a class="header-anchor" href="#what-s-in-a-bsp-folder"><span>What&#39;s in a BSP folder</span></a></h2><p>The only required file is <code>bsp.yaml</code>. Everything else is convention — but it&#39;s a useful convention, shared by all the example BSPs. Here is the layout of <code>hd215-rk3576</code>, a real BSP for a Rockchip RK3576 board:</p><div class="language-txt line-numbers-mode" data-highlighter="prismjs" data-ext="txt"><pre><code><span class="line">bsp/hd215-rk3576/</span>
<span class="line">├── bsp.yaml          # The BSP configuration — the only required file</span>
<span class="line">├── scripts/          # Lifecycle scripts (custom bootloader build, image creation, flashing)</span>
<span class="line">├── boot/             # Boot configuration templates (extlinux.conf, U-Boot boot script)</span>
<span class="line">├── configs/          # Saved kernel configuration (kernel.config)</span>
<span class="line">├── dts/              # Device tree sources for the kernel and bootloader</span>
<span class="line">├── patches/          # Kernel and U-Boot patches</span>
<span class="line">├── blobs/            # Vendor firmware binaries (DDR init, ARM Trusted Firmware, TEE)</span>
<span class="line">├── image/            # genimage partition layout configs</span>
<span class="line">├── overlay/          # Files copied verbatim into the root filesystem</span>
<span class="line">└── runtime/          # Go runtime extensions (board-specific backend APIs)</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>A minimal BSP — like the <code>qemu</code> one — is just <code>bsp.yaml</code> plus a single <code>scripts/make-image.sh</code>.</p><h2 id="the-bsp-yaml-file" tabindex="-1"><a class="header-anchor" href="#the-bsp-yaml-file"><span>The bsp.yaml file</span></a></h2><p>The file has two top-level keys: <code>strux_version</code> (the Strux version the BSP was written against) and <code>bsp</code>, which holds the actual configuration:</p><div class="language-yaml line-numbers-mode" data-highlighter="prismjs" data-ext="yml"><pre><code><span class="line"><span class="token key atrule">strux_version</span><span class="token punctuation">:</span> 0.3.0</span>
<span class="line"><span class="token key atrule">bsp</span><span class="token punctuation">:</span></span>
<span class="line">  <span class="token key atrule">name</span><span class="token punctuation">:</span> hd215<span class="token punctuation">-</span>rk3576</span>
<span class="line">  <span class="token key atrule">description</span><span class="token punctuation">:</span> <span class="token string">&quot;Medeiros IT HD215 RK3576 Board&quot;</span></span>
<span class="line">  <span class="token key atrule">arch</span><span class="token punctuation">:</span> arm64</span>
<span class="line">  <span class="token key atrule">hostname</span><span class="token punctuation">:</span> hd215</span>
<span class="line">  <span class="token key atrule">display</span><span class="token punctuation">:</span></span>
<span class="line">    <span class="token key atrule">resolution</span><span class="token punctuation">:</span> 1920x1080</span>
<span class="line">  <span class="token key atrule">cage</span><span class="token punctuation">:</span></span>
<span class="line">    <span class="token key atrule">hide_cursor</span><span class="token punctuation">:</span> <span class="token boolean important">true</span></span>
<span class="line">    <span class="token key atrule">env</span><span class="token punctuation">:</span></span>
<span class="line">      <span class="token punctuation">-</span> WLR_DRM_NO_MODIFIERS=1</span>
<span class="line">  <span class="token key atrule">scripts</span><span class="token punctuation">:</span> <span class="token punctuation">[</span><span class="token punctuation">]</span>        <span class="token comment"># Lifecycle scripts (see below)</span></span>
<span class="line">  <span class="token key atrule">boot</span><span class="token punctuation">:</span></span>
<span class="line">    <span class="token key atrule">bootloader</span><span class="token punctuation">:</span> <span class="token punctuation">{</span> <span class="token key atrule">enabled</span><span class="token punctuation">:</span> <span class="token boolean important">false</span> <span class="token punctuation">}</span></span>
<span class="line">    <span class="token key atrule">kernel</span><span class="token punctuation">:</span> <span class="token punctuation">{</span> <span class="token key atrule">custom_kernel</span><span class="token punctuation">:</span> <span class="token boolean important">false</span> <span class="token punctuation">}</span></span>
<span class="line">  <span class="token key atrule">rootfs</span><span class="token punctuation">:</span></span>
<span class="line">    <span class="token key atrule">overlay</span><span class="token punctuation">:</span> ./overlay</span>
<span class="line">    <span class="token key atrule">packages</span><span class="token punctuation">:</span> <span class="token punctuation">[</span>curl<span class="token punctuation">,</span> wget<span class="token punctuation">]</span></span>
<span class="line">  <span class="token key atrule">runtime</span><span class="token punctuation">:</span></span>
<span class="line">    <span class="token key atrule">extensions</span><span class="token punctuation">:</span> <span class="token punctuation">[</span><span class="token punctuation">]</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>What each section does:</p><table><thead><tr><th>Key</th><th>Required</th><th>Description</th></tr></thead><tbody><tr><td><code>name</code></td><td>yes</td><td>BSP identifier. Should match the folder name.</td></tr><tr><td><code>description</code></td><td>yes</td><td>Human-readable description.</td></tr><tr><td><code>arch</code></td><td>yes</td><td>Target CPU architecture: <code>host</code>, <code>arm64</code> (or <code>aarch64</code>), <code>x86_64</code> (or <code>amd64</code>), or <code>armhf</code> (also <code>armv7</code>/<code>arm</code>). Controls the cross-compiler for every build step.</td></tr><tr><td><code>hostname</code></td><td>yes</td><td>The device&#39;s network hostname.</td></tr><tr><td><code>display</code></td><td>no</td><td><code>resolution: &quot;WIDTHxHEIGHT&quot;</code> — the panel&#39;s native mode. Exposed to BSP scripts as <code>DISPLAY_WIDTH</code>/<code>DISPLAY_HEIGHT</code>.</td></tr><tr><td><code>cage</code></td><td>no</td><td>Compositor tuning: <code>env</code> (a list of <code>NAME=value</code> strings written to the device&#39;s cage environment file) and <code>hide_cursor</code> (sets <code>STRUX_HIDE_CURSOR=1</code> for touch-only devices).</td></tr><tr><td><code>scripts</code></td><td>no</td><td>Lifecycle scripts that hook into (or replace) build steps. See <a href="/bsp/concepts/lifecycle-scripts.html" target="_blank" rel="noopener noreferrer">Lifecycle Scripts</a>.</td></tr><tr><td><code>boot.kernel</code></td><td>no</td><td>Custom kernel configuration. <code>custom_kernel: false</code> uses the stock Debian kernel from the rootfs. See <a href="/bsp/guide/kernel.html" target="_blank" rel="noopener noreferrer">Custom Kernels</a>.</td></tr><tr><td><code>boot.bootloader</code></td><td>no</td><td>Bootloader configuration. <code>enabled: false</code> means no bootloader is built (fine for QEMU). See <a href="/bsp/guide/bootloader.html" target="_blank" rel="noopener noreferrer">Bootloaders</a>.</td></tr><tr><td><code>rootfs</code></td><td>no</td><td><code>overlay</code> (a folder copied verbatim into the root filesystem) and <code>packages</code> (extra Debian packages to install).</td></tr><tr><td><code>runtime</code></td><td>no</td><td><code>extensions</code> (board-specific Go APIs added to the Strux runtime) and <code>compatible_strux_api</code> (the Strux API versions this BSP has been tested with). See <a href="/bsp/guide/runtime-extensions.html" target="_blank" rel="noopener noreferrer">Runtime Extensions</a>.</td></tr></tbody></table><p>The full key-by-key reference, with every field and type, is at <a href="/bsp/reference/bsp-yaml.html" target="_blank" rel="noopener noreferrer">bsp.yaml Reference</a>.</p><div class="hint-container tip"><p class="hint-container-title">A note on <code>cage.env</code></p><p>The values in the <code>qemu</code> BSP — <code>WLR_DRM_NO_MODIFIERS=1</code>, <code>WLR_NO_HARDWARE_CURSORS=1</code> — are wlroots display-stack workarounds that real boards often need too. The <code>ht109-rk3576s</code> tablet BSP uses <code>STRUX_OUTPUT_TRANSFORM=90</code> here to rotate a portrait panel into landscape. See <a href="/concepts/display-stack.html" target="_blank" rel="noopener noreferrer">Display Stack</a>.</p></div><h2 id="how-a-bsp-plugs-into-the-build-pipeline" tabindex="-1"><a class="header-anchor" href="#how-a-bsp-plugs-into-the-build-pipeline"><span>How a BSP plugs into the build pipeline</span></a></h2><p><code>strux build &lt;bsp&gt;</code> runs a fixed sequence of steps inside the <code>strux-builder</code> Docker container: frontend → application → cage → wpe → client → kernel → bootloader → rootfs → final image. The BSP shapes this pipeline in three ways:</p><ol><li><strong>Configuration.</strong> <code>arch</code> selects the cross-compiler; <code>rootfs.packages</code> and <code>rootfs.overlay</code> feed the rootfs steps; <code>boot.kernel</code> and <code>boot.bootloader</code> configure their steps.</li><li><strong>Conditional steps.</strong> The kernel step only runs when <code>boot.kernel.custom_kernel: true</code>, and the bootloader step only runs when <code>boot.bootloader.enabled: true</code>. A QEMU-only BSP skips both.</li><li><strong>Lifecycle scripts.</strong> Every entry in <code>scripts</code> declares a <code>location</code> and a <code>step</code> — a hook like <code>before_rootfs</code>, <code>after_bootloader</code>, or <code>make_image</code>. Strux runs the script inside Docker at that point in the pipeline, with environment variables describing the build (<code>BSP_NAME</code>, <code>TARGET_ARCH</code>, <code>PROJECT_DIST_CACHE_FOLDER</code>, and more — see <a href="/bsp/reference/environment-variables.html" target="_blank" rel="noopener noreferrer">Environment Variables</a>). Three special steps <em>replace</em> built-in behavior instead of hooking around it: <code>custom_kernel</code>, <code>custom_bootloader</code>, and <code>make_image</code>.</li></ol><p>The <code>make_image</code> step deserves emphasis: <strong>Strux builds a root filesystem tarball, but the BSP turns it into a bootable disk image.</strong> Only the BSP knows the board&#39;s partition layout, where the bootloader lives, and what the flashing tool expects. Every BSP needs at least one <code>make_image</code> script.</p><p>Scripts participate in the <a href="/concepts/caching.html" target="_blank" rel="noopener noreferrer">build cache</a>: declare <code>cached_generated_artifacts</code> and <code>depends_on</code> on a script, and Strux skips it when its outputs exist and no dependency has changed. The path shorthand (<code>cache/</code> → <code>dist/cache/{bsp}/</code>, <code>output/</code> → <code>dist/output/{bsp}/</code>, <code>./</code> → the BSP folder) is documented in <a href="/bsp/reference/path-resolution.html" target="_blank" rel="noopener noreferrer">Path Resolution</a>.</p><h2 id="the-recommended-path" tabindex="-1"><a class="header-anchor" href="#the-recommended-path"><span>The recommended path</span></a></h2><p>Don&#39;t write a BSP from a blank file. The path that works:</p><ol><li><strong>Start from the qemu BSP.</strong> Copy <code>bsp/qemu/</code> to <code>bsp/&lt;your-board&gt;/</code>, rename it, and get <code>strux build &lt;your-board&gt;</code> producing an image with no custom kernel and no bootloader. This proves the pipeline, your packages, and your overlay before any hardware is involved.</li><li><strong>Add the kernel.</strong> Switch on <code>custom_kernel</code>, point <code>source</code> at the kernel tree your board needs (often a vendor fork), and get it compiling with the right device tree. See <a href="/bsp/guide/kernel.html" target="_blank" rel="noopener noreferrer">Custom Kernels</a>.</li><li><strong>Add the bootloader.</strong> Enable the bootloader, supply the defconfig, patches, and any vendor boot blobs. See <a href="/bsp/guide/bootloader.html" target="_blank" rel="noopener noreferrer">Bootloaders</a>.</li><li><strong>Make it flashable.</strong> Write the <code>make_image</code> script for the board&#39;s partition layout, then a <code>flash_script</code> so <code>strux flash</code> works. See <a href="/bsp/guide/flash-scripts.html" target="_blank" rel="noopener noreferrer">Flash Scripts</a>.</li></ol><p>The <a href="/bsp/guide/writing-a-bsp.html" target="_blank" rel="noopener noreferrer">Writing a BSP</a> guide walks this exact path with real values.</p><h2 id="where-to-go-next" tabindex="-1"><a class="header-anchor" href="#where-to-go-next"><span>Where to go next</span></a></h2><ul><li><a href="/bsp/guide/writing-a-bsp.html" target="_blank" rel="noopener noreferrer">Writing a BSP</a> — the from-scratch walkthrough.</li><li><a href="/bsp/guide/kernel.html" target="_blank" rel="noopener noreferrer">Custom Kernels</a> — sources, defconfigs, fragments, patches, device trees.</li><li><a href="/bsp/guide/bootloader.html" target="_blank" rel="noopener noreferrer">Bootloaders</a> — U-Boot builds, boot methods, vendor blobs.</li><li><a href="/bsp/concepts/lifecycle-scripts.html" target="_blank" rel="noopener noreferrer">Lifecycle Scripts</a> — every hook, in pipeline order.</li><li><a href="/bsp/guide/runtime-extensions.html" target="_blank" rel="noopener noreferrer">Runtime Extensions</a> — exposing board hardware (WiFi, sensors) to the app.</li><li><a href="/bsp/guide/examples.html" target="_blank" rel="noopener noreferrer">Example BSPs</a> — the real BSPs these docs draw from.</li><li><a href="/bsp/reference/bsp-yaml.html" target="_blank" rel="noopener noreferrer">bsp.yaml Reference</a> — every key, exhaustively.</li></ul>`,29)])])}const d=n(o,[["render",r]]),c=JSON.parse(`{"path":"/bsp/guide/introduction.html","title":"BSP Development","lang":"en-US","frontmatter":{},"headers":[{"level":2,"title":"What is a BSP?","slug":"what-is-a-bsp","link":"#what-is-a-bsp","children":[]},{"level":2,"title":"What's in a BSP folder","slug":"what-s-in-a-bsp-folder","link":"#what-s-in-a-bsp-folder","children":[]},{"level":2,"title":"The bsp.yaml file","slug":"the-bsp-yaml-file","link":"#the-bsp-yaml-file","children":[]},{"level":2,"title":"How a BSP plugs into the build pipeline","slug":"how-a-bsp-plugs-into-the-build-pipeline","link":"#how-a-bsp-plugs-into-the-build-pipeline","children":[]},{"level":2,"title":"The recommended path","slug":"the-recommended-path","link":"#the-recommended-path","children":[]},{"level":2,"title":"Where to go next","slug":"where-to-go-next","link":"#where-to-go-next","children":[]}],"git":{"updatedTime":1781317241000,"contributors":[{"name":"Miguel Medeiros","username":"","email":"miguel@medeirosconsulting.ca","commits":1}],"changelog":[{"hash":"e32366d86296aed0c45cf8447d8ea8147c1d03da","time":1781317241000,"email":"miguel@medeirosconsulting.ca","author":"Miguel Medeiros","message":"add docs"}]},"filePathRelative":"bsp/guide/introduction.md"}`);export{d as comp,c as data};
