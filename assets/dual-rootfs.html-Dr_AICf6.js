import{_ as t,c as a,a as o,o as s}from"./app-CBOFuHUC.js";const n={};function i(r,e){return s(),a("div",null,[...e[0]||(e[0]=[o(`<h1 id="dual-rootfs-a-b-updates" tabindex="-1"><a class="header-anchor" href="#dual-rootfs-a-b-updates"><span>Dual Rootfs &amp; A/B Updates</span></a></h1><p>This page explains how Strux applies a full-system update on a device without risking a brick: two root filesystem slots, A and B. An update is written to the slot you&#39;re <em>not</em> running, and the bootloader switches over only if the new slot boots. If it doesn&#39;t, the device falls back to the old one.</p><div class="hint-container warning"><p class="hint-container-title">Experimental</p><p>The A/B update system is new in v0.3.0 and still hardening. The <strong>on-device contract described here</strong> — partition labels, kernel cmdline keys, boot-environment format, and state-file locations — is what a BSP must honor today, and these pages document it so BSP authors can ship compatible builds. But the design may still change in a future release. Treat the conventions below as required for v0.3.0 builds, and expect to revisit them when you upgrade Strux.</p></div><h2 id="the-mental-model" tabindex="-1"><a class="header-anchor" href="#the-mental-model"><span>The mental model</span></a></h2><p>A normal embedded device has one root filesystem. Updating it in place is dangerous: lose power halfway through writing the new OS and the device won&#39;t boot. The fix is <strong>two slots</strong>:</p><div class="language-txt line-numbers-mode" data-highlighter="prismjs" data-ext="txt"><pre><code><span class="line">                 ┌──────────────────────────────────────┐</span>
<span class="line">   running ────▶ │  strux-rootfs-a   (active)           │</span>
<span class="line">                 ├──────────────────────────────────────┤</span>
<span class="line">   update ─────▶ │  strux-rootfs-b   (inactive ← write) │</span>
<span class="line">                 ├──────────────────────────────────────┤</span>
<span class="line">                 │  strux-data       (boot state, FAT32)│</span>
<span class="line">                 └──────────────────────────────────────┘</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>You boot from slot A. An update writes the new OS into slot B while A keeps running. The device then marks B as <em>pending</em>, reboots, and the bootloader tries B. If B boots cleanly, it becomes the new active slot. If B fails to boot, the bootloader exhausts its retry count and falls back to A — the device is never left without a working OS.</p><p>A third partition, <code>strux-data</code>, is a small FAT32 volume holding the boot-selection state. It survives updates (neither rootfs write touches it) and is readable by both U-Boot and Linux.</p><h2 id="who-builds-what" tabindex="-1"><a class="header-anchor" href="#who-builds-what"><span>Who builds what</span></a></h2><p>This is the key thing to understand. The work is split between <strong>Strux core</strong> and the <strong>BSP</strong>:</p><table><thead><tr><th>Strux core provides</th><th>The BSP author provides</th></tr></thead><tbody><tr><td>The bundle format (<code>.struxb</code>) and signing</td><td>The A/B partition layout (image generation)</td></tr><tr><td>The on-device updater (in the strux client)</td><td>The U-Boot boot script that selects a slot</td></tr><tr><td>The slot-selection <em>contract</em> (labels, cmdline, state files)</td><td>A boot config that installs the update public key</td></tr><tr><td>Signature verification on device</td><td>Wiring all of the above into the build via scripts</td></tr></tbody></table><p>The updater that applies a bundle is shipped by Strux and compiled into every image — a BSP author does <strong>not</strong> write update logic. What a BSP author must do is lay out partitions and configure the bootloader so they match the contract the updater expects. Get the labels and cmdline right and updates &quot;just work&quot;; get them wrong and the updater can&#39;t find its slots.</p><h2 id="the-bundle-struxb" tabindex="-1"><a class="header-anchor" href="#the-bundle-struxb"><span>The bundle: <code>.struxb</code></span></a></h2><p>An update is distributed as a signed bundle, produced by <a href="/reference/cli.html#strux-update-bundle" target="_blank" rel="noopener noreferrer"><code>strux update bundle</code></a> (or automatically when <code>update.auto_bundle</code> is set — see <a href="/guide/updates.html" target="_blank" rel="noopener noreferrer">Updates</a>). It is a gzip-compressed tar archive containing:</p><ul><li><code>manifest.json</code> — metadata: schema (<code>dev.strux.update.bundle.v1</code>), target BSP, version, and a <code>payload</code> block describing the rootfs image with its size and SHA-256.</li><li><code>manifest.sig</code> — an RSA-PSS / SHA-512 signature over <code>manifest.json</code>, base64-encoded.</li><li><code>rootfs.img</code> — the full root filesystem image.</li></ul><p>The payload is a <strong>full rootfs</strong>, not a delta. The signature covers the manifest, and the manifest pins the payload&#39;s SHA-256 — so verifying the manifest signature and then checking the payload hash proves the whole bundle is authentic and intact.</p><p>Bundles are signed with a 4096-bit RSA key you generate with <a href="/reference/cli.html#strux-update-gen-keypair" target="_blank" rel="noopener noreferrer"><code>strux update gen-keypair</code></a>. The private key (<code>strux-update.key</code>) signs; the public key (<code>strux-update.pub</code>) is embedded in the image and used on-device to verify. See <a href="/guide/updates.html" target="_blank" rel="noopener noreferrer">Updates</a> for the signing workflow.</p><h2 id="the-on-device-contract" tabindex="-1"><a class="header-anchor" href="#the-on-device-contract"><span>The on-device contract</span></a></h2><p>These are the conventions a BSP must implement so the Strux updater can do its job. They are fixed strings — the updater hard-codes them.</p><h3 id="partition-labels" tabindex="-1"><a class="header-anchor" href="#partition-labels"><span>Partition labels</span></a></h3><p>The updater locates slots by <strong>partition label</strong> via <code>/dev/disk/by-partlabel/</code>:</p><table><thead><tr><th>Label</th><th>Filesystem</th><th>Role</th></tr></thead><tbody><tr><td><code>strux-rootfs-a</code></td><td>ext4</td><td>Root filesystem slot A</td></tr><tr><td><code>strux-rootfs-b</code></td><td>ext4</td><td>Root filesystem slot B</td></tr><tr><td><code>strux-data</code></td><td>FAT32</td><td>Boot-selection state</td></tr></tbody></table><p>Both rootfs slots must be the same size (an update written to either must fit). The filesystem label of each slot is expected to match its partition label; the updater relabels a freshly written slot with <code>e2label</code> to keep them in sync.</p><h3 id="kernel-command-line" tabindex="-1"><a class="header-anchor" href="#kernel-command-line"><span>Kernel command line</span></a></h3><p>The bootloader must tell the running system which slot it booted from, by adding to the kernel command line:</p><div class="language-txt line-numbers-mode" data-highlighter="prismjs" data-ext="txt"><pre><code><span class="line">strux.slot=A          # or B</span>
<span class="line">strux.data=PARTLABEL=strux-data</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>On boot, the updater reads <code>/proc/cmdline</code>, finds <code>strux.slot=</code>, and that tells it which slot is active — and therefore which slot (the other one) to write the next update into. Typical full bootargs also set <code>root=PARTLABEL=strux-rootfs-a rootfstype=ext4 rw rootwait</code> for the selected slot.</p><h3 id="the-strux-data-partition-layout" tabindex="-1"><a class="header-anchor" href="#the-strux-data-partition-layout"><span>The <code>strux-data</code> partition layout</span></a></h3><p>The FAT32 data partition is mounted at <code>/strux-data</code>, and the boot state lives under a <code>strux/</code> subfolder:</p><table><thead><tr><th>Path (on the device)</th><th>Purpose</th></tr></thead><tbody><tr><td><code>/strux-data/strux/BOOTENV.TXT</code></td><td>Primary boot environment</td></tr><tr><td><code>/strux-data/strux/BOOTBAK.TXT</code></td><td>Backup copy (used if the primary is corrupt)</td></tr><tr><td><code>/strux-data/strux/update-state.json</code></td><td>Update state mirrored for the runtime/UI</td></tr><tr><td><code>/strux-data/strux/boot.scr</code></td><td>Compiled U-Boot boot script (optional placement)</td></tr></tbody></table><p><code>BOOTENV.TXT</code> is a plain <code>key=value</code> file the bootloader and the updater both read and write:</p><div class="language-txt line-numbers-mode" data-highlighter="prismjs" data-ext="txt"><pre><code><span class="line">strux_active=A</span>
<span class="line">strux_pending=</span>
<span class="line">strux_tries=0</span>
<span class="line">strux_generation=1</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><table><thead><tr><th>Key</th><th>Meaning</th></tr></thead><tbody><tr><td><code>strux_active</code></td><td>The slot to boot normally (<code>A</code> or <code>B</code>)</td></tr><tr><td><code>strux_pending</code></td><td>A slot being trialed after an update; empty when none</td></tr><tr><td><code>strux_tries</code></td><td>Boot attempts remaining for the pending slot before fallback</td></tr><tr><td><code>strux_generation</code></td><td>Counter incremented on each successful switch</td></tr></tbody></table><p>Two copies (<code>BOOTENV.TXT</code> + <code>BOOTBAK.TXT</code>) give crash resilience: if power is lost mid-write, the bootloader can fall back to the backup.</p><h3 id="the-update-public-key" tabindex="-1"><a class="header-anchor" href="#the-update-public-key"><span>The update public key</span></a></h3><p>The signing public key must be installed in the rootfs at:</p><div class="language-txt line-numbers-mode" data-highlighter="prismjs" data-ext="txt"><pre><code><span class="line">/etc/strux/update.pub</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>The updater loads this PEM key, requires it to be RSA ≥ 4096 bits, and verifies every bundle&#39;s manifest signature against it before writing anything. A BSP&#39;s boot-config step is responsible for copying the project&#39;s <code>strux-update.pub</code> to this path when updates are enabled.</p><h2 id="how-an-update-plays-out" tabindex="-1"><a class="header-anchor" href="#how-an-update-plays-out"><span>How an update plays out</span></a></h2><p>Putting the contract in motion, here is the full sequence when a bundle is applied:</p><ol><li><strong>Verify.</strong> The updater opens the <code>.struxb</code>, extracts <code>manifest.json</code> + <code>manifest.sig</code>, and verifies the signature with <code>/etc/strux/update.pub</code> (RSA-PSS / SHA-512). It checks the schema and that the bundle&#39;s target BSP matches the installed one.</li><li><strong>Pick the inactive slot.</strong> It reads <code>strux.slot=</code> from <code>/proc/cmdline</code> and chooses the opposite slot&#39;s device by partlabel.</li><li><strong>Write &amp; hash.</strong> It streams <code>rootfs.img</code> straight onto the inactive slot&#39;s block device, computing SHA-256 as it goes and comparing against the manifest. It relabels the slot&#39;s filesystem to match.</li><li><strong>Mark pending.</strong> It updates the boot environment: <code>strux_pending</code> = the just-written slot, <code>strux_tries</code> = 3, and bumps <code>strux_generation</code>, writing both <code>BOOTENV.TXT</code> and <code>BOOTBAK.TXT</code>.</li><li><strong>Reboot.</strong> The device reboots. The bootloader sees a pending slot with tries remaining, decrements the counter, and boots it.</li><li><strong>Confirm or fall back.</strong> If the pending slot boots successfully, the updater promotes it: <code>strux_active</code> = pending, <code>strux_pending</code> cleared, tries reset. If it fails to boot, the bootloader&#39;s retry logic exhausts <code>strux_tries</code> and reverts to <code>strux_active</code> — the old, known-good slot.</li></ol><p>The bootloader half of steps 5–6 is the part a BSP author writes (a U-Boot boot script that reads <code>BOOTENV.TXT</code> and implements the try/fallback logic). The rest is the Strux client.</p><h2 id="what-a-bsp-author-must-deliver" tabindex="-1"><a class="header-anchor" href="#what-a-bsp-author-must-deliver"><span>What a BSP author must deliver</span></a></h2><p>To support A/B updates on a board, a BSP needs all of the following, gated on updates being enabled for the build:</p><ol><li><strong>An A/B partition image.</strong> An image-generation config (e.g. genimage) defining <code>strux-rootfs-a</code>, <code>strux-rootfs-b</code> (equal-size ext4), and <code>strux-data</code> (FAT32), plus the board&#39;s fixed bootloader blob offsets. A non-update build can use a simpler single-rootfs layout.</li><li><strong>An image build script</strong> that, when updates are enabled, produces the two-slot image and pre-populates <code>strux-data</code> with an initial <code>BOOTENV.TXT</code> (active = A, no pending).</li><li><strong>A U-Boot boot script</strong> that reads <code>BOOTENV.TXT</code> from <code>strux-data</code>, selects active-vs-pending with the tries/fallback logic, loads the kernel from the chosen slot&#39;s partlabel, and sets the <code>strux.slot=</code> / <code>strux.data=</code> bootargs.</li><li><strong>A boot-config step</strong> that installs <code>/etc/strux/update.pub</code> from the project key and adds the <code>strux-data</code> mount to <code>fstab</code>.</li></ol><p>The real Rockchip BSPs under <code>test/bsp/</code> (notably <code>hd215-rk3576</code>) implement all four and are the reference to copy from — see <a href="/bsp/guide/examples.html" target="_blank" rel="noopener noreferrer">BSP Examples</a>.</p><h2 id="current-limitations" tabindex="-1"><a class="header-anchor" href="#current-limitations"><span>Current limitations</span></a></h2><p>Because the system is experimental, note what it does <strong>not</strong> do yet in v0.3.0:</p><ul><li><strong>Full images only</strong> — there are no delta/incremental updates; each bundle carries a complete rootfs.</li><li><strong>No built-in update server or scheduling</strong> — bundles are delivered out of band (today, pushed via the dev server with <a href="/reference/cli.html#strux-update-send" target="_blank" rel="noopener noreferrer"><code>strux update send</code></a>); there&#39;s no OTA check-in/availability protocol.</li><li><strong>Limited field testing</strong> — the boot-validation and fallback paths are implemented but new; validate them on your hardware before relying on them in production.</li></ul><h2 id="where-to-go-next" tabindex="-1"><a class="header-anchor" href="#where-to-go-next"><span>Where to go next</span></a></h2><ul><li><a href="/guide/updates.html" target="_blank" rel="noopener noreferrer">Updates</a> — generating keys, building, and sending bundles.</li><li><a href="/bsp/guide/examples.html" target="_blank" rel="noopener noreferrer">BSP Examples</a> — the reference A/B BSP to copy from.</li><li><a href="/bsp/reference/bsp-yaml.html" target="_blank" rel="noopener noreferrer">bsp.yaml reference</a> — bootloader and script configuration.</li></ul>`,51)])])}const l=t(n,[["render",i]]),c=JSON.parse('{"path":"/bsp/concepts/dual-rootfs.html","title":"Dual Rootfs & A/B Updates","lang":"en-US","frontmatter":{},"headers":[{"level":2,"title":"The mental model","slug":"the-mental-model","link":"#the-mental-model","children":[]},{"level":2,"title":"Who builds what","slug":"who-builds-what","link":"#who-builds-what","children":[]},{"level":2,"title":"The bundle: .struxb","slug":"the-bundle-struxb","link":"#the-bundle-struxb","children":[]},{"level":2,"title":"The on-device contract","slug":"the-on-device-contract","link":"#the-on-device-contract","children":[{"level":3,"title":"Partition labels","slug":"partition-labels","link":"#partition-labels","children":[]},{"level":3,"title":"Kernel command line","slug":"kernel-command-line","link":"#kernel-command-line","children":[]},{"level":3,"title":"The strux-data partition layout","slug":"the-strux-data-partition-layout","link":"#the-strux-data-partition-layout","children":[]},{"level":3,"title":"The update public key","slug":"the-update-public-key","link":"#the-update-public-key","children":[]}]},{"level":2,"title":"How an update plays out","slug":"how-an-update-plays-out","link":"#how-an-update-plays-out","children":[]},{"level":2,"title":"What a BSP author must deliver","slug":"what-a-bsp-author-must-deliver","link":"#what-a-bsp-author-must-deliver","children":[]},{"level":2,"title":"Current limitations","slug":"current-limitations","link":"#current-limitations","children":[]},{"level":2,"title":"Where to go next","slug":"where-to-go-next","link":"#where-to-go-next","children":[]}],"git":{"updatedTime":1781318751000,"contributors":[{"name":"Miguel Medeiros","username":"","email":"miguel@medeirosconsulting.ca","commits":1}],"changelog":[{"hash":"fa7599236d7ec48609b6b87cb11f6cd0312ca9af","time":1781318751000,"email":"miguel@medeirosconsulting.ca","author":"Miguel Medeiros","message":"update docs"}]},"filePathRelative":"bsp/concepts/dual-rootfs.md"}');export{l as comp,c as data};
