import{_ as n,c as s,a,o as t}from"./app-CBOFuHUC.js";const o={};function i(l,e){return t(),s("div",null,[...e[0]||(e[0]=[a(`<h1 id="display-stack" tabindex="-1"><a class="header-anchor" href="#display-stack"><span>Display Stack</span></a></h1><p>This page explains how your web app gets onto physical screens: the Cage compositor, the Cog browser, and the <code>display</code> configuration in <code>strux.yaml</code> that maps URL paths to monitors, touchscreens to outputs, and handles rotated panels.</p><h2 id="what-s-a-compositor-anyway" tabindex="-1"><a class="header-anchor" href="#what-s-a-compositor-anyway"><span>What&#39;s a compositor, anyway?</span></a></h2><p>On a desktop, a window manager juggles overlapping windows. A kiosk doesn&#39;t need any of that — it needs exactly one app, full-screen, forever. A <strong>Wayland compositor</strong> is the Linux component that owns the displays and input devices and decides what gets drawn; <strong>Cage</strong> is a tiny one built for the kiosk case: it shows a single maximized application per output and nothing else. No taskbar, no alt-tab, nothing to escape into.</p><p>Strux ships a modified fork of Cage (the source is in your project at <code>dist/artifacts/cage/</code> — see <a href="/concepts/artifacts.html" target="_blank" rel="noopener noreferrer">Artifacts</a>). The fork adds what kiosks need: rendering the boot splash itself for a seamless logo-to-app transition, spawning one browser per monitor, per-output rotation, and mapping touchscreens to the right screen.</p><p>Inside Cage runs <strong>Cog</strong>, a minimal browser shell around WPE WebKit — all of the rendering engine, none of the browser UI. For web developers: think of each Cog instance as one full-screen, chromeless browser tab pointed at your backend.</p><h2 id="from-yaml-to-pixels" tabindex="-1"><a class="header-anchor" href="#from-yaml-to-pixels"><span>From YAML to pixels</span></a></h2><p>Displays are configured in <code>strux.yaml</code>. Here&#39;s a real two-monitor setup — a built-in DSI touch panel showing the main UI, and an HDMI output showing a different page of the same app:</p><div class="language-yaml line-numbers-mode" data-highlighter="prismjs" data-ext="yml"><pre><code><span class="line"><span class="token key atrule">display</span><span class="token punctuation">:</span></span>
<span class="line">  <span class="token key atrule">monitors</span><span class="token punctuation">:</span></span>
<span class="line">    <span class="token punctuation">-</span> <span class="token key atrule">path</span><span class="token punctuation">:</span> /</span>
<span class="line">      <span class="token key atrule">resolution</span><span class="token punctuation">:</span> 1920x1080</span>
<span class="line">      <span class="token key atrule">names</span><span class="token punctuation">:</span></span>
<span class="line">        <span class="token punctuation">-</span> DSI<span class="token punctuation">-</span><span class="token number">1</span></span>
<span class="line">        <span class="token punctuation">-</span> Virtual<span class="token punctuation">-</span><span class="token number">1</span></span>
<span class="line">      <span class="token key atrule">input_devices</span><span class="token punctuation">:</span></span>
<span class="line">        <span class="token punctuation">-</span> ILITEK</span>
<span class="line">    <span class="token punctuation">-</span> <span class="token key atrule">path</span><span class="token punctuation">:</span> /tv</span>
<span class="line">      <span class="token key atrule">resolution</span><span class="token punctuation">:</span> 1920x1080</span>
<span class="line">      <span class="token key atrule">names</span><span class="token punctuation">:</span></span>
<span class="line">        <span class="token punctuation">-</span> HDMI<span class="token punctuation">-</span>A<span class="token punctuation">-</span><span class="token number">1</span></span>
<span class="line">        <span class="token punctuation">-</span> Virtual<span class="token punctuation">-</span><span class="token number">2</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>What each key does (schema: <code>DisplaySchema</code> in <code>src/types/main-yaml.ts</code>):</p><table><thead><tr><th>Key</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>path</code></td><td>string (required)</td><td>URL path appended to your backend&#39;s base URL (<code>http://localhost:8080</code>). This monitor loads <code>http://localhost:8080</code> + <code>path</code>.</td></tr><tr><td><code>resolution</code></td><td><code>WIDTHxHEIGHT</code></td><td>Mode to set on this output (applied via <code>wlr-randr</code> before the browser starts).</td></tr><tr><td><code>transform</code></td><td>see below</td><td>Rotation/flip for this output.</td></tr><tr><td><code>names</code></td><td>string[]</td><td>Output names this entry matches — list several to cover hardware and QEMU.</td></tr><tr><td><code>input_devices</code></td><td>string[]</td><td>Input device name substrings (e.g. the touch controller&#39;s name) bound to this output.</td></tr></tbody></table><div class="hint-container tip"><p class="hint-container-title">Where do output names come from?</p><p>The kernel names each video connector: <code>DSI-1</code> for a ribbon-cable panel, <code>HDMI-A-1</code> for the first HDMI port, <code>Virtual-1</code>/<code>Virtual-2</code> in QEMU. Listing both a hardware name and a <code>Virtual-*</code> name in <code>names</code> lets the same config work on the device and in <code>strux dev</code>.</p></div><p>The plumbing between that YAML and the screen:</p><ol><li>At build time, the CLI writes the monitor list to <code>.display-config.json</code> and the input mappings to <code>.input-map</code>, and the rootfs-post step installs them as <code>/strux/.display-config.json</code> and <code>/strux/.input-map</code> on the device.</li><li>At boot, the Strux client reads the config and writes a display map file, then launches Cage with <code>--display-map</code> and <code>--input-map</code>. Cage always runs in <em>per-view</em> mode: each app window is confined to its own output.</li><li>For every output it finds, Cage looks up the matching entry by name and spawns a Cog instance through <code>/strux/strux-run-cog.sh</code> — a user-editable script (in <code>dist/artifacts/scripts/</code>), so you can customize browser flags per output or even swap Cog for something else.</li><li>Each Cog loads your backend URL plus that monitor&#39;s <code>path</code>. Your frontend reads <code>location.pathname</code> and renders the right view — one app, one backend, multiple screens.</li></ol><p>A connected output with no matching entry isn&#39;t left black: Cage points it at a built-in &quot;not configured&quot; page (<code>/strux/.not-configured.html</code>, also user-editable), so a misnamed output is immediately visible and self-explanatory.</p><div class="language-txt line-numbers-mode" data-highlighter="prismjs" data-ext="txt"><pre><code><span class="line">strux.yaml display.monitors</span>
<span class="line">        │ build</span>
<span class="line">        ▼</span>
<span class="line">/strux/.display-config.json ──▶ strux client ──▶ cage --display-map ... --input-map ...</span>
<span class="line">                                                   ├─ output DSI-1    → strux-run-cog.sh → cog → localhost:8080/</span>
<span class="line">                                                   └─ output HDMI-A-1 → strux-run-cog.sh → cog → localhost:8080/tv</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>If you configure nothing, you still get a working single display: the build falls back to one monitor at <code>path: /</code> using the BSP&#39;s <code>display.resolution</code>.</p><h2 id="touch-input-mapping" tabindex="-1"><a class="header-anchor" href="#touch-input-mapping"><span>Touch input mapping</span></a></h2><p>With one screen, touch &quot;just works.&quot; With two, the compositor has to know <em>which</em> screen a touch event belongs to — otherwise tapping the panel might click things on the HDMI output. That&#39;s what <code>input_devices</code> solves: each listed string is matched as a substring against input device names, and matching devices have their coordinates mapped to that monitor&#39;s first listed output. In the example above, the ILITEK touch controller is bound to <code>DSI-1</code>, so touches always land on the panel&#39;s UI regardless of how the outputs are arranged.</p><p>Devices with no mapping fall back to the first output. To find your touch device&#39;s name, check the kernel log or <code>/proc/bus/input/devices</code> on the device.</p><h2 id="rotation-transforms" tabindex="-1"><a class="header-anchor" href="#rotation-transforms"><span>Rotation transforms</span></a></h2><p>Many tablet-style panels are physically portrait but mounted landscape. The <code>transform</code> key rotates an output in the compositor — the panel still scans out portrait, but everything you render (splash included) appears correctly rotated, and touch coordinates are rotated to match.</p><p>Accepted values: <code>normal</code>, <code>0</code>, <code>90</code>, <code>180</code>, <code>270</code>, <code>flipped</code>, <code>flipped-90</code>, <code>flipped-180</code>, <code>flipped-270</code> (the numeric ones can be written without quotes). Per-monitor in <code>strux.yaml</code>:</p><div class="language-yaml line-numbers-mode" data-highlighter="prismjs" data-ext="yml"><pre><code><span class="line"><span class="token key atrule">display</span><span class="token punctuation">:</span></span>
<span class="line">  <span class="token key atrule">monitors</span><span class="token punctuation">:</span></span>
<span class="line">    <span class="token punctuation">-</span> <span class="token key atrule">path</span><span class="token punctuation">:</span> /</span>
<span class="line">      <span class="token key atrule">transform</span><span class="token punctuation">:</span> <span class="token number">90</span></span>
<span class="line">      <span class="token key atrule">names</span><span class="token punctuation">:</span></span>
<span class="line">        <span class="token punctuation">-</span> DSI<span class="token punctuation">-</span><span class="token number">1</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>A BSP can also set a board-wide default by exporting <code>STRUX_OUTPUT_TRANSFORM</code> in <code>bsp.cage.env</code> — that&#39;s how the some tablet BSPs rotate their portrait panel for every project built on it. A per-monitor <code>transform</code> in <code>strux.yaml</code> takes precedence over the environment value.</p><h2 id="what-the-bsp-contributes" tabindex="-1"><a class="header-anchor" href="#what-the-bsp-contributes"><span>What the BSP contributes</span></a></h2><p>Two compositor-related knobs live in <code>bsp.yaml</code> rather than <code>strux.yaml</code>, because they&#39;re properties of the hardware:</p><div class="language-yaml line-numbers-mode" data-highlighter="prismjs" data-ext="yml"><pre><code><span class="line"><span class="token key atrule">bsp</span><span class="token punctuation">:</span></span>
<span class="line">  <span class="token key atrule">display</span><span class="token punctuation">:</span></span>
<span class="line">    <span class="token key atrule">resolution</span><span class="token punctuation">:</span> 1920x1080   <span class="token comment"># panel&#39;s native mode; default when strux.yaml has no display section</span></span>
<span class="line">  <span class="token key atrule">cage</span><span class="token punctuation">:</span></span>
<span class="line">    <span class="token key atrule">hide_cursor</span><span class="token punctuation">:</span> <span class="token boolean important">true</span>       <span class="token comment"># touch-only kiosk: never show a mouse cursor</span></span>
<span class="line">    <span class="token key atrule">env</span><span class="token punctuation">:</span>                    <span class="token comment"># environment for Cage and everything it spawns</span></span>
<span class="line">      <span class="token punctuation">-</span> WLR_DRM_NO_MODIFIERS=1</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>cage.env</code> entries are baked into <code>/strux/.cage-env</code> at build time — typically wlroots/driver workarounds (<code>WLR_DRM_NO_MODIFIERS=1</code> appears in every shipped BSP) or the <code>STRUX_OUTPUT_TRANSFORM</code> default above. <code>hide_cursor: true</code> keeps the cursor hidden even when a pointer device is present.</p><h2 id="where-to-go-next" tabindex="-1"><a class="header-anchor" href="#where-to-go-next"><span>Where to go next</span></a></h2><ul><li><a href="/guide/frontend.html" target="_blank" rel="noopener noreferrer">Frontend</a> — routing different paths to different views in your app.</li><li><a href="/concepts/overview.html" target="_blank" rel="noopener noreferrer">Architecture Overview</a> — where the display stack sits in the boot chain.</li><li><a href="/bsp/reference/bsp-yaml.html" target="_blank" rel="noopener noreferrer">bsp.yaml reference</a> — the <code>display</code> and <code>cage</code> keys in full.</li><li><a href="/reference/strux-yaml.html" target="_blank" rel="noopener noreferrer">strux.yaml reference</a> — the <code>display.monitors</code> schema in full.</li></ul>`,31)])])}const p=n(o,[["render",i]]),r=JSON.parse(`{"path":"/concepts/display-stack.html","title":"Display Stack","lang":"en-US","frontmatter":{},"headers":[{"level":2,"title":"What's a compositor, anyway?","slug":"what-s-a-compositor-anyway","link":"#what-s-a-compositor-anyway","children":[]},{"level":2,"title":"From YAML to pixels","slug":"from-yaml-to-pixels","link":"#from-yaml-to-pixels","children":[]},{"level":2,"title":"Touch input mapping","slug":"touch-input-mapping","link":"#touch-input-mapping","children":[]},{"level":2,"title":"Rotation transforms","slug":"rotation-transforms","link":"#rotation-transforms","children":[]},{"level":2,"title":"What the BSP contributes","slug":"what-the-bsp-contributes","link":"#what-the-bsp-contributes","children":[]},{"level":2,"title":"Where to go next","slug":"where-to-go-next","link":"#where-to-go-next","children":[]}],"git":{"updatedTime":1781317241000,"contributors":[{"name":"Miguel Medeiros","username":"","email":"miguel@medeirosconsulting.ca","commits":1}],"changelog":[{"hash":"e32366d86296aed0c45cf8447d8ea8147c1d03da","time":1781317241000,"email":"miguel@medeirosconsulting.ca","author":"Miguel Medeiros","message":"add docs"}]},"filePathRelative":"concepts/display-stack.md"}`);export{p as comp,r as data};
