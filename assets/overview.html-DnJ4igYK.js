import{_ as s,c as n,a as t,o as a}from"./app-B4LHn44G.js";const i={};function r(o,e){return a(),n("div",null,[...e[0]||(e[0]=[t(`<h1 id="architecture-overview" tabindex="-1"><a class="header-anchor" href="#architecture-overview"><span>Architecture Overview</span></a></h1><p>This page explains how Strux works under the hood, in two halves: what happens on your machine when you run <code>strux build</code>, and what happens on the device every time it powers on. Read this once and the rest of the docs will make much more sense.</p><h2 id="the-two-halves" tabindex="-1"><a class="header-anchor" href="#the-two-halves"><span>The two halves</span></a></h2><div class="language-txt line-numbers-mode" data-highlighter="prismjs" data-ext="txt"><pre><code><span class="line">BUILD TIME (your machine)                ON DEVICE (every boot)</span>
<span class="line">┌──────────────────────────┐             ┌──────────────────────────┐</span>
<span class="line">│  strux CLI               │             │  Bootloader → Kernel     │</span>
<span class="line">│    │                     │             │    │                     │</span>
<span class="line">│    ▼                     │             │    ▼                     │</span>
<span class="line">│  strux-builder (Docker)  │   image     │  systemd                 │</span>
<span class="line">│    frontend, Go app,     │  ───────▶   │    │                     │</span>
<span class="line">│    Cage, WPE, kernel,    │             │    ▼                     │</span>
<span class="line">│    bootloader, rootfs    │             │  strux.service           │</span>
<span class="line">│    │                     │             │    ├─ Go backend (:8080) │</span>
<span class="line">│    ▼                     │             │    └─ strux client       │</span>
<span class="line">│  dist/output/{bsp}/      │             │         └─ Cage → Cog    │</span>
<span class="line">│    bootable image        │             │              └─ your app │</span>
<span class="line">└──────────────────────────┘             └──────────────────────────┘</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="build-time-architecture" tabindex="-1"><a class="header-anchor" href="#build-time-architecture"><span>Build-time architecture</span></a></h2><p>The <code>strux</code> CLI is a single binary (TypeScript, compiled with Bun) that orchestrates the build. It doesn&#39;t compile anything itself — every build step runs as a shell script inside a Docker container called <strong>strux-builder</strong>, which has all the cross-compilers, <code>debootstrap</code>, and build tools pre-installed. That&#39;s why Docker is the only real prerequisite: your machine never needs an ARM toolchain or kernel headers.</p><p>A build works like this:</p><ol><li>The CLI validates <code>strux.yaml</code> (your project config) and <code>bsp/{name}/bsp.yaml</code> (the board config) against strict schemas. Typos fail fast, before any compilation.</li><li>It copies its embedded assets (build scripts, systemd services, the Cage and client source code) into <code>dist/artifacts/</code>, where you can inspect and even edit them. See <a href="/concepts/artifacts.html" target="_blank" rel="noopener noreferrer">Artifacts</a>.</li><li>It runs the build steps in order — frontend, Go application, Cage compositor, WPE extension, client, kernel, bootloader, root filesystem — each as a script inside the strux-builder container with your project mounted at <code>/project</code>. The full sequence is described in <a href="/concepts/build-pipeline.html" target="_blank" rel="noopener noreferrer">Build Pipeline</a>.</li><li>A smart cache skips any step whose inputs haven&#39;t changed, so incremental builds are fast. See <a href="/concepts/caching.html" target="_blank" rel="noopener noreferrer">Caching</a>.</li><li>The BSP&#39;s <code>make_image</code> script assembles everything into a bootable disk image under <code>dist/output/{bsp}/</code>.</li></ol><p>Everything lands in <code>dist/</code>, split per board so you can build for QEMU and real hardware side by side:</p><div class="language-txt line-numbers-mode" data-highlighter="prismjs" data-ext="txt"><pre><code><span class="line">dist/</span>
<span class="line">├── artifacts/          # Embedded assets, copied out once — yours to edit</span>
<span class="line">├── cache/</span>
<span class="line">│   ├── frontend/       # Built frontend (shared — architecture-agnostic)</span>
<span class="line">│   └── {bsp}/          # Per-board compiled binaries, kernel, rootfs tarballs</span>
<span class="line">└── output/</span>
<span class="line">    └── {bsp}/          # The final bootable image</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><div class="hint-container tip"><p class="hint-container-title">What&#39;s a rootfs?</p><p>The <strong>root filesystem</strong> (rootfs) is everything a Linux system has on disk: <code>/usr</code>, <code>/etc</code>, your app, all of it. Strux builds a minimal Debian rootfs and layers your app on top.</p></div><p>Hardware specifics — CPU architecture, kernel source, bootloader, device-specific scripts — live entirely in the <strong>Board Support Package</strong>. The pipeline is the same for every board; the BSP fills in the blanks. See <a href="/concepts/bsp.html" target="_blank" rel="noopener noreferrer">Board Support Packages</a>.</p><h2 id="on-device-architecture" tabindex="-1"><a class="header-anchor" href="#on-device-architecture"><span>On-device architecture</span></a></h2><p>The built image boots a fixed, minimal chain. There&#39;s no desktop, no login screen — just enough Linux to put your app on the screen.</p><div class="language-txt line-numbers-mode" data-highlighter="prismjs" data-ext="txt"><pre><code><span class="line">Bootloader (U-Boot/GRUB — BSP-dependent)</span>
<span class="line">  └─ Linux kernel + initramfs (Plymouth splash shows your logo)</span>
<span class="line">       └─ systemd</span>
<span class="line">            ├─ strux-network.service   (network bring-up)</span>
<span class="line">            ├─ seatd                   (grants display/input access)</span>
<span class="line">            └─ strux.service</span>
<span class="line">                 └─ /strux/strux.sh</span>
<span class="line">                      ├─ /strux/main      ← your Go backend, HTTP on :8080</span>
<span class="line">                      └─ /strux/client    ← the Strux client</span>
<span class="line">                           └─ cage        ← Wayland compositor (with splash)</span>
<span class="line">                                └─ cog    ← WPE WebKit browser, one per display</span>
<span class="line">                                     └─ your frontend, full-screen</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><div class="hint-container tip"><p class="hint-container-title">New to some of these terms?</p><p><strong>systemd</strong> is Linux&#39;s service manager — it starts and supervises processes at boot. <strong>Plymouth</strong> draws the boot splash before the graphics stack is up. A <strong>Wayland compositor</strong> (Cage) is the component that puts pixels on the screen; <strong>WPE WebKit</strong> (launched via the <code>cog</code> browser shell) renders your web app inside it.</p></div><p>Everything Strux-specific lives in <code>/strux/</code> on the device. The key players:</p><ul><li><strong><code>strux.service</code></strong> — the systemd unit that owns the whole UI stack. It requires <code>seatd</code> (the daemon that hands out access to displays and input devices) and restarts on failure.</li><li><strong><code>strux.sh</code></strong> — the startup script. It waits for the GPU and seatd, applies any staged binary updates, starts your Go backend (<code>/strux/main</code>), hands off from Plymouth, then launches the client. It&#39;s user-editable — see <a href="/concepts/artifacts.html" target="_blank" rel="noopener noreferrer">Artifacts</a>.</li><li><strong>The Strux client</strong> (<code>/strux/client</code>) — a small Go program that decides between production and dev mode, then launches Cage. In production it simply runs the compositor; in dev mode (when <code>/strux/.dev-env.json</code> exists) it first discovers your dev machine over mDNS, USB networking, or configured fallback hosts, and connects via WebSocket so the CLI can push new binaries without reflashing. See <a href="/guide/dev-mode.html" target="_blank" rel="noopener noreferrer">Dev Mode</a>.</li><li><strong>Cage</strong> — Strux ships a modified Cage that renders the boot splash itself (so the logo never flickers between Plymouth and your app), supports multiple monitors with per-output URLs, rotation, and touch-device mapping. See <a href="/concepts/display-stack.html" target="_blank" rel="noopener noreferrer">Display Stack</a>.</li><li><strong>Cog</strong> — the WPE WebKit browser shell. Cage launches one Cog instance per configured display through the user-editable <code>/strux/strux-run-cog.sh</code> script.</li></ul><h3 id="how-the-frontend-talks-to-the-backend" tabindex="-1"><a class="header-anchor" href="#how-the-frontend-talks-to-the-backend"><span>How the frontend talks to the backend</span></a></h3><p>Your Go backend serves HTTP on port <code>8080</code> — it serves the built frontend files from <code>/strux/frontend</code> and answers API calls. In production it binds to <code>127.0.0.1</code> only, so nothing is exposed on the network; in dev mode it listens on all interfaces.</p><p>Cog loads <code>http://localhost:8080</code> plus the path configured for its display. Alongside HTTP, a WPE WebKit <strong>web extension</strong> (<code>libstrux-extension.so</code>) runs inside the browser&#39;s web process and connects to the backend over a Unix socket (<code>/tmp/strux-ipc.sock</code>). It injects a <code>window.strux</code> object into every page: your backend&#39;s fields and methods appear as typed JavaScript properties and async functions, plus a <code>strux.ipc</code> event API (<code>on</code>, <code>off</code>, <code>send</code>) for bidirectional events. That&#39;s the bridge the <a href="/guide/backend.html" target="_blank" rel="noopener noreferrer">Backend guide</a> and <a href="/reference/frontend-api.html" target="_blank" rel="noopener noreferrer">Frontend API reference</a> describe.</p><h2 id="where-to-go-next" tabindex="-1"><a class="header-anchor" href="#where-to-go-next"><span>Where to go next</span></a></h2><ul><li><a href="/concepts/build-pipeline.html" target="_blank" rel="noopener noreferrer">Build Pipeline</a> — every build step in detail.</li><li><a href="/concepts/caching.html" target="_blank" rel="noopener noreferrer">Caching</a> — why rebuilds are fast.</li><li><a href="/concepts/bsp.html" target="_blank" rel="noopener noreferrer">Board Support Packages</a> — how hardware support is packaged.</li><li><a href="/concepts/display-stack.html" target="_blank" rel="noopener noreferrer">Display Stack</a> — Cage, Cog, and multi-monitor setups.</li></ul>`,23)])])}const c=s(i,[["render",r]]),d=JSON.parse('{"path":"/concepts/overview.html","title":"Architecture Overview","lang":"en-US","frontmatter":{},"headers":[{"level":2,"title":"The two halves","slug":"the-two-halves","link":"#the-two-halves","children":[]},{"level":2,"title":"Build-time architecture","slug":"build-time-architecture","link":"#build-time-architecture","children":[]},{"level":2,"title":"On-device architecture","slug":"on-device-architecture","link":"#on-device-architecture","children":[{"level":3,"title":"How the frontend talks to the backend","slug":"how-the-frontend-talks-to-the-backend","link":"#how-the-frontend-talks-to-the-backend","children":[]}]},{"level":2,"title":"Where to go next","slug":"where-to-go-next","link":"#where-to-go-next","children":[]}],"git":{"updatedTime":1781317241000,"contributors":[{"name":"Miguel Medeiros","username":"","email":"miguel@medeirosconsulting.ca","commits":1}],"changelog":[{"hash":"e32366d86296aed0c45cf8447d8ea8147c1d03da","time":1781317241000,"email":"miguel@medeirosconsulting.ca","author":"Miguel Medeiros","message":"add docs"}]},"filePathRelative":"concepts/overview.md"}');export{c as comp,d as data};
