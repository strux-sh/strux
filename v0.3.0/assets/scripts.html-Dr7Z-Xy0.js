import{_ as s,c as n,a,o as t}from"./app-BJNURCT2.js";const i={};function r(o,e){return t(),n("div",null,[...e[0]||(e[0]=[a(`<h1 id="writing-lifecycle-scripts" tabindex="-1"><a class="header-anchor" href="#writing-lifecycle-scripts"><span>Writing Lifecycle Scripts</span></a></h1><p>Lifecycle scripts are how your BSP does board-specific work the generic build pipeline can&#39;t: assembling the final disk image, patching kernel sources, converting a splash logo for U-Boot. This page walks you through writing one — picking the right hook, declaring caching so it doesn&#39;t re-run needlessly, and debugging it when it fails. For the underlying model, read <a href="/bsp/concepts/lifecycle-scripts.html" target="_blank" rel="noopener noreferrer">lifecycle scripts</a> first.</p><h2 id="where-scripts-live" tabindex="-1"><a class="header-anchor" href="#where-scripts-live"><span>Where scripts live</span></a></h2><p>By convention, scripts go in a <code>scripts/</code> folder inside your BSP directory and are registered in <code>bsp.yaml</code> under <code>bsp.scripts</code>:</p><div class="language-txt line-numbers-mode" data-highlighter="prismjs" data-ext="txt"><pre><code><span class="line">bsp/my-board/</span>
<span class="line">├── bsp.yaml</span>
<span class="line">└── scripts/</span>
<span class="line">    └── make-image.sh</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><div class="language-yaml line-numbers-mode" data-highlighter="prismjs" data-ext="yml"><pre><code><span class="line"><span class="token key atrule">scripts</span><span class="token punctuation">:</span></span>
<span class="line">  <span class="token punctuation">-</span> <span class="token key atrule">location</span><span class="token punctuation">:</span> ./scripts/make<span class="token punctuation">-</span>image.sh    <span class="token comment"># path relative to the BSP directory</span></span>
<span class="line">    <span class="token key atrule">step</span><span class="token punctuation">:</span> make_image                     <span class="token comment"># which hook to run at</span></span>
<span class="line">    <span class="token key atrule">description</span><span class="token punctuation">:</span> <span class="token string">&quot;Create disk image&quot;</span>     <span class="token comment"># optional, shown in build logs</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Every build, Strux runs the scripts registered for each hook at the right moment in the pipeline, inside the <code>strux-builder</code> Docker container with your project mounted at <code>/project</code>. Multiple scripts on the same hook run in the order they appear in <code>bsp.yaml</code>.</p><h2 id="_1-write-the-script" tabindex="-1"><a class="header-anchor" href="#_1-write-the-script"><span>1. Write the script</span></a></h2><p>Here is the skeleton every real Strux BSP script uses — fail fast, report where you failed, and emit progress the CLI can display:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh"><pre><code><span class="line"><span class="token shebang important">#!/bin/bash</span></span>
<span class="line"><span class="token builtin class-name">set</span> <span class="token parameter variable">-eo</span> pipefail</span>
<span class="line"></span>
<span class="line"><span class="token comment"># Print the failing command and line on any error</span></span>
<span class="line"><span class="token builtin class-name">trap</span> <span class="token string">&#39;echo &quot;Error: Command failed at line $LINENO with exit code $?: $BASH_COMMAND&quot; &gt;&amp;2&#39;</span> ERR</span>
<span class="line"></span>
<span class="line"><span class="token comment"># Lines prefixed with &quot;STRUX_PROGRESS: &quot; update the build spinner</span></span>
<span class="line"><span class="token function-name function">progress</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span></span>
<span class="line">    <span class="token builtin class-name">echo</span> <span class="token string">&quot;STRUX_PROGRESS: <span class="token variable">$1</span>&quot;</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span>
<span class="line">progress <span class="token string">&quot;Doing board-specific work...&quot;</span></span>
<span class="line"></span>
<span class="line"><span class="token comment"># Use the env vars Strux provides — never hardcode paths</span></span>
<span class="line"><span class="token comment"># PROJECT_FOLDER              = /project (your project, mounted in Docker)</span></span>
<span class="line"><span class="token comment"># PROJECT_DIST_CACHE_FOLDER   = /project/dist/cache/{bsp}</span></span>
<span class="line"><span class="token comment"># PROJECT_DIST_OUTPUT_FOLDER  = /project/dist/output/{bsp}</span></span>
<span class="line"><span class="token comment"># BSP_FOLDER                  = /project/bsp/{bsp}</span></span>
<span class="line"></span>
<span class="line"><span class="token function">mkdir</span> <span class="token parameter variable">-p</span> <span class="token string">&quot;<span class="token variable">$PROJECT_DIST_CACHE_FOLDER</span>&quot;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><div class="hint-container tip"><p class="hint-container-title">Why the env vars matter</p><p>The script runs inside Docker, where your project lives at <code>/project</code> — paths from your host machine don&#39;t exist there. <code>BSP_NAME</code>, <code>TARGET_ARCH</code>, <code>STEP</code>, and friends are also set so one script can serve several boards. The full list is in the <a href="/bsp/reference/environment-variables.html" target="_blank" rel="noopener noreferrer">environment variables reference</a>.</p></div><p>Two progress marker formats are recognized in your script&#39;s output:</p><ul><li><code>STRUX_PROGRESS: message</code> — replaces the spinner text.</li><li><code>STRUX_PROGRESS_BAR: message (42%)</code> — renders a progress bar (the HD215 flash script uses this to surface <code>rkdeveloptool</code>&#39;s percentage output).</li></ul><p>Everything else your script prints is hidden behind the spinner unless the script fails or you run with <code>--verbose</code>.</p><h2 id="_2-choose-a-step" tabindex="-1"><a class="header-anchor" href="#_2-choose-a-step"><span>2. Choose a step</span></a></h2><p>Ask: <em>what does my script need to exist, and what consumes its output?</em> Then pick the hook between those two points. Some real choices from the BSPs in the Strux repo:</p><table><thead><tr><th>You want to...</th><th>Step</th><th>Real example</th></tr></thead><tbody><tr><td>Patch or extend kernel sources before they compile</td><td><code>after_kernel_extract</code></td><td>HT109 installs vendor touchscreen/audio/NFC drivers into the kernel tree</td></tr><tr><td>Build a vendor bootloader fork yourself</td><td><code>custom_bootloader</code></td><td>HD215 builds Rockchip&#39;s U-Boot fork (the built-in build is replaced)</td></tr><tr><td>Post-process bootloader output</td><td><code>after_bootloader</code></td><td>HD215 converts the splash PNG to BMP and packages <code>idbloader.img</code></td></tr><tr><td>Install files into the finished rootfs</td><td><code>before_bundle</code></td><td>HD215 installs <code>extlinux.conf</code> and boot assets into the rootfs tarball</td></tr><tr><td>Produce the final disk image</td><td><code>make_image</code></td><td>Every BSP — qemu makes a raw ext4, the Rockchip boards run <code>genimage</code></td></tr><tr><td>Flash the image to hardware</td><td><code>flash_script</code></td><td>Runs on the host via <code>strux flash</code>, see <a href="/bsp/guide/flash-scripts.html" target="_blank" rel="noopener noreferrer">flash scripts</a></td></tr></tbody></table><p>The complete hook list in execution order is in the <a href="/bsp/reference/build-steps.html" target="_blank" rel="noopener noreferrer">build steps reference</a>. Remember that kernel hooks only fire when <code>boot.kernel.custom_kernel: true</code>, and bootloader hooks only when <code>boot.bootloader.enabled: true</code>.</p><h2 id="_3-declare-caching" tabindex="-1"><a class="header-anchor" href="#_3-declare-caching"><span>3. Declare caching</span></a></h2><p>Without caching declarations, your script runs on <strong>every</strong> build. That&#39;s fine for cheap scripts, but a 10-minute U-Boot build needs to be skipped when nothing changed. Declare what the script produces and what it reads:</p><div class="language-yaml line-numbers-mode" data-highlighter="prismjs" data-ext="yml"><pre><code><span class="line"><span class="token punctuation">-</span> <span class="token key atrule">location</span><span class="token punctuation">:</span> ./scripts/build<span class="token punctuation">-</span>bootloader<span class="token punctuation">-</span>rockchip.sh</span>
<span class="line">  <span class="token key atrule">step</span><span class="token punctuation">:</span> custom_bootloader</span>
<span class="line">  <span class="token key atrule">description</span><span class="token punctuation">:</span> <span class="token string">&quot;Build Rockchip vendor U-Boot&quot;</span></span>
<span class="line">  <span class="token key atrule">cached_generated_artifacts</span><span class="token punctuation">:</span>        <span class="token comment"># skip the script if these all exist...</span></span>
<span class="line">    <span class="token punctuation">-</span> cache/bootloader/u<span class="token punctuation">-</span>boot.bin</span>
<span class="line">    <span class="token punctuation">-</span> cache/bootloader/idbloader.img</span>
<span class="line">  <span class="token key atrule">depends_on</span><span class="token punctuation">:</span>                        <span class="token comment"># ...and none of these changed</span></span>
<span class="line">    <span class="token punctuation">-</span> ./dts/rk3576<span class="token punctuation">-</span>hd215<span class="token punctuation">-</span>uboot.dts</span>
<span class="line">    <span class="token punctuation">-</span> ./blobs/rk3576_bl31_v1.20.elf</span>
<span class="line">    <span class="token punctuation">-</span> cache/kernel/rk3576<span class="token punctuation">-</span>hd215<span class="token punctuation">-</span>linux.dtb</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The rules, in the order Strux checks them:</p><ol><li><code>--clean</code> forces every script to run (and wipes <code>dist/cache/{bsp}/</code> first).</li><li>No <code>cached_generated_artifacts</code> declared → the script always runs.</li><li>Any declared artifact missing → run.</li><li>The script file itself changed (SHA256 hash) → run. You never list the script in its own <code>depends_on</code>.</li><li>Any <code>depends_on</code> file changed, missing, or not seen before → run.</li></ol><p>Paths use a prefix convention: <code>cache/</code> → <code>dist/cache/{bsp}/</code>, <code>output/</code> → <code>dist/output/{bsp}/</code>, <code>./</code> → the BSP directory, anything else → <code>dist/</code>. Details in the <a href="/bsp/reference/path-resolution.html" target="_blank" rel="noopener noreferrer">path resolution reference</a>.</p><p>Getting this right is mostly about honesty:</p><ul><li><strong>List every input.</strong> If your script reads a DTS file, a blob, a config under <code>./boot/</code>, or another script&#39;s output from <code>cache/</code>, list it. An unlisted input means stale builds — the script gets skipped when it shouldn&#39;t be.</li><li><strong>List real outputs.</strong> Only declare files the script reliably writes; if one is ever missing, the script just re-runs.</li><li><strong>Depend on upstream outputs to chain scripts.</strong> The HD215 <code>package-rockchip.sh</code> script depends on <code>cache/bootloader/spl/u-boot-spl.bin</code>, so it re-runs exactly when the bootloader build produced a new SPL.</li><li><strong>Files, not directories.</strong> <code>depends_on</code> entries are hashed as single files; a directory can&#39;t be hashed and forces the script to run every build.</li></ul><h2 id="_4-run-and-debug" tabindex="-1"><a class="header-anchor" href="#_4-run-and-debug"><span>4. Run and debug</span></a></h2><p>Run a build and watch for your script&#39;s description in the output:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh"><pre><code><span class="line">strux build my-board</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>A script that runs logs <code>Running BSP script: &lt;description&gt; (&lt;step&gt;)...</code> and, on success, <code>Completed BSP script: &lt;description&gt;</code>. A skipped script logs <code>Skipping script: &lt;description&gt;</code>.</p><p>When a script <strong>fails</strong> (any non-zero exit code), the build aborts and prints the script&#39;s stderr and stdout. The <code>trap ... ERR</code> line from the skeleton makes sure the very first failing command and its line number appear in that output — put it in every script.</p><p>To see everything live, use the global <code>--verbose</code> flag:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh"><pre><code><span class="line">strux <span class="token parameter variable">--verbose</span> build my-board</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>Verbose mode streams the full output of every script (and every built-in step) as it runs instead of hiding it behind a spinner, and prints cache decisions — including why a script was <em>not</em> skipped (changed dependency, missing artifact, edited script).</p><p>Other debugging tactics that work well:</p><ul><li><strong>Force a re-run</strong> by touching the script file — its hash is part of the cache key — or by building with <code>--clean</code> (slower: it invalidates the whole BSP cache).</li><li><strong>Check the right hook fired.</strong> Each script&#39;s log line shows the step name in parentheses; <code>STEP</code> is also exported into the script&#39;s environment.</li><li><strong>Inspect intermediate state</strong> under <code>dist/cache/{bsp}/</code> on the host after a failed run — the rootfs tarballs, kernel artifacts, and whatever your script wrote are all there. Note that paths inside the script are container paths: <code>/project/dist/...</code> in the script is <code>dist/...</code> in your project.</li><li><strong>Missing script file?</strong> Strux aborts immediately with <code>Script ... not found</code> — check the <code>location</code> path is relative to the BSP directory.</li></ul><h2 id="where-to-go-next" tabindex="-1"><a class="header-anchor" href="#where-to-go-next"><span>Where to go next</span></a></h2><ul><li><a href="/bsp/concepts/lifecycle-scripts.html" target="_blank" rel="noopener noreferrer">Lifecycle scripts concept</a> — the hook model, execution environment, and caching semantics in depth.</li><li><a href="/bsp/reference/build-steps.html" target="_blank" rel="noopener noreferrer">Build steps reference</a> — every hook in exact order.</li><li><a href="/bsp/guide/examples.html" target="_blank" rel="noopener noreferrer">BSP examples</a> — real scripts in real BSPs, annotated.</li><li><a href="/bsp/guide/flash-scripts.html" target="_blank" rel="noopener noreferrer">Flash scripts</a> — the host-side counterpart for <code>strux flash</code>.</li></ul>`,38)])])}const l=s(i,[["render",r]]),p=JSON.parse('{"path":"/bsp/guide/scripts.html","title":"Writing Lifecycle Scripts","lang":"en-US","frontmatter":{},"headers":[{"level":2,"title":"Where scripts live","slug":"where-scripts-live","link":"#where-scripts-live","children":[]},{"level":2,"title":"1. Write the script","slug":"_1-write-the-script","link":"#_1-write-the-script","children":[]},{"level":2,"title":"2. Choose a step","slug":"_2-choose-a-step","link":"#_2-choose-a-step","children":[]},{"level":2,"title":"3. Declare caching","slug":"_3-declare-caching","link":"#_3-declare-caching","children":[]},{"level":2,"title":"4. Run and debug","slug":"_4-run-and-debug","link":"#_4-run-and-debug","children":[]},{"level":2,"title":"Where to go next","slug":"where-to-go-next","link":"#where-to-go-next","children":[]}],"git":{"updatedTime":1781317241000,"contributors":[{"name":"Miguel Medeiros","username":"","email":"miguel@medeirosconsulting.ca","commits":1}],"changelog":[{"hash":"e32366d86296aed0c45cf8447d8ea8147c1d03da","time":1781317241000,"email":"miguel@medeirosconsulting.ca","author":"Miguel Medeiros","message":"add docs"}]},"filePathRelative":"bsp/guide/scripts.md"}');export{l as comp,p as data};
