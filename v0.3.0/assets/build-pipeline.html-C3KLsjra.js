import{_ as t,c as o,a as s,o as n}from"./app-CnSTJJsv.js";const i={};function a(r,e){return n(),o("div",null,[...e[0]||(e[0]=[s(`<h1 id="build-pipeline" tabindex="-1"><a class="header-anchor" href="#build-pipeline"><span>Build Pipeline</span></a></h1><p>When you run <code>strux build</code>, the CLI executes a fixed sequence of steps, each one a shell script running inside the strux-builder Docker container. This page walks through every step: what it consumes, what it produces, and when it&#39;s skipped. For the caching logic that decides whether a step runs at all, see <a href="/concepts/caching.html" target="_blank" rel="noopener noreferrer">Caching</a>.</p><h2 id="the-step-order" tabindex="-1"><a class="header-anchor" href="#the-step-order"><span>The step order</span></a></h2><div class="language-txt line-numbers-mode" data-highlighter="prismjs" data-ext="txt"><pre><code><span class="line">before_build (BSP hook)</span>
<span class="line">  1. frontend        → dist/cache/frontend/</span>
<span class="line">  2. application     → dist/cache/{bsp}/app/main</span>
<span class="line">  3. cage            → dist/cache/{bsp}/cage</span>
<span class="line">  4. wpe             → dist/cache/{bsp}/libstrux-extension.so + cog</span>
<span class="line">  5. screen          → dist/cache/{bsp}/screen</span>
<span class="line">  6. client          → dist/cache/{bsp}/client</span>
<span class="line">  7. kernel          → dist/cache/{bsp}/kernel/        (only if custom_kernel)</span>
<span class="line">  8. bootloader      → dist/cache/{bsp}/bootloader/    (only if enabled)</span>
<span class="line">  9. rootfs-base     → dist/cache/{bsp}/rootfs-base.tar.gz</span>
<span class="line"> 10. rootfs-post     → dist/cache/{bsp}/rootfs-post.tar.gz</span>
<span class="line"> 11. make_image      → dist/output/{bsp}/...           (BSP script)</span>
<span class="line"> 12. update bundle   → .struxb file                    (only if auto_bundle)</span>
<span class="line">after_build (BSP hook)</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Around almost every step, the BSP can register <strong>lifecycle scripts</strong> — <code>before_frontend</code>, <code>after_kernel</code>, and so on — that run at that exact point in the sequence. That&#39;s how board-specific work (installing vendor drivers, building a vendor bootloader, packing a Rockchip image) plugs into the generic pipeline without modifying it. See <a href="/bsp/concepts/lifecycle-scripts.html" target="_blank" rel="noopener noreferrer">Lifecycle Scripts</a> for the full hook list.</p><p>Before any step runs, the CLI validates <code>strux.yaml</code> and the selected <code>bsp.yaml</code>, prepares the <code>dist/</code> directory structure, copies embedded assets to <code>dist/artifacts/</code> (see <a href="/concepts/artifacts.html" target="_blank" rel="noopener noreferrer">Artifacts</a>), and makes sure the strux-builder Docker image is up to date. If the Docker image had to be rebuilt, every cached step is invalidated.</p><h2 id="_1-frontend" tabindex="-1"><a class="header-anchor" href="#_1-frontend"><span>1. Frontend</span></a></h2><p>Runs <code>npm install</code> and <code>npm run build</code> in your <code>frontend/</code> directory, then copies the build output to <code>dist/cache/frontend/</code>.</p><p>This is the only step whose output goes in the <em>shared</em> cache rather than the per-BSP cache: a built web app is just files, identical for every CPU architecture, so it&#39;s reused across boards.</p><h2 id="_2-application" tabindex="-1"><a class="header-anchor" href="#_2-application"><span>2. Application</span></a></h2><p>Compiles your <code>main.go</code> (and the rest of your Go module) for the target architecture into <code>dist/cache/{bsp}/app/main</code>. If the BSP declares <a href="/bsp/guide/runtime-extensions.html" target="_blank" rel="noopener noreferrer">runtime extensions</a> under <code>bsp.runtime.extensions</code>, their Go packages are compiled in too — that&#39;s why this step is per-BSP even though your code doesn&#39;t change between boards.</p><h2 id="_3-cage" tabindex="-1"><a class="header-anchor" href="#_3-cage"><span>3. Cage</span></a></h2><p>Compiles the Cage Wayland compositor from the sources in <code>dist/artifacts/cage/</code> (Strux&#39;s modified fork — splash rendering, multi-monitor, input mapping; see <a href="/concepts/display-stack.html" target="_blank" rel="noopener noreferrer">Display Stack</a>) using Meson, producing <code>dist/cache/{bsp}/cage</code>. The build also writes a <code>.cage-env</code> file from the BSP&#39;s <code>bsp.cage.env</code> and <code>bsp.cage.hide_cursor</code> settings, which later ends up on the device as <code>/strux/.cage-env</code>.</p><h2 id="_4-wpe" tabindex="-1"><a class="header-anchor" href="#_4-wpe"><span>4. WPE</span></a></h2><p>Two builds in one step:</p><ul><li>The <strong>Strux web extension</strong> (<code>dist/artifacts/wpe-extension/extension.c</code>) — the C library that injects <code>window.strux</code> into your pages — is compiled with CMake to <code>dist/cache/{bsp}/libstrux-extension.so</code>.</li><li>A <strong>patched Cog</strong> browser shell: Cog 0.18.5 is cloned from upstream, an autoplay-policy backport patch is applied (so unmuted media can autoplay in kiosks), and the result is compiled to <code>dist/cache/{bsp}/cog</code>.</li></ul><p>Both are cross-compiled when the target architecture differs from the host.</p><h2 id="_5-screen" tabindex="-1"><a class="header-anchor" href="#_5-screen"><span>5. Screen</span></a></h2><p>Compiles <code>strux-screen</code>, the screen-capture daemon used for remote viewing, from <code>dist/artifacts/screen/</code> to <code>dist/cache/{bsp}/screen</code>. This step has no BSP hooks yet.</p><div class="hint-container warning"><p class="hint-container-title">Experimental</p><p>We&#39;re currently working on remote utilities so that you can access your device&#39;s screens in the field remotely. This will enable better troubleshooting capabilities without requiring further tools. This feature is not completely implemented yet.</p></div><h2 id="_6-client" tabindex="-1"><a class="header-anchor" href="#_6-client"><span>6. Client</span></a></h2><p>Compiles the Strux client — the on-device Go program that launches Cage and handles dev-mode connectivity — from <code>dist/artifacts/client/</code> to <code>dist/cache/{bsp}/client</code>.</p><p>Even when this step is cached, the CLI still refreshes the dev-mode configuration: in dev builds it rewrites <code>.dev-env.json</code> (so inspector and dev-server settings from <code>strux.yaml</code> always take effect), and in production builds it removes it.</p><h2 id="_7-kernel-—-only-if-custom-kernel" tabindex="-1"><a class="header-anchor" href="#_7-kernel-—-only-if-custom-kernel"><span>7. Kernel — only if <code>custom_kernel</code></span></a></h2><p><strong>Skipped entirely unless</strong> the BSP sets <code>bsp.boot.kernel.custom_kernel: true</code>. Without it, the image uses Debian&#39;s stock kernel package instead — that&#39;s what the qemu BSP does.</p><div class="hint-container tip"><p class="hint-container-title">What&#39;s a defconfig?</p><p>A <strong>defconfig</strong> is a named baseline kernel configuration shipped with the kernel source (e.g. <code>rockchip_linux_defconfig</code>). <strong>Fragments</strong> are small config snippets applied on top, and a <strong>device tree</strong> (DTS) is a data file describing the board&#39;s hardware to the kernel.</p></div><p>The built-in kernel build runs in two phases:</p><ol><li><strong>Extract</strong> — fetch the kernel source (Git repository or tarball URL from <code>bsp.boot.kernel.source</code>) and apply the BSP&#39;s patches. The <code>after_kernel_extract</code> hook runs here, so BSP scripts can drop extra drivers into the source tree before configuration.</li><li><strong>Build</strong> — apply the defconfig and fragments, compile the kernel, modules, and device tree, and install the results to <code>dist/cache/{bsp}/kernel/</code>.</li></ol><p>A BSP can replace the built-in build entirely by registering a script on the <code>custom_kernel</code> step; the <code>before_kernel</code>/<code>after_kernel</code> hooks still run around it.</p><h2 id="_8-bootloader-—-only-if-enabled" tabindex="-1"><a class="header-anchor" href="#_8-bootloader-—-only-if-enabled"><span>8. Bootloader — only if enabled</span></a></h2><p><strong>Skipped entirely unless</strong> the BSP sets <code>bsp.boot.bootloader.enabled: true</code>. QEMU boots the kernel directly, so the qemu BSP leaves this off; real ARM boards typically need U-Boot.</p><div class="hint-container tip"><p class="hint-container-title">What&#39;s a bootloader?</p><p>The <strong>bootloader</strong> is the first code the board runs from power-on. It initializes RAM and loads the Linux kernel. <strong>U-Boot</strong> is the standard one for ARM single-board computers; <strong>GRUB</strong> is common on x86.</p></div><p>The built-in build fetches the bootloader source, applies patches, fragments, and device trees, stages any vendor firmware blobs declared in <code>bsp.boot.bootloader.blobs</code>, and compiles into <code>dist/cache/{bsp}/bootloader/</code>. Two escape hatches:</p><ul><li>A script on the <code>custom_bootloader</code> step replaces the built-in build (used by Rockchip boards that need the vendor U-Boot tree).</li><li>If <code>bsp.boot.bootloader.type</code> is <code>custom</code> or <code>none</code>, the built-in build is skipped even with <code>enabled: true</code> — the BSP&#39;s own scripts are expected to provide the boot chain.</li></ul><h2 id="_9-rootfs-base" tabindex="-1"><a class="header-anchor" href="#_9-rootfs-base"><span>9. RootFS base</span></a></h2><p>Builds the base Debian root filesystem with <code>debootstrap</code> (Debian &quot;trixie&quot;), then installs the fixed system stack inside it: systemd, the Mesa graphics drivers, Wayland, WPE WebKit, Cog, fonts, and media support. The result is archived as <code>dist/cache/{bsp}/rootfs-base.tar.gz</code>.</p><p>This is the slowest step on a first build (it downloads a Debian system) and one of the most aggressively cached: it only depends on the target architecture and the build script itself, so it almost never reruns.</p><h2 id="_10-rootfs-post" tabindex="-1"><a class="header-anchor" href="#_10-rootfs-post"><span>10. RootFS post</span></a></h2><p>Takes the base tarball and layers everything project-specific on top:</p><ul><li>Installs your packages from <code>rootfs.packages</code> (in <code>strux.yaml</code>) and <code>bsp.rootfs.packages</code> — both repository names and local <code>.deb</code> files.</li><li>Applies overlays: first the BSP&#39;s <code>bsp.rootfs.overlay</code>, then your project&#39;s <code>rootfs.overlay</code>, copied verbatim into the filesystem.</li><li>Copies in the compiled binaries: your backend (<code>/strux/main</code>), the client, Cage, the patched Cog, the web extension, the screen daemon, and the built frontend (<code>/strux/frontend</code>).</li><li>Copies the display configuration (<code>/strux/.display-config.json</code>, generated fresh from <code>strux.yaml</code> before this step on every build) and the input device map.</li><li>Installs and enables the systemd services, sets the hostname, installs the kernel (custom or Debian stock), and builds the Plymouth splash theme and initramfs from your logo.</li></ul><p>Output: <code>dist/cache/{bsp}/rootfs-post.tar.gz</code>, plus the kernel and initramfs images used for booting.</p><h2 id="_11-image-creation" tabindex="-1"><a class="header-anchor" href="#_11-image-creation"><span>11. Image creation</span></a></h2><p>Creating the actual bootable image is <strong>always the BSP&#39;s job</strong> — partition layouts and boot flows are too board-specific to generalize. The <code>before_bundle</code> hook runs, then every BSP script registered on the <code>make_image</code> step. For QEMU that&#39;s a script producing a <code>rootfs.ext4</code>; for a Rockchip board it&#39;s a full GPT disk image with the boot chain in the right sectors. Output lands in <code>dist/output/{bsp}/</code>.</p><h2 id="_12-update-bundle-—-only-if-auto-bundle-is-on" tabindex="-1"><a class="header-anchor" href="#_12-update-bundle-—-only-if-auto-bundle-is-on"><span>12. Update bundle — only if auto-bundle is on</span></a></h2><p>If <code>strux.yaml</code> has both <code>update.enabled: true</code> and <code>update.auto_bundle: true</code>, the build ends by producing a signed <code>.struxb</code> update bundle — a tarball containing the rootfs image, a manifest, and an RSA signature — ready to ship to devices over the air. See <a href="/guide/updates.html" target="_blank" rel="noopener noreferrer">Updates</a>.</p><p>Finally, the <code>after_build</code> hook runs and the CLI writes <code>dist/output/{bsp}/.build-info.json</code> with the build mode, timestamp, BSP name, and versions.</p><h2 id="where-to-go-next" tabindex="-1"><a class="header-anchor" href="#where-to-go-next"><span>Where to go next</span></a></h2><ul><li><a href="/concepts/caching.html" target="_blank" rel="noopener noreferrer">Caching</a> — how steps get skipped on incremental builds.</li><li><a href="/bsp/concepts/lifecycle-scripts.html" target="_blank" rel="noopener noreferrer">Lifecycle Scripts</a> — hooking BSP scripts into the pipeline.</li><li><a href="/guide/building.html" target="_blank" rel="noopener noreferrer">Building</a> — running builds day to day.</li></ul>`,48)])])}const d=t(i,[["render",a]]),c=JSON.parse('{"path":"/concepts/build-pipeline.html","title":"Build Pipeline","lang":"en-US","frontmatter":{},"headers":[{"level":2,"title":"The step order","slug":"the-step-order","link":"#the-step-order","children":[]},{"level":2,"title":"1. Frontend","slug":"_1-frontend","link":"#_1-frontend","children":[]},{"level":2,"title":"2. Application","slug":"_2-application","link":"#_2-application","children":[]},{"level":2,"title":"3. Cage","slug":"_3-cage","link":"#_3-cage","children":[]},{"level":2,"title":"4. WPE","slug":"_4-wpe","link":"#_4-wpe","children":[]},{"level":2,"title":"5. Screen","slug":"_5-screen","link":"#_5-screen","children":[]},{"level":2,"title":"6. Client","slug":"_6-client","link":"#_6-client","children":[]},{"level":2,"title":"7. Kernel — only if custom_kernel","slug":"_7-kernel-—-only-if-custom-kernel","link":"#_7-kernel-—-only-if-custom-kernel","children":[]},{"level":2,"title":"8. Bootloader — only if enabled","slug":"_8-bootloader-—-only-if-enabled","link":"#_8-bootloader-—-only-if-enabled","children":[]},{"level":2,"title":"9. RootFS base","slug":"_9-rootfs-base","link":"#_9-rootfs-base","children":[]},{"level":2,"title":"10. RootFS post","slug":"_10-rootfs-post","link":"#_10-rootfs-post","children":[]},{"level":2,"title":"11. Image creation","slug":"_11-image-creation","link":"#_11-image-creation","children":[]},{"level":2,"title":"12. Update bundle — only if auto-bundle is on","slug":"_12-update-bundle-—-only-if-auto-bundle-is-on","link":"#_12-update-bundle-—-only-if-auto-bundle-is-on","children":[]},{"level":2,"title":"Where to go next","slug":"where-to-go-next","link":"#where-to-go-next","children":[]}],"git":{"updatedTime":1781317241000,"contributors":[{"name":"Miguel Medeiros","username":"","email":"miguel@medeirosconsulting.ca","commits":1}],"changelog":[{"hash":"e32366d86296aed0c45cf8447d8ea8147c1d03da","time":1781317241000,"email":"miguel@medeirosconsulting.ca","author":"Miguel Medeiros","message":"add docs"}]},"filePathRelative":"concepts/build-pipeline.md"}');export{d as comp,c as data};
