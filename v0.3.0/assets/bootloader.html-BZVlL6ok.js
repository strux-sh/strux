import{_ as s,c as o,a as t,o as n}from"./app-BJNURCT2.js";const a={};function l(d,e){return n(),o("div",null,[...e[0]||(e[0]=[t(`<h1 id="bootloaders" tabindex="-1"><a class="header-anchor" href="#bootloaders"><span>Bootloaders</span></a></h1><p>This page covers the <code>boot.bootloader</code> block of <code>bsp.yaml</code>: bootloader types, the built-in U-Boot build, bootloader-level device trees, boot methods, and the vendor firmware blobs that ARM boards need before a bootloader can even run. Read <a href="/bsp/guide/kernel.html" target="_blank" rel="noopener noreferrer">Custom Kernels</a> first — the bootloader&#39;s whole job is loading what that page builds.</p><h2 id="what-the-bootloader-does" tabindex="-1"><a class="header-anchor" href="#what-the-bootloader-does"><span>What the bootloader does</span></a></h2><p>When a board powers on, its ROM loads a tiny program from a fixed location on storage. That program — the <strong>bootloader</strong> — initializes RAM, sets up the display for a splash screen, finds the kernel and device tree on disk, and jumps into Linux. On most ARM boards this is <strong>U-Boot</strong>; on x86 it&#39;s typically GRUB or systemd-boot behind UEFI.</p><p>QEMU doesn&#39;t need one at all — <code>strux run</code> hands the kernel straight to the emulator — which is why the qemu BSP ships with <code>enabled: false</code> and why you can defer this whole page until the kernel works.</p><h2 id="the-bootloader-block" tabindex="-1"><a class="header-anchor" href="#the-bootloader-block"><span>The bootloader block</span></a></h2><p>The gate is <code>enabled</code>. When <code>false</code> (or the block is absent), the bootloader step is skipped entirely, including any <code>before_bootloader</code>/<code>after_bootloader</code> scripts. When <code>true</code>, the step runs and <code>type</code> decides what happens:</p><div class="language-yaml line-numbers-mode" data-highlighter="prismjs" data-ext="yml"><pre><code><span class="line">  <span class="token key atrule">boot</span><span class="token punctuation">:</span></span>
<span class="line">    <span class="token key atrule">bootloader</span><span class="token punctuation">:</span></span>
<span class="line">      <span class="token key atrule">enabled</span><span class="token punctuation">:</span> <span class="token boolean important">true</span></span>
<span class="line">      <span class="token key atrule">type</span><span class="token punctuation">:</span> u<span class="token punctuation">-</span>boot</span>
<span class="line">      <span class="token key atrule">version</span><span class="token punctuation">:</span> <span class="token string">&quot;2017.09&quot;</span></span>
<span class="line">      <span class="token key atrule">source</span><span class="token punctuation">:</span> https<span class="token punctuation">:</span>//github.com/rockchip<span class="token punctuation">-</span>linux/u<span class="token punctuation">-</span>boot.git<span class="token comment">#b14196eade471bbc000c368f8555f2a2a1ecc17d</span></span>
<span class="line">      <span class="token key atrule">defconfig</span><span class="token punctuation">:</span> rk3576_defconfig</span>
<span class="line">      <span class="token key atrule">device_tree</span><span class="token punctuation">:</span></span>
<span class="line">        <span class="token key atrule">dts</span><span class="token punctuation">:</span> ./dts/rk3576<span class="token punctuation">-</span>hd215<span class="token punctuation">-</span>uboot.dts</span>
<span class="line">      <span class="token key atrule">patches</span><span class="token punctuation">:</span></span>
<span class="line">        <span class="token punctuation">-</span> ./patches/uboot<span class="token punctuation">-</span>rockchip<span class="token punctuation">-</span>strux<span class="token punctuation">-</span>bootcmd.diff</span>
<span class="line">        <span class="token punctuation">-</span> ./patches/uboot<span class="token punctuation">-</span>drm<span class="token punctuation">-</span>logo<span class="token punctuation">-</span>fs<span class="token punctuation">-</span>fallback.diff</span>
<span class="line">      <span class="token key atrule">fragments</span><span class="token punctuation">:</span></span>
<span class="line">        <span class="token punctuation">-</span> <span class="token punctuation">|</span><span class="token scalar string"></span>
<span class="line">          CONFIG_CMD_SYSBOOT=y</span>
<span class="line">          CONFIG_SPLASH_SCREEN=y</span>
<span class="line">          CONFIG_CMD_BMP=y</span></span>
<span class="line">      <span class="token key atrule">boot_method</span><span class="token punctuation">:</span> extlinux</span>
<span class="line">      <span class="token key atrule">boot_config</span><span class="token punctuation">:</span> ./boot/extlinux.conf</span>
<span class="line">      <span class="token key atrule">blobs</span><span class="token punctuation">:</span></span>
<span class="line">        <span class="token punctuation">-</span> <span class="token key atrule">id</span><span class="token punctuation">:</span> ddr</span>
<span class="line">          <span class="token key atrule">role</span><span class="token punctuation">:</span> ddr_init</span>
<span class="line">          <span class="token key atrule">path</span><span class="token punctuation">:</span> ./blobs/rk3576_ddr_lp4_2112MHz_lp5_2736MHz_v1.09.bin</span>
<span class="line">          <span class="token key atrule">required</span><span class="token punctuation">:</span> <span class="token boolean important">true</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="types" tabindex="-1"><a class="header-anchor" href="#types"><span>Types</span></a></h2><table><thead><tr><th>Type</th><th>Status</th></tr></thead><tbody><tr><td><code>u-boot</code></td><td>Fully implemented built-in build: fetch, configure, patch, compile.</td></tr><tr><td><code>grub</code></td><td>Placeholder only — the step logs a warning and produces no real artifacts yet.</td></tr><tr><td><code>systemd-boot</code></td><td>Accepted by the schema, but the built-in build does not implement it yet.</td></tr><tr><td><code>custom</code></td><td>The built-in build is skipped; your BSP scripts produce the bootloader.</td></tr><tr><td><code>none</code></td><td>No bootloader is built (QEMU direct boot).</td></tr></tbody></table><p>Two ways to take over the build yourself, with slightly different mechanics:</p><ul><li><strong><code>type: custom</code></strong> — the built-in build script exits immediately; whatever your <code>before_bootloader</code>/<code>after_bootloader</code> scripts produce <em>is</em> the bootloader.</li><li><strong>A script with <code>step: custom_bootloader</code></strong> — regardless of <code>type</code>, registering this script replaces the built-in build with your script (the <code>before_</code>/<code>after_</code> hooks still run around it).</li></ul><p>All three hardware example BSPs use the custom route, because Rockchip&#39;s vendor U-Boot fork has its own packaging steps (<code>idbloader.img</code> assembly, FIT image generation) that the generic build doesn&#39;t know about. Their <code>scripts/build-bootloader-rockchip.sh</code> still reads <code>source</code>, <code>defconfig</code>, <code>patches</code>, <code>fragments</code>, and <code>blobs</code> from <code>bsp.yaml</code> — so the configuration stays declarative even when the build is custom.</p><h2 id="the-built-in-u-boot-build" tabindex="-1"><a class="header-anchor" href="#the-built-in-u-boot-build"><span>The built-in U-Boot build</span></a></h2><p>With <code>type: u-boot</code>, the step works through these stages, all in <code>dist/cache/&lt;bsp&gt;/</code>:</p><ol><li><strong>Fetch</strong> — <code>source</code> is a git URL with an optional <code>#branch</code>/<code>#tag</code>/<code>#commit</code> ref, or a tarball URL (<code>.tar.gz</code>, <code>.tar.xz</code>, <code>.tar.bz2</code>, <code>.tgz</code>; tarballs are cached). The checkout lives at <code>bootloader-source/</code>. <code>version</code> is a declarative label; <code>source</code> determines what&#39;s fetched.</li><li><strong>Stage blobs</strong> — see <a href="#vendor-boot-blobs">Vendor boot blobs</a> below.</li><li><strong>Install device trees</strong> — see <a href="#device-trees-at-the-bootloader-level">Device trees at the bootloader level</a>.</li><li><strong>Patch</strong> — <code>patches</code> are applied with <code>patch -p1</code>, dry-run first (a failing patch fails the build; an already-applied patch is skipped). Paths are relative to the BSP folder.</li><li><strong>Configure</strong> — <code>make &lt;defconfig&gt;</code> (the name of a file in U-Boot&#39;s <code>configs/</code>; required for the built-in build), then <code>fragments</code> are appended to the build&#39;s <code>.config</code> and re-synced with <code>olddefconfig</code>. Fragments work like kernel fragments: inline multiline strings of <code>CONFIG_*</code> lines, or file paths (<code>./configs/foo.config</code>). Later fragments win. The kernel comment syntax <code># CONFIG_FOO is not set</code> disables options.</li><li><strong>Compile</strong> — cross-compiled for your <code>arch</code>, with any blob <code>make_var</code>s passed on the make command line.</li></ol><p>The outputs land in <code>dist/cache/&lt;bsp&gt;/bootloader/</code>: <code>u-boot.bin</code>, plus <code>u-boot.itb</code> and <code>u-boot.img</code> when the board config produces them, the <code>spl/</code> directory (the <strong>SPL</strong> is U-Boot&#39;s tiny first-stage loader), and the final <code>.config</code> for reference. Your <code>make_image</code> script places these into the disk image at the offsets your SoC&#39;s boot ROM expects.</p><h2 id="device-trees-at-the-bootloader-level" tabindex="-1"><a class="header-anchor" href="#device-trees-at-the-bootloader-level"><span>Device trees at the bootloader level</span></a></h2><p>U-Boot uses a device tree of its own — separate from the kernel&#39;s — to drive the hardware <em>it</em> touches: the storage controller it loads from, the PMIC it programs, the panel it splashes on. <code>device_tree</code> under the bootloader configures it, and it supports two modes.</p><h3 id="standard-mode" tabindex="-1"><a class="header-anchor" href="#standard-mode"><span>Standard mode</span></a></h3><div class="language-yaml line-numbers-mode" data-highlighter="prismjs" data-ext="yml"><pre><code><span class="line">      <span class="token key atrule">device_tree</span><span class="token punctuation">:</span></span>
<span class="line">        <span class="token key atrule">dts</span><span class="token punctuation">:</span> ./dts/rk3288<span class="token punctuation">-</span>hd215<span class="token punctuation">-</span>uboot<span class="token punctuation">-</span>rockchip.dts</span>
<span class="line">        <span class="token key atrule">dtsi</span><span class="token punctuation">:</span></span>
<span class="line">          <span class="token punctuation">-</span> ./dts/rk3288<span class="token punctuation">-</span>hd215<span class="token punctuation">-</span>uboot<span class="token punctuation">-</span>rockchip<span class="token punctuation">-</span>u<span class="token punctuation">-</span>boot.dtsi</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Your DTS files are copied into the U-Boot source tree (<code>arch/arm/dts/</code> for both <code>arm</code> and <code>arm64</code>), registered in U-Boot&#39;s DTS Makefile, and built by U-Boot itself — so <code>#include</code>s of U-Boot&#39;s SoC dtsi files resolve normally. Strux also pins the configuration to your trees (<code>CONFIG_DEFAULT_DEVICE_TREE</code>, <code>CONFIG_OF_LIST</code>, <code>CONFIG_SPL_OF_LIST</code>) and passes <code>DEVICE_TREE=&lt;name&gt;</code> to make.</p><p>The extra keys:</p><ul><li><code>dtsi</code> — include files copied alongside your DTS so its <code>#include &quot;...&quot;</code> lines resolve. The <code>hd215-rk3288</code> BSP uses this for the <code>-u-boot.dtsi</code> file carrying U-Boot-specific properties.</li><li><code>include_paths</code> — directories whose <code>.dts</code>/<code>.dtsi</code> files are all copied into U-Boot&#39;s dts directory before the build.</li></ul><h3 id="standalone-mode" tabindex="-1"><a class="header-anchor" href="#standalone-mode"><span>Standalone mode</span></a></h3><div class="language-yaml line-numbers-mode" data-highlighter="prismjs" data-ext="yml"><pre><code><span class="line">      <span class="token key atrule">device_tree</span><span class="token punctuation">:</span></span>
<span class="line">        <span class="token key atrule">dts</span><span class="token punctuation">:</span> ./dts/rk3576<span class="token punctuation">-</span>board<span class="token punctuation">-</span>full.dts</span>
<span class="line">        <span class="token key atrule">standalone</span><span class="token punctuation">:</span> <span class="token boolean important">true</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>For a DTS that is <em>complete</em> — no includes, for example one decompiled from a running vendor system. Strux compiles it directly with <code>dtc</code>, bypassing U-Boot&#39;s device tree build entirely, and passes the result to make as <code>EXT_DTB=&lt;path&gt;</code>. U-Boot only supports one external DTB, so the first entry wins.</p><p>The <code>hd215-rk3576</code> BSP shows a related real-world pattern worth knowing: its U-Boot DTS only covers what the SPL needs, while U-Boot proper gets the <em>kernel&#39;s</em> DTB injected via <code>EXT_DTB</code> by the custom build script — mirroring how the board&#39;s factory loader works. Whether your board wants its configuration in the U-Boot DTS or the kernel DTB is a property of the vendor U-Boot fork; the vendor image is your reference.</p><h2 id="vendor-boot-blobs" tabindex="-1"><a class="header-anchor" href="#vendor-boot-blobs"><span>Vendor boot blobs</span></a></h2><p>On many SoCs, U-Boot is not actually the first code to run. The boot ROM first needs SoC-specific binaries that vendors ship only in binary form: DDR memory training firmware, ARM Trusted Firmware (BL31), a TEE/OP-TEE image (BL32). These are the <strong>boot blobs</strong> — you typically extract them from the vendor&#39;s SDK or <code>rkbin</code> repository, commit them to the BSP&#39;s <code>blobs/</code> folder, and declare them:</p><div class="language-yaml line-numbers-mode" data-highlighter="prismjs" data-ext="yml"><pre><code><span class="line">      <span class="token key atrule">blobs</span><span class="token punctuation">:</span></span>
<span class="line">        <span class="token punctuation">-</span> <span class="token key atrule">id</span><span class="token punctuation">:</span> ddr</span>
<span class="line">          <span class="token key atrule">role</span><span class="token punctuation">:</span> ddr_init</span>
<span class="line">          <span class="token key atrule">path</span><span class="token punctuation">:</span> ./blobs/rk3576_ddr_lp4_2112MHz_lp5_2736MHz_v1.09.bin</span>
<span class="line">          <span class="token key atrule">required</span><span class="token punctuation">:</span> <span class="token boolean important">true</span></span>
<span class="line">        <span class="token punctuation">-</span> <span class="token key atrule">id</span><span class="token punctuation">:</span> bl31</span>
<span class="line">          <span class="token key atrule">role</span><span class="token punctuation">:</span> bl31</span>
<span class="line">          <span class="token key atrule">path</span><span class="token punctuation">:</span> ./blobs/rk3576_bl31_v1.20.elf</span>
<span class="line">          <span class="token key atrule">make_var</span><span class="token punctuation">:</span> BL31</span>
<span class="line">          <span class="token key atrule">required</span><span class="token punctuation">:</span> <span class="token boolean important">true</span></span>
<span class="line">        <span class="token punctuation">-</span> <span class="token key atrule">id</span><span class="token punctuation">:</span> optee</span>
<span class="line">          <span class="token key atrule">role</span><span class="token punctuation">:</span> bl32</span>
<span class="line">          <span class="token key atrule">path</span><span class="token punctuation">:</span> ./blobs/rk3576_bl32_v1.06.bin</span>
<span class="line">          <span class="token key atrule">make_var</span><span class="token punctuation">:</span> TEE</span>
<span class="line">          <span class="token key atrule">required</span><span class="token punctuation">:</span> <span class="token boolean important">true</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Each blob:</p><table><thead><tr><th>Key</th><th>Required</th><th>Description</th></tr></thead><tbody><tr><td><code>id</code></td><td>yes</td><td>Your identifier for the blob.</td></tr><tr><td><code>role</code></td><td>yes</td><td>What it is in the boot chain. The example BSPs use <code>ddr_init</code>, <code>bl31</code>, <code>bl32</code>; any string is accepted.</td></tr><tr><td><code>path</code></td><td>yes</td><td>Where the file lives. <code>./</code> resolves to the BSP folder; <code>cache/</code> and <code>output/</code> resolve to the BSP&#39;s dist folders.</td></tr><tr><td><code>required</code></td><td>no</td><td>If <code>true</code>, a missing file fails the build instead of warning.</td></tr><tr><td><code>sha256</code></td><td>no</td><td>Checksum verified before use — recommended for binaries you can&#39;t rebuild.</td></tr><tr><td><code>make_var</code></td><td>no</td><td>Pass the blob&#39;s path to the U-Boot make invocation as <code>&lt;make_var&gt;=&lt;path&gt;</code>.</td></tr></tbody></table><p>During the build, blobs are staged to <code>dist/cache/&lt;bsp&gt;/bootloader/blobs/&lt;role&gt;/&lt;id&gt;</code> with a <code>manifest.tsv</code>, so custom packaging scripts can find them by role. For the make command line, <code>make_var</code> is explicit control; without it, the roles <code>bl31</code> and <code>bl32</code>/<code>tee</code> map automatically to U-Boot&#39;s <code>BL31=</code> and <code>TEE=</code> variables.</p><p>The Rockchip example makes the chain concrete: the boot ROM loads the DDR init blob plus U-Boot&#39;s SPL (packaged together as <code>idbloader.img</code>), which loads a FIT image (<code>u-boot.itb</code>) containing BL31, OP-TEE, and U-Boot proper — which finally boots Linux. The <code>package-rockchip.sh</code> scripts in the example BSPs assemble exactly that.</p><h2 id="boot-method-and-boot-config" tabindex="-1"><a class="header-anchor" href="#boot-method-and-boot-config"><span>Boot method and boot config</span></a></h2><p><code>boot_method</code> declares <em>how the bootloader finds the kernel</em>, and <code>boot_config</code> points at the template file for it:</p><div class="language-yaml line-numbers-mode" data-highlighter="prismjs" data-ext="yml"><pre><code><span class="line">      <span class="token key atrule">boot_method</span><span class="token punctuation">:</span> extlinux</span>
<span class="line">      <span class="token key atrule">boot_config</span><span class="token punctuation">:</span> ./boot/extlinux.conf</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><table><thead><tr><th>Method</th><th>Meaning</th></tr></thead><tbody><tr><td><code>extlinux</code></td><td>U-Boot&#39;s &quot;distro boot&quot;: it reads a plain-text menu file at <code>/boot/extlinux/extlinux.conf</code> naming the kernel, initrd, DTB, and kernel command line.</td></tr><tr><td><code>script</code></td><td>A compiled U-Boot script (<code>boot.scr</code>) with explicit boot commands.</td></tr><tr><td><code>direct</code></td><td>No boot configuration — something else loads the kernel (QEMU does this).</td></tr></tbody></table><p>These two keys are declarative: the Strux core records them, and <strong>the BSP&#39;s own lifecycle scripts act on them</strong>. The convention, implemented by <code>scripts/install-boot-config.sh</code> in the hardware example BSPs (registered at the <code>before_bundle</code> step), is:</p><ul><li><code>extlinux</code> — copy the <code>boot_config</code> file to <code>/boot/extlinux/extlinux.conf</code> in the rootfs.</li><li><code>script</code> — compile the <code>boot_config</code> source with <code>mkimage -T script</code> and install it as <code>/boot/boot.scr</code>.</li><li><code>direct</code> — install nothing.</li></ul><p>Copy that script into a new BSP rather than reinventing it. The <code>extlinux.conf</code> from <code>hd215-rk3576</code> shows what the file contains:</p><div class="language-txt line-numbers-mode" data-highlighter="prismjs" data-ext="txt"><pre><code><span class="line">default strux</span>
<span class="line">timeout 1</span>
<span class="line"></span>
<span class="line">label strux</span>
<span class="line">    kernel /boot/vmlinuz</span>
<span class="line">    initrd /boot/initrd.img</span>
<span class="line">    fdt /boot/dtbs/rk3576-hd215-linux.dtb</span>
<span class="line">    append root=LABEL=root rootfstype=ext4 rw rootwait splash console=ttyS0,1500000</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>kernel</code>, <code>initrd</code>, and <code>fdt</code> point at the artifacts your kernel build produced (installed into <code>/boot</code> by the image scripts); <code>append</code> is the kernel command line — root device, console, splash flags.</p><div class="hint-container warning"><p class="hint-container-title">Experimental: A/B boot scripts</p><p>The example BSPs also carry a <code>boot/strux-ab-boot.cmd</code> U-Boot script and <code>-ab.genimage.cfg</code> partition layouts. These belong to the dual-rootfs A/B update system, which is experimental and only activates when updates are enabled in <code>strux.yaml</code> — the install script then switches to slot-aware boot assets instead of the plain extlinux flow. The design may change; see <a href="/bsp/concepts/dual-rootfs.html" target="_blank" rel="noopener noreferrer">Dual RootFS</a> before depending on it.</p></div><h2 id="splash-screens" tabindex="-1"><a class="header-anchor" href="#splash-screens"><span>Splash screens</span></a></h2><p>A kiosk should show your logo from the first second, and that&#39;s a bootloader job. The fragments in the example BSPs (<code>CONFIG_SPLASH_SCREEN=y</code>, <code>CONFIG_CMD_BMP=y</code>, <code>CONFIG_BMP_24BPP=y</code>, <code>CONFIG_HIDE_LOGO_VERSION=y</code>) enable U-Boot&#39;s BMP splash support, and an <code>after_bootloader</code> script (<code>install-boot-logo.sh</code>) converts the project&#39;s splash PNG to BMP for it — BSP scripts receive <code>SPLASH_ENABLED</code>, <code>SPLASH_LOGO</code>, and <code>SPLASH_COLOR</code> from <code>strux.yaml</code>, plus <code>DISPLAY_WIDTH</code>/<code>DISPLAY_HEIGHT</code>, for exactly this. The patch <code>uboot-drm-logo-fs-fallback.diff</code>, shared by all three hardware BSPs, teaches the Rockchip U-Boot to load the logo from the rootfs.</p><h2 id="where-to-go-next" tabindex="-1"><a class="header-anchor" href="#where-to-go-next"><span>Where to go next</span></a></h2><ul><li><a href="/bsp/guide/writing-a-bsp.html" target="_blank" rel="noopener noreferrer">Writing a BSP</a> — where the bootloader fits in the bring-up sequence.</li><li><a href="/bsp/concepts/lifecycle-scripts.html" target="_blank" rel="noopener noreferrer">Lifecycle Scripts</a> — <code>before_bootloader</code>, <code>after_bootloader</code>, <code>custom_bootloader</code>.</li><li><a href="/bsp/guide/flash-scripts.html" target="_blank" rel="noopener noreferrer">Flash Scripts</a> — getting the assembled image onto the board.</li><li><a href="/bsp/reference/bsp-yaml.html" target="_blank" rel="noopener noreferrer">bsp.yaml Reference</a> — every bootloader key in one table.</li><li><a href="/bsp/guide/examples.html" target="_blank" rel="noopener noreferrer">Example BSPs</a> — three real Rockchip bootloader setups to crib from.</li></ul>`,49)])])}const c=s(a,[["render",l]]),r=JSON.parse('{"path":"/bsp/guide/bootloader.html","title":"Bootloaders","lang":"en-US","frontmatter":{},"headers":[{"level":2,"title":"What the bootloader does","slug":"what-the-bootloader-does","link":"#what-the-bootloader-does","children":[]},{"level":2,"title":"The bootloader block","slug":"the-bootloader-block","link":"#the-bootloader-block","children":[]},{"level":2,"title":"Types","slug":"types","link":"#types","children":[]},{"level":2,"title":"The built-in U-Boot build","slug":"the-built-in-u-boot-build","link":"#the-built-in-u-boot-build","children":[]},{"level":2,"title":"Device trees at the bootloader level","slug":"device-trees-at-the-bootloader-level","link":"#device-trees-at-the-bootloader-level","children":[{"level":3,"title":"Standard mode","slug":"standard-mode","link":"#standard-mode","children":[]},{"level":3,"title":"Standalone mode","slug":"standalone-mode","link":"#standalone-mode","children":[]}]},{"level":2,"title":"Vendor boot blobs","slug":"vendor-boot-blobs","link":"#vendor-boot-blobs","children":[]},{"level":2,"title":"Boot method and boot config","slug":"boot-method-and-boot-config","link":"#boot-method-and-boot-config","children":[]},{"level":2,"title":"Splash screens","slug":"splash-screens","link":"#splash-screens","children":[]},{"level":2,"title":"Where to go next","slug":"where-to-go-next","link":"#where-to-go-next","children":[]}],"git":{"updatedTime":1781317241000,"contributors":[{"name":"Miguel Medeiros","username":"","email":"miguel@medeirosconsulting.ca","commits":1}],"changelog":[{"hash":"e32366d86296aed0c45cf8447d8ea8147c1d03da","time":1781317241000,"email":"miguel@medeirosconsulting.ca","author":"Miguel Medeiros","message":"add docs"}]},"filePathRelative":"bsp/guide/bootloader.md"}');export{c as comp,r as data};
