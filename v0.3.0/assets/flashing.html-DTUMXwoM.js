import{_ as s,c as a,a as n,o as t}from"./app-BJNURCT2.js";const r={};function o(i,e){return t(),a("div",null,[...e[0]||(e[0]=[n(`<h1 id="flashing" tabindex="-1"><a class="header-anchor" href="#flashing"><span>Flashing</span></a></h1><p>Flashing is the step where your built image leaves your laptop and lands on real hardware — written to a board&#39;s eMMC (built-in flash storage), an SD card, or whatever your board boots from. This page explains what <code>strux flash</code> does, what a real flash flow looks like, and what to do if your board doesn&#39;t have one yet.</p><h2 id="what-strux-flash-does" tabindex="-1"><a class="header-anchor" href="#what-strux-flash-does"><span>What <code>strux flash</code> does</span></a></h2><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh"><pre><code><span class="line">strux flash my-board</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p><code>strux flash</code> runs the <strong>flash scripts defined by your BSP</strong> (Board Support Package — the folder under <code>bsp/</code> describing your hardware). Every board is flashed differently — Rockchip boards use a USB recovery mode, others want an image <code>dd</code>&#39;d to an SD card — so Strux doesn&#39;t hardcode a flashing method. Instead, the BSP declares scripts in its <code>bsp.yaml</code>, and <code>strux flash</code> executes them <strong>on your host machine</strong> (not inside Docker, since they need direct access to your USB ports and card readers).</p><p>If you omit the BSP name, the <code>bsp</code> field from <code>strux.yaml</code> is used:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh"><pre><code><span class="line">strux flash</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>Two kinds of scripts run, in order:</p><ol><li><strong><code>flash_script_tool</code></strong> scripts — preparation: locate or set up the host-side flashing tool the board needs.</li><li><strong><code>flash_script</code></strong> scripts — the actual flash: talk to the board and write the image.</li></ol><p>The scripts run with a working directory of <code>dist/flash/&lt;bsp&gt;/</code> (created for you — a scratch space for tools and temporary files) and receive the standard Strux environment variables, including <code>PROJECT_DIST_OUTPUT_FOLDER</code> (where the built image lives), <code>FLASH_DIR</code>, <code>BSP_FOLDER</code>, <code>BSP_NAME</code>, and <code>TARGET_ARCH</code>. The full list is in the <a href="/bsp/reference/environment-variables.html" target="_blank" rel="noopener noreferrer">environment variables reference</a>.</p><div class="hint-container tip"><p class="hint-container-title">Build first</p><p><code>strux flash</code> doesn&#39;t build anything — it expects the image to already exist in <code>dist/output/&lt;bsp&gt;/</code>. Run <a href="/guide/building.html" target="_blank" rel="noopener noreferrer"><code>strux build</code></a> first; the flash script will tell you if the image is missing.</p></div><p>You can also trigger a flash from inside <a href="/guide/dev-mode.html" target="_blank" rel="noopener noreferrer">dev mode</a>: the TUI shows a <strong>Flash</strong> entry (and a &quot;Flash Device&quot; action in the config panel) whenever the active BSP defines a <code>flash_script</code>, with the script&#39;s output streamed into its own log tab.</p><h2 id="a-real-example-rockchip-rk3576" tabindex="-1"><a class="header-anchor" href="#a-real-example-rockchip-rk3576"><span>A real example: Rockchip RK3576</span></a></h2><p>Here&#39;s how a real BSP wires this up. A Rockchip RK3576 board flashes over USB using <strong>Maskrom mode</strong> — a recovery mode baked into the chip where it waits for a host tool (<code>rkdeveloptool</code>) to send it a bootloader and an image. The BSP declares both script kinds in <code>bsp.yaml</code>:</p><div class="language-yaml line-numbers-mode" data-highlighter="prismjs" data-ext="yml"><pre><code><span class="line">  <span class="token key atrule">scripts</span><span class="token punctuation">:</span></span>
<span class="line">    <span class="token punctuation">-</span> <span class="token key atrule">location</span><span class="token punctuation">:</span> ./scripts/prepare<span class="token punctuation">-</span>rkdeveloptool.sh</span>
<span class="line">      <span class="token key atrule">step</span><span class="token punctuation">:</span> flash_script_tool</span>
<span class="line">      <span class="token key atrule">description</span><span class="token punctuation">:</span> <span class="token string">&quot;Prepare rkdeveloptool for HD215 flashing&quot;</span></span>
<span class="line"></span>
<span class="line">    <span class="token punctuation">-</span> <span class="token key atrule">location</span><span class="token punctuation">:</span> ./scripts/flash<span class="token punctuation">-</span>rk3576.sh</span>
<span class="line">      <span class="token key atrule">step</span><span class="token punctuation">:</span> flash_script</span>
<span class="line">      <span class="token key atrule">description</span><span class="token punctuation">:</span> <span class="token string">&quot;Flash HD215 RK3576 eMMC over Rockchip Maskrom&quot;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Running <code>strux flash hd215-rk3576</code> then looks like this:</p><ol><li><strong>Tool preparation</strong> — <code>prepare-rkdeveloptool.sh</code> finds <code>rkdeveloptool</code> on your machine (checking your <code>PATH</code> and common install locations) and copies the board&#39;s SPL loader blob from the BSP folder into <code>dist/flash/hd215-rk3576/tools/</code>.</li><li><strong>Wait for the board</strong> — <code>flash-rk3576.sh</code> polls until it sees a device in Maskrom mode. This is when you connect the board over USB and put it into recovery mode.</li><li><strong>Flash</strong> — the script sends the loader to enter flash mode, erases the eMMC, and writes <code>dist/output/hd215-rk3576/strux-hd215-rk3576.img</code> to the board, reporting percentage progress as it goes.</li></ol><div class="language-txt line-numbers-mode" data-highlighter="prismjs" data-ext="txt"><pre><code><span class="line">◐ Trying to find HD215-RK3576</span>
<span class="line">◐ Entering Flash Mode</span>
<span class="line">◐ Erasing Flash</span>
<span class="line">▰ Flashing Image (42%)</span>
<span class="line">◐ HD215-RK3576 flash completed</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Flash scripts report progress by printing <code>STRUX_PROGRESS:</code> and <code>STRUX_PROGRESS_BAR:</code> markers, which the CLI renders as the spinner and progress bar above.</p><p>When the script finishes, power-cycle the board: it boots your image — splash logo, then your app.</p><h2 id="if-your-bsp-has-no-flash-script" tabindex="-1"><a class="header-anchor" href="#if-your-bsp-has-no-flash-script"><span>If your BSP has no flash script</span></a></h2><p><code>strux flash</code> exits with an error if the BSP doesn&#39;t define a <code>flash_script</code>:</p><div class="language-txt line-numbers-mode" data-highlighter="prismjs" data-ext="txt"><pre><code><span class="line">Not Available for this BSP: my-board does not define a flash_script in bsp.yaml.</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>That just means nobody has written one for this board yet. Two options:</p><ul><li><strong>Flash manually.</strong> The built image is a regular file in <code>dist/output/&lt;bsp&gt;/</code> — if your board boots from an SD card, writing the image with <code>dd</code> or a tool like balenaEtcher works fine.</li><li><strong>Write a flash script.</strong> It&#39;s an ordinary bash script plus two lines of <code>bsp.yaml</code> — the <a href="/bsp/guide/flash-scripts.html" target="_blank" rel="noopener noreferrer">flash scripts guide</a> walks through writing one, with the conventions for tool preparation, progress markers, and error handling.</li></ul><h2 id="where-to-go-next" tabindex="-1"><a class="header-anchor" href="#where-to-go-next"><span>Where to go next</span></a></h2><ul><li><a href="/bsp/guide/flash-scripts.html" target="_blank" rel="noopener noreferrer">Flash Scripts</a> — write a flash script for your board.</li><li><a href="/guide/dev-mode.html" target="_blank" rel="noopener noreferrer">Dev Mode</a> — flash a development image, then develop against the real device with <code>strux dev --remote</code>.</li><li><a href="/guide/updates.html" target="_blank" rel="noopener noreferrer">Updates</a> — after the first flash, ship new versions over the network instead of re-flashing.</li></ul>`,27)])])}const d=s(r,[["render",o]]),c=JSON.parse('{"path":"/guide/flashing.html","title":"Flashing","lang":"en-US","frontmatter":{},"headers":[{"level":2,"title":"What strux flash does","slug":"what-strux-flash-does","link":"#what-strux-flash-does","children":[]},{"level":2,"title":"A real example: Rockchip RK3576","slug":"a-real-example-rockchip-rk3576","link":"#a-real-example-rockchip-rk3576","children":[]},{"level":2,"title":"If your BSP has no flash script","slug":"if-your-bsp-has-no-flash-script","link":"#if-your-bsp-has-no-flash-script","children":[]},{"level":2,"title":"Where to go next","slug":"where-to-go-next","link":"#where-to-go-next","children":[]}],"git":{"updatedTime":1781317241000,"contributors":[{"name":"Miguel Medeiros","username":"","email":"miguel@medeirosconsulting.ca","commits":1}],"changelog":[{"hash":"e32366d86296aed0c45cf8447d8ea8147c1d03da","time":1781317241000,"email":"miguel@medeirosconsulting.ca","author":"Miguel Medeiros","message":"add docs"}]},"filePathRelative":"guide/flashing.md"}');export{d as comp,c as data};
