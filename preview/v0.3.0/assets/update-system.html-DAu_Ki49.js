import{_ as t,c as s,a,o as n}from"./app-BxL53bLZ.js";const o={};function i(l,e){return n(),s("div",null,[...e[0]||(e[0]=[a(`<h1 id="update-system" tabindex="-1"><a class="header-anchor" href="#update-system"><span>Update System</span></a></h1><p>This page explains how Strux OTA updates work internally: the <code>.struxb</code> bundle format, the signing and verification model, and how a device installs an update and recovers from a bad one. For the step-by-step workflow, see the <a href="/guide/updates.html" target="_blank" rel="noopener noreferrer">Updates guide</a>.</p><div class="hint-container warning"><p class="hint-container-title">Experimental</p><p>Right now, System Updates are an experimental feature. We have major plans regarding this feature, so stay tuned!</p></div><h2 id="the-mental-model" tabindex="-1"><a class="header-anchor" href="#the-mental-model"><span>The mental model</span></a></h2><p>A Strux update is <strong>the whole OS, replaced atomically</strong>. There is no package manager on the device and no incremental patching — a bundle contains a complete root filesystem image, and the device keeps two copies of the OS side by side:</p><div class="language-txt line-numbers-mode" data-highlighter="prismjs" data-ext="txt"><pre><code><span class="line">┌─────────────────┬─────────────────┬──────────────┐</span>
<span class="line">│  strux-rootfs-a │  strux-rootfs-b │  strux-data  │</span>
<span class="line">│  (running OS)   │  (idle slot)    │  (FAT32)     │</span>
<span class="line">└─────────────────┴─────────────────┴──────────────┘</span>
<span class="line">        ▲                  ▲                ▲</span>
<span class="line">        │                  │                │</span>
<span class="line">   booted from      update written     boot script +</span>
<span class="line">   this slot        here, then         slot selection</span>
<span class="line">                    becomes active     state (read by</span>
<span class="line">                                       the bootloader)</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>An update is written to the idle slot while the device keeps running. Only after the payload is fully verified on disk does the device ask the bootloader to try the new slot — with a bounded number of attempts, so a broken update rolls back by itself.</p><h2 id="the-bundle-format-struxb" tabindex="-1"><a class="header-anchor" href="#the-bundle-format-struxb"><span>The bundle format (.struxb)</span></a></h2><p>A <code>.struxb</code> file is a gzip-compressed tar archive with exactly three members:</p><div class="language-txt line-numbers-mode" data-highlighter="prismjs" data-ext="txt"><pre><code><span class="line">update.struxb (tar.gz)</span>
<span class="line">├── manifest.json    # what&#39;s in the bundle, and how it&#39;s signed</span>
<span class="line">├── manifest.sig     # base64 RSA-PSS signature over manifest.json</span>
<span class="line">└── rootfs.img       # the complete root filesystem (ext4)</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The manifest is small and human-readable:</p><div class="language-json line-numbers-mode" data-highlighter="prismjs" data-ext="json"><pre><code><span class="line"><span class="token punctuation">{</span></span>
<span class="line">  <span class="token property">&quot;schema&quot;</span><span class="token operator">:</span> <span class="token string">&quot;dev.strux.update.bundle.v1&quot;</span><span class="token punctuation">,</span></span>
<span class="line">  <span class="token property">&quot;bsp&quot;</span><span class="token operator">:</span> <span class="token string">&quot;hd215-rk3576&quot;</span><span class="token punctuation">,</span></span>
<span class="line">  <span class="token property">&quot;version&quot;</span><span class="token operator">:</span> <span class="token string">&quot;1.2.0&quot;</span><span class="token punctuation">,</span></span>
<span class="line">  <span class="token property">&quot;projectVersion&quot;</span><span class="token operator">:</span> <span class="token string">&quot;1.2.0&quot;</span><span class="token punctuation">,</span></span>
<span class="line">  <span class="token property">&quot;struxVersion&quot;</span><span class="token operator">:</span> <span class="token string">&quot;0.3.0&quot;</span><span class="token punctuation">,</span></span>
<span class="line">  <span class="token property">&quot;createdAt&quot;</span><span class="token operator">:</span> <span class="token string">&quot;2026-06-12T15:04:05Z&quot;</span><span class="token punctuation">,</span></span>
<span class="line">  <span class="token property">&quot;payload&quot;</span><span class="token operator">:</span> <span class="token punctuation">{</span></span>
<span class="line">    <span class="token property">&quot;type&quot;</span><span class="token operator">:</span> <span class="token string">&quot;full-rootfs&quot;</span><span class="token punctuation">,</span></span>
<span class="line">    <span class="token property">&quot;file&quot;</span><span class="token operator">:</span> <span class="token string">&quot;rootfs.img&quot;</span><span class="token punctuation">,</span></span>
<span class="line">    <span class="token property">&quot;size&quot;</span><span class="token operator">:</span> <span class="token number">2147483648</span><span class="token punctuation">,</span></span>
<span class="line">    <span class="token property">&quot;sha256&quot;</span><span class="token operator">:</span> <span class="token string">&quot;...&quot;</span></span>
<span class="line">  <span class="token punctuation">}</span><span class="token punctuation">,</span></span>
<span class="line">  <span class="token property">&quot;signing&quot;</span><span class="token operator">:</span> <span class="token punctuation">{</span></span>
<span class="line">    <span class="token property">&quot;algorithm&quot;</span><span class="token operator">:</span> <span class="token string">&quot;rsa-pss-sha512&quot;</span><span class="token punctuation">,</span></span>
<span class="line">    <span class="token property">&quot;keyBits&quot;</span><span class="token operator">:</span> <span class="token number">4096</span><span class="token punctuation">,</span></span>
<span class="line">    <span class="token property">&quot;saltLength&quot;</span><span class="token operator">:</span> <span class="token string">&quot;hash&quot;</span><span class="token punctuation">,</span></span>
<span class="line">    <span class="token property">&quot;signedBytes&quot;</span><span class="token operator">:</span> <span class="token string">&quot;manifest.json&quot;</span></span>
<span class="line">  <span class="token punctuation">}</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The signature covers the manifest, and the manifest pins the payload by size and SHA-256 — so verifying the small manifest transitively verifies the multi-gigabyte payload. The member order in the archive (manifest before payload) is what makes streaming installation possible: by the time the payload bytes arrive, the device already knows their expected hash.</p><p>Bundles are created by <code>strux update bundle</code> (or automatically at the end of a build — see <a href="#update-enabled-and-auto-bundle">semantics</a> below), which runs inside the builder container and signs with OpenSSL.</p><h2 id="signing-and-verification" tabindex="-1"><a class="header-anchor" href="#signing-and-verification"><span>Signing and verification</span></a></h2><p>The trust model is a single project-owned RSA keypair:</p><ul><li><strong>Private key</strong> (<code>strux-update.key</code>): generated by <code>strux init</code> or <code>strux update gen-keypair</code>, 4096-bit, kept out of git, used by the CLI to sign manifests with RSA-PSS/SHA-512 (salt length = hash length).</li><li><strong>Public key</strong> (<code>strux-update.pub</code>): committed with the project and installed into every update-enabled image at <code>/etc/strux/update.pub</code>.</li></ul><p>On the device, the Strux client rejects a bundle unless <strong>all</strong> of these hold:</p><ol><li>The signature over <code>manifest.json</code> verifies against <code>/etc/strux/update.pub</code> (RSA keys under 4096 bits are refused outright).</li><li>The manifest declares the supported schema (<code>dev.strux.update.bundle.v1</code>), payload type (<code>full-rootfs</code>), and signing algorithm (<code>rsa-pss-sha512</code>).</li><li>The manifest&#39;s <code>bsp</code> matches the BSP recorded in the image at <code>/etc/strux/project.json</code> — a bundle built for one board cannot be installed on another.</li><li>The payload&#39;s size and SHA-256 match the manifest, checked twice: once while writing, and again by reading the slot back from disk.</li></ol><p>There is deliberately no unsigned mode — a device without a valid public key cannot install updates at all.</p><h2 id="how-the-client-applies-an-update" tabindex="-1"><a class="header-anchor" href="#how-the-client-applies-an-update"><span>How the client applies an update</span></a></h2><p>The on-device Strux client receives an update either as a URL to download or a local file path (today, both arrive via the dev server connection — <code>strux update send</code>). Installation is a streaming pipeline:</p><ol><li><strong>Identify the idle slot.</strong> The kernel command line carries <code>strux.slot=A</code> or <code>strux.slot=B</code>; the client targets the other one, at <code>/dev/disk/by-partlabel/strux-rootfs-a</code> or <code>-b</code>.</li><li><strong>Stream and verify.</strong> The tar stream is decompressed on the fly: manifest and signature are read and verified first, then the rootfs payload is written directly to the idle slot&#39;s block device while being hashed. No temporary copy of the payload ever exists.</li><li><strong>Re-verify on disk.</strong> The slot is read back and its size and SHA-256 compared against the manifest — catching any write or storage error.</li><li><strong>Relabel.</strong> The new filesystem is relabeled (<code>e2label</code>) to its slot&#39;s label (<code>strux-rootfs-a</code>/<code>strux-rootfs-b</code>), since the bundled image carries the build-time label.</li><li><strong>Arm the bootloader.</strong> The client writes the boot environment on the shared data partition: the idle slot becomes <em>pending</em> with <code>strux_tries=3</code>, and the generation counter increments. The environment is written redundantly (<code>BOOTENV.TXT</code> and <code>BOOTBAK.TXT</code>) so a power cut mid-write can&#39;t lose the boot state.</li><li><strong>Reboot.</strong> On each boot attempt, the bootloader decrements the try counter <em>before</em> booting the pending slot. When the new OS comes up and the Strux client starts, it marks the slot active and clears the pending state. If the counter reaches zero without that ever happening, the bootloader boots the old slot — automatic rollback.</li></ol><p>Throughout the install, the client writes progress to <code>/run/strux/update-progress.json</code> and persists slot state to <code>/strux-data/strux/update-state.json</code>. The Go runtime exposes both to your app under the <code>update</code> API namespace (progress and state queries), so your frontend can render an update screen — see the <a href="/reference/go-runtime.html" target="_blank" rel="noopener noreferrer">Go runtime reference</a>.</p><h2 id="update-enabled-and-auto-bundle" tabindex="-1"><a class="header-anchor" href="#update-enabled-and-auto-bundle"><span>update.enabled and auto_bundle</span></a></h2><div class="language-yaml line-numbers-mode" data-highlighter="prismjs" data-ext="yml"><pre><code><span class="line"><span class="token key atrule">update</span><span class="token punctuation">:</span></span>
<span class="line">  <span class="token key atrule">enabled</span><span class="token punctuation">:</span> <span class="token boolean important">true</span></span>
<span class="line">  <span class="token key atrule">auto_bundle</span><span class="token punctuation">:</span> <span class="token boolean important">true</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><table><thead><tr><th>Key</th><th>Type</th><th>Default</th><th>Description</th></tr></thead><tbody><tr><td><code>enabled</code></td><td>boolean</td><td><code>false</code></td><td>Makes the build update-capable. BSP lifecycle scripts receive <code>STRUX_UPDATE_ENABLED=true</code> and switch to the A/B image layout; the public key gets baked into the image.</td></tr><tr><td><code>auto_bundle</code></td><td>boolean</td><td><code>false</code></td><td>After the BSP&#39;s <code>make_image</code> step, automatically sign <code>dist/output/&lt;bsp&gt;/rootfs.ext4</code> into a <code>.struxb</code> bundle.</td></tr></tbody></table><p>The two are independent switches but bundling requires both: the build pipeline only generates a bundle when <code>enabled</code> <strong>and</strong> <code>auto_bundle</code> are true. <code>enabled: true</code> with <code>auto_bundle: false</code> builds an update-capable image and leaves bundling to an explicit <code>strux update bundle</code>.</p><p>Note that <code>enabled</code> changes the <em>image layout</em>, not just a feature flag — flipping it produces a different partition table, so devices flashed without it can&#39;t be updated over the air.</p><div class="hint-container warning"><p class="hint-container-title">Experimental — A/B dual-rootfs conventions</p><p>The dual-rootfs design is <strong>experimental and may change</strong>. However, BSPs targeting Strux v0.3.0 that want update support must follow these conventions today — the on-device client and bootloader contract depend on them:</p><ul><li><strong>Partitions:</strong> a GPT disk with two equally-sized ext4 rootfs partitions whose GPT partition labels (and discoverable paths) are <code>strux-rootfs-a</code> (populated) and <code>strux-rootfs-b</code> (left blank), plus a shared FAT32 partition labeled <code>strux-data</code>. The client resolves slots via <code>/dev/disk/by-partlabel/</code>.</li><li><strong>Filesystem labels:</strong> the rootfs filesystems are labeled <code>strux-rootfs-a</code> / <code>strux-rootfs-b</code> to match their slots.</li><li><strong><code>strux-data</code> contents:</strong> FAT32 (so the bootloader can read <em>and write</em> it), containing <code>/strux/boot.scr</code> (the compiled A/B boot script), <code>/strux/BOOTENV.TXT</code> + <code>/strux/BOOTBAK.TXT</code> (redundant text boot environment), and <code>/strux/update-state.json</code>.</li><li><strong>Boot environment keys:</strong> <code>strux_active</code>, <code>strux_pending</code>, <code>strux_tries</code>, <code>strux_generation</code> — the bootloader selects the slot, decrements <code>strux_tries</code> for a pending slot before booting it, and falls back to the active slot when tries are exhausted.</li><li><strong>Kernel command line:</strong> must carry <code>strux.slot=&lt;A|B&gt;</code> so the client knows which slot it booted from, and the root device is selected by partition label (<code>root=PARTLABEL=strux-rootfs-a</code> or <code>-b</code>).</li><li><strong>Rootfs configuration:</strong> the image mounts the data partition at <code>/strux-data</code> via fstab, installs the update public key at <code>/etc/strux/update.pub</code> (the build must fail if <code>strux-update.pub</code> is missing), and keeps a migration copy of the boot script at <code>/etc/strux/boot.scr</code>. First-boot partition resizing must be disabled — slot boundaries are fixed.</li><li><strong>Update payload export:</strong> the <code>make_image</code> step must also export the bare rootfs image to <code>dist/output/&lt;bsp&gt;/rootfs.ext4</code>, which is what <code>strux update bundle</code> signs by default.</li></ul><p>For the bootloader scripting, image generation, and a complete worked example, see <a href="/bsp/concepts/dual-rootfs.html" target="_blank" rel="noopener noreferrer">Dual Rootfs</a> in the BSP docs.</p></div><h2 id="where-to-go-next" tabindex="-1"><a class="header-anchor" href="#where-to-go-next"><span>Where to go next</span></a></h2><ul><li><a href="/guide/updates.html" target="_blank" rel="noopener noreferrer">Updates guide</a> — the end-to-end workflow with commands.</li><li><a href="/bsp/concepts/dual-rootfs.html" target="_blank" rel="noopener noreferrer">Dual Rootfs</a> — implementing the conventions in a BSP.</li><li><a href="/concepts/build-pipeline.html" target="_blank" rel="noopener noreferrer">Build Pipeline</a> — where image creation and auto-bundling sit in the build.</li></ul>`,32)])])}const d=t(o,[["render",i]]),p=JSON.parse('{"path":"/concepts/update-system.html","title":"Update System","lang":"en-US","frontmatter":{},"headers":[{"level":2,"title":"The mental model","slug":"the-mental-model","link":"#the-mental-model","children":[]},{"level":2,"title":"The bundle format (.struxb)","slug":"the-bundle-format-struxb","link":"#the-bundle-format-struxb","children":[]},{"level":2,"title":"Signing and verification","slug":"signing-and-verification","link":"#signing-and-verification","children":[]},{"level":2,"title":"How the client applies an update","slug":"how-the-client-applies-an-update","link":"#how-the-client-applies-an-update","children":[]},{"level":2,"title":"update.enabled and auto_bundle","slug":"update-enabled-and-auto-bundle","link":"#update-enabled-and-auto-bundle","children":[]},{"level":2,"title":"Where to go next","slug":"where-to-go-next","link":"#where-to-go-next","children":[]}],"git":{"updatedTime":1781317241000,"contributors":[{"name":"Miguel Medeiros","username":"","email":"miguel@medeirosconsulting.ca","commits":1}],"changelog":[{"hash":"e32366d86296aed0c45cf8447d8ea8147c1d03da","time":1781317241000,"email":"miguel@medeirosconsulting.ca","author":"Miguel Medeiros","message":"add docs"}]},"filePathRelative":"concepts/update-system.md"}');export{d as comp,p as data};
