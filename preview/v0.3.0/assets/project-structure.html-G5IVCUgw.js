import{_ as t,c as a,a as s,o as r}from"./app-BxL53bLZ.js";const i={};function n(o,e){return r(),a("div",null,[...e[0]||(e[0]=[s(`<h1 id="project-structure" tabindex="-1"><a class="header-anchor" href="#project-structure"><span>Project Structure</span></a></h1><p>This page walks through every file and folder in a Strux project — what it&#39;s for, what you should edit, and what Strux manages for you. If you haven&#39;t created a project yet, start with <a href="/guide/getting-started.html" target="_blank" rel="noopener noreferrer">Getting Started</a>.</p><h2 id="the-scaffolded-project" tabindex="-1"><a class="header-anchor" href="#the-scaffolded-project"><span>The scaffolded project</span></a></h2><p>Running <code>strux init my-kiosk</code> creates this:</p><div class="language-txt line-numbers-mode" data-highlighter="prismjs" data-ext="txt"><pre><code><span class="line">my-kiosk/</span>
<span class="line">├── strux.yaml           # Project configuration</span>
<span class="line">├── main.go              # Go backend entry point</span>
<span class="line">├── go.mod / go.sum      # Go module files</span>
<span class="line">├── frontend/            # Your web app (Vite project)</span>
<span class="line">│   └── src/strux.d.ts   # Generated TypeScript types for the backend API</span>
<span class="line">├── bsp/</span>
<span class="line">│   └── qemu/            # Board profile for local testing</span>
<span class="line">│       ├── bsp.yaml     # BSP configuration</span>
<span class="line">│       ├── scripts/     # BSP lifecycle scripts (make-image.sh)</span>
<span class="line">│       └── overlay/     # BSP-specific filesystem overlay</span>
<span class="line">├── assets/</span>
<span class="line">│   └── logo.png         # Splash screen logo</span>
<span class="line">├── overlay/             # Global filesystem overlay</span>
<span class="line">├── strux-update.key     # Update signing key (private — gitignored)</span>
<span class="line">├── strux-update.pub     # Update verification key (public — built into images)</span>
<span class="line">└── .gitignore</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>After your first build, a <code>dist/</code> folder appears too — covered <a href="#the-dist-folder">below</a>.</p><h2 id="strux-yaml" tabindex="-1"><a class="header-anchor" href="#strux-yaml"><span>strux.yaml</span></a></h2><p>The project&#39;s main configuration: project name, which BSP to build by default, display layout, splash screen, root filesystem packages, QEMU options, and dev server settings. Every key is documented in the <a href="/reference/strux-yaml.html" target="_blank" rel="noopener noreferrer">strux.yaml reference</a>.</p><h2 id="main-go-and-go-mod" tabindex="-1"><a class="header-anchor" href="#main-go-and-go-mod"><span>main.go and go.mod</span></a></h2><p>Your Go backend. The template ships a small <code>App</code> struct whose exported fields and methods are automatically exposed to the frontend as a typed API — see the <a href="/guide/backend.html" target="_blank" rel="noopener noreferrer">Backend guide</a>. <code>go.mod</code> declares the dependency on the Strux runtime library (<code>github.com/strux-dev/strux/pkg/runtime</code>); <code>strux init</code> creates it for you.</p><h2 id="frontend" tabindex="-1"><a class="header-anchor" href="#frontend"><span>frontend/</span></a></h2><p>A completely normal Vite project — React, Vue, or vanilla TypeScript depending on the <code>--template</code> you picked. The one Strux-specific file is <code>frontend/src/strux.d.ts</code>: auto-generated TypeScript definitions for your Go backend&#39;s API. Don&#39;t edit it by hand; it&#39;s regenerated by <code>strux types</code> and at the start of every build. The <a href="/guide/frontend.html" target="_blank" rel="noopener noreferrer">Frontend guide</a> covers all of this.</p><h2 id="bsp" tabindex="-1"><a class="header-anchor" href="#bsp"><span>bsp/</span></a></h2><p>Board Support Packages — one folder per hardware target. Each BSP describes how to build the OS for a specific board: kernel, bootloader, lifecycle scripts, and board-specific filesystem additions.</p><div class="hint-container tip"><p class="hint-container-title">What&#39;s a BSP?</p><p>A Board Support Package is everything hardware-specific: which kernel to compile, which bootloader the board needs, what drivers and configuration it requires. Your app code stays the same across boards; the BSP changes. See <a href="/concepts/bsp.html" target="_blank" rel="noopener noreferrer">BSP concepts</a>.</p></div><p>Your project starts with one BSP, <code>bsp/qemu/</code>, containing:</p><ul><li><code>bsp.yaml</code> — the BSP configuration (architecture, kernel and bootloader settings, build scripts). Heavily commented in the template; full reference at <a href="/bsp/reference/bsp-yaml.html" target="_blank" rel="noopener noreferrer">bsp.yaml</a>.</li><li><code>scripts/make-image.sh</code> — the lifecycle script that assembles the final disk image.</li><li><code>overlay/</code> — files copied into the OS filesystem only when building <em>this</em> BSP.</li></ul><div class="hint-container warning"><p class="hint-container-title">Keep the qemu BSP</p><p>Do <strong>not</strong> delete <code>bsp/qemu/</code>. It&#39;s what lets <code>strux dev</code> and <code>strux run</code> work on your machine. Add BSPs for real hardware alongside it — see <a href="/bsp/guide/writing-a-bsp.html" target="_blank" rel="noopener noreferrer">Writing a BSP</a>.</p></div><h2 id="assets" tabindex="-1"><a class="header-anchor" href="#assets"><span>assets/</span></a></h2><p>Static assets used at build time. The template puts <code>logo.png</code> here — the boot splash image, referenced from <code>strux.yaml</code> under <code>boot.splash.logo</code>. Swap it for your own (it must be a PNG).</p><h2 id="overlay" tabindex="-1"><a class="header-anchor" href="#overlay"><span>overlay/</span></a></h2><p>The global root filesystem overlay: anything you put here is copied verbatim into the OS image, for every BSP.</p><div class="hint-container tip"><p class="hint-container-title">What&#39;s a rootfs overlay?</p><p>The <strong>root filesystem</strong> (rootfs) is the <code>/</code> of your device&#39;s Linux system — all its files and directories. An <strong>overlay</strong> is a folder mirroring that structure: <code>overlay/etc/myapp.conf</code> in your project becomes <code>/etc/myapp.conf</code> on the device. It&#39;s the standard way to drop config files into the image.</p></div><p>Each BSP also has its own <code>overlay/</code> for board-specific files; this top-level one applies to all builds.</p><h2 id="strux-update-key-strux-update-pub" tabindex="-1"><a class="header-anchor" href="#strux-update-key-strux-update-pub"><span>strux-update.key / strux-update.pub</span></a></h2><p>An RSA keypair generated by <code>strux init</code>, used to sign OS update bundles. The private key (<code>strux-update.key</code>) stays on your machine and is excluded by the generated <code>.gitignore</code>; the public key is built into your images so devices can verify updates. See <a href="/guide/updates.html" target="_blank" rel="noopener noreferrer">Updates</a>.</p><h2 id="the-dist-folder" tabindex="-1"><a class="header-anchor" href="#the-dist-folder"><span>The dist/ folder</span></a></h2><p><code>dist/</code> is created on your first build and holds everything Strux produces. It has five subfolders, each with different rules:</p><div class="language-txt line-numbers-mode" data-highlighter="prismjs" data-ext="txt"><pre><code><span class="line">dist/</span>
<span class="line">├── artifacts/    # User-editable build files — written once, then yours</span>
<span class="line">├── cache/        # Compiled intermediate artifacts (per BSP)</span>
<span class="line">├── output/       # Final images, ready to run or flash (per BSP)</span>
<span class="line">├── temp/         # Runtime scratch files</span>
<span class="line">└── flash/        # Flashing tool workspace (per BSP)</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="dist-artifacts-—-yours-to-edit" tabindex="-1"><a class="header-anchor" href="#dist-artifacts-—-yours-to-edit"><span>dist/artifacts/ — yours to edit</span></a></h3><p>These files are copied from the CLI&#39;s embedded assets <strong>once, on first build</strong>, and never overwritten after that. Edit them freely to customize the OS — your changes persist across builds. They include the builder <code>Dockerfile</code>, init and service scripts (<code>scripts/</code>), systemd units (<code>systemd/</code>), the Plymouth boot splash config (<code>plymouth/</code>), and the source for the on-device Strux client, Cage compositor, and WPE extension. One exception: <code>artifacts/logo.png</code> is re-copied on every build from the location set in <code>strux.yaml</code>. See <a href="/concepts/artifacts.html" target="_blank" rel="noopener noreferrer">Artifacts</a> and <a href="/guide/customizing-the-os.html" target="_blank" rel="noopener noreferrer">Customizing the OS</a>. Also, if you upgrade to a newer version of Strux, you may need to delete the dist/artifacts folder so that Strux can recreate them as they may implement version-specific features. There is also an option to restore your dist/artifacts to the built-in version in the <code>strux dev</code> Terminal User Interface.</p><h3 id="dist-cache-—-auto-generated-don-t-edit" tabindex="-1"><a class="header-anchor" href="#dist-cache-—-auto-generated-don-t-edit"><span>dist/cache/ — auto-generated, don&#39;t edit</span></a></h3><p>Compiled intermediate results: the bundled frontend (shared across BSPs) and a per-BSP folder (e.g. <code>cache/qemu/</code>) holding the compiled Go app, Cage, the Strux client, kernel artifacts, and rootfs tarballs. This is what makes rebuilds fast — the <a href="/concepts/caching.html" target="_blank" rel="noopener noreferrer">build cache</a> hashes each step&#39;s inputs and skips anything unchanged. Wipe it with <code>strux build &lt;bsp&gt; --clean</code> or simply delete the folder. Deleting the cahce folder can fix a lot of issues during build, with only caveat that the next build will be a complete rebuild.</p><h3 id="dist-output-—-the-result" tabindex="-1"><a class="header-anchor" href="#dist-output-—-the-result"><span>dist/output/ — the result</span></a></h3><p>Final build outputs, per BSP. For the qemu BSP this is <code>rootfs.ext4</code> (the root filesystem image), <code>vmlinuz</code> (the kernel), and <code>initrd.img</code> (the initial ramdisk). <code>strux run</code> boots these; <code>strux flash</code> writes them to hardware.</p><h3 id="dist-temp-—-scratch" tabindex="-1"><a class="header-anchor" href="#dist-temp-—-scratch"><span>dist/temp/ — scratch</span></a></h3><p>Small runtime files, like <code>qemu-pid</code> (the process ID of the QEMU instance started by <code>strux run</code>). Safe to ignore.</p><h3 id="dist-flash-—-flashing-workspace" tabindex="-1"><a class="header-anchor" href="#dist-flash-—-flashing-workspace"><span>dist/flash/ — flashing workspace</span></a></h3><p>A per-BSP workspace used by <code>strux flash</code> for downloaded or compiled flashing tools. BSP flash scripts get it as the <code>FLASH_DIR</code> environment variable. See <a href="/guide/flashing.html" target="_blank" rel="noopener noreferrer">Flashing</a>.</p><h2 id="where-to-go-next" tabindex="-1"><a class="header-anchor" href="#where-to-go-next"><span>Where to go next</span></a></h2><ul><li><a href="/guide/frontend.html" target="_blank" rel="noopener noreferrer">Frontend</a> and <a href="/guide/backend.html" target="_blank" rel="noopener noreferrer">Backend</a> — building your actual app.</li><li><a href="/concepts/build-pipeline.html" target="_blank" rel="noopener noreferrer">Build Pipeline</a> — what happens when you run <code>strux build</code>.</li><li><a href="/reference/strux-yaml.html" target="_blank" rel="noopener noreferrer">strux.yaml reference</a> — every configuration key.</li></ul>`,41)])])}const l=t(i,[["render",n]]),c=JSON.parse(`{"path":"/guide/project-structure.html","title":"Project Structure","lang":"en-US","frontmatter":{},"headers":[{"level":2,"title":"The scaffolded project","slug":"the-scaffolded-project","link":"#the-scaffolded-project","children":[]},{"level":2,"title":"strux.yaml","slug":"strux-yaml","link":"#strux-yaml","children":[]},{"level":2,"title":"main.go and go.mod","slug":"main-go-and-go-mod","link":"#main-go-and-go-mod","children":[]},{"level":2,"title":"frontend/","slug":"frontend","link":"#frontend","children":[]},{"level":2,"title":"bsp/","slug":"bsp","link":"#bsp","children":[]},{"level":2,"title":"assets/","slug":"assets","link":"#assets","children":[]},{"level":2,"title":"overlay/","slug":"overlay","link":"#overlay","children":[]},{"level":2,"title":"strux-update.key / strux-update.pub","slug":"strux-update-key-strux-update-pub","link":"#strux-update-key-strux-update-pub","children":[]},{"level":2,"title":"The dist/ folder","slug":"the-dist-folder","link":"#the-dist-folder","children":[{"level":3,"title":"dist/artifacts/ — yours to edit","slug":"dist-artifacts-—-yours-to-edit","link":"#dist-artifacts-—-yours-to-edit","children":[]},{"level":3,"title":"dist/cache/ — auto-generated, don't edit","slug":"dist-cache-—-auto-generated-don-t-edit","link":"#dist-cache-—-auto-generated-don-t-edit","children":[]},{"level":3,"title":"dist/output/ — the result","slug":"dist-output-—-the-result","link":"#dist-output-—-the-result","children":[]},{"level":3,"title":"dist/temp/ — scratch","slug":"dist-temp-—-scratch","link":"#dist-temp-—-scratch","children":[]},{"level":3,"title":"dist/flash/ — flashing workspace","slug":"dist-flash-—-flashing-workspace","link":"#dist-flash-—-flashing-workspace","children":[]}]},{"level":2,"title":"Where to go next","slug":"where-to-go-next","link":"#where-to-go-next","children":[]}],"git":{"updatedTime":1781317241000,"contributors":[{"name":"Miguel Medeiros","username":"","email":"miguel@medeirosconsulting.ca","commits":1}],"changelog":[{"hash":"e32366d86296aed0c45cf8447d8ea8147c1d03da","time":1781317241000,"email":"miguel@medeirosconsulting.ca","author":"Miguel Medeiros","message":"add docs"}]},"filePathRelative":"guide/project-structure.md"}`);export{l as comp,c as data};
