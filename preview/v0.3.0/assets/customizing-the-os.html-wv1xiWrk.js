import{_ as s,c as n,a,o as t}from"./app-BxL53bLZ.js";const o={};function i(l,e){return t(),n("div",null,[...e[0]||(e[0]=[a(`<h1 id="customizing-the-os" tabindex="-1"><a class="header-anchor" href="#customizing-the-os"><span>Customizing the OS</span></a></h1><p>Your project&#39;s <code>strux.yaml</code> controls more than the app — it shapes the operating system itself. This page covers the boot splash, adding your own files and packages to the image, the device hostname, and multi-monitor display configuration.</p><p>All examples on this page are real keys from the <code>strux.yaml</code> schema (<code>src/types/main-yaml.ts</code> in the CLI). Run <code>strux build</code> after changing any of them — the <a href="/concepts/caching.html" target="_blank" rel="noopener noreferrer">build cache</a> only rebuilds the steps affected by your change.</p><h2 id="boot-splash" tabindex="-1"><a class="header-anchor" href="#boot-splash"><span>Boot splash</span></a></h2><p>A kiosk should never show a Linux console scrolling text. With the splash enabled, the device shows your logo from early boot until your app takes over:</p><div class="language-yaml line-numbers-mode" data-highlighter="prismjs" data-ext="yml"><pre><code><span class="line"><span class="token key atrule">boot</span><span class="token punctuation">:</span></span>
<span class="line">  <span class="token key atrule">splash</span><span class="token punctuation">:</span></span>
<span class="line">    <span class="token key atrule">enabled</span><span class="token punctuation">:</span> <span class="token boolean important">true</span></span>
<span class="line">    <span class="token key atrule">logo</span><span class="token punctuation">:</span> ./assets/logo.png</span>
<span class="line">    <span class="token key atrule">color</span><span class="token punctuation">:</span> <span class="token string">&quot;000000&quot;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><table><thead><tr><th>Key</th><th>Type</th><th>Default</th><th>Description</th></tr></thead><tbody><tr><td><code>enabled</code></td><td>boolean</td><td>—</td><td>Turn the splash on or off.</td></tr><tr><td><code>logo</code></td><td>path</td><td>—</td><td>A PNG file, relative to the project root.</td></tr><tr><td><code>color</code></td><td>string</td><td>—</td><td>6-digit hex color, <strong>without</strong> the <code>#</code> (e.g. <code>&quot;1a1a2e&quot;</code>).</td></tr></tbody></table><p>How it works: at build time your logo is copied into the image, where it&#39;s shown twice — first by Plymouth (the Linux boot splash system) during early boot, then by Strux&#39;s Cage compositor while the browser engine starts. Your app appears only when it&#39;s ready to render, so the user never sees an in-between state.</p><div class="hint-container tip"><p class="hint-container-title">Make the hand-off invisible</p><p>Set <code>color</code> to the background color of your app, and give your app&#39;s root element that same background. The splash fades into your UI with no visible transition. The color is also passed to BSP scripts as <code>SPLASH_COLOR</code>, so boards that render a bootloader splash can match it too — see <a href="/bsp/reference/environment-variables.html" target="_blank" rel="noopener noreferrer">environment variables</a>.</p></div><p>If the logo file is missing, the build logs an error and falls back to the default Strux logo rather than failing.</p><h2 id="the-rootfs-overlay" tabindex="-1"><a class="header-anchor" href="#the-rootfs-overlay"><span>The rootfs overlay</span></a></h2><div class="hint-container tip"><p class="hint-container-title">What&#39;s a rootfs?</p><p>The <strong>root filesystem (rootfs)</strong> is the entire Linux filesystem of your device — <code>/etc</code>, <code>/usr</code>, <code>/home</code>, everything. Strux assembles a minimal Debian rootfs for you during the build. An <strong>overlay</strong> is a folder in your project whose contents get copied on top of that filesystem, verbatim.</p></div><div class="language-yaml line-numbers-mode" data-highlighter="prismjs" data-ext="yml"><pre><code><span class="line"><span class="token key atrule">rootfs</span><span class="token punctuation">:</span></span>
<span class="line">  <span class="token key atrule">overlay</span><span class="token punctuation">:</span> ./overlay</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>Anything you put in the overlay folder lands at the same path in the image. For example, to ship a custom SSH server configuration:</p><div class="language-txt line-numbers-mode" data-highlighter="prismjs" data-ext="txt"><pre><code><span class="line">overlay/</span>
<span class="line">└── etc/</span>
<span class="line">    └── ssh/</span>
<span class="line">        └── sshd_config    →  becomes /etc/ssh/sshd_config on the device</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The overlay is applied near the end of the rootfs build with <code>rsync</code>, so files overwrite anything already at that path, and permissions (including executable bits) are preserved. If your BSP also defines an overlay, the BSP overlay is applied first and your project overlay second — your files win on conflict.</p><p>Common uses: systemd units, udev rules, network configuration, certificates, fonts, and any static files your Go backend reads at runtime.</p><h2 id="installing-packages" tabindex="-1"><a class="header-anchor" href="#installing-packages"><span>Installing packages</span></a></h2><p>Your image is based on Debian, so you can install anything from the Debian package archive — or your own <code>.deb</code> files:</p><div class="language-yaml line-numbers-mode" data-highlighter="prismjs" data-ext="yml"><pre><code><span class="line"><span class="token key atrule">rootfs</span><span class="token punctuation">:</span></span>
<span class="line">  <span class="token key atrule">packages</span><span class="token punctuation">:</span></span>
<span class="line">    <span class="token punctuation">-</span> curl</span>
<span class="line">    <span class="token punctuation">-</span> ffmpeg</span>
<span class="line">    <span class="token punctuation">-</span> openssh<span class="token punctuation">-</span>server</span>
<span class="line">    <span class="token punctuation">-</span> ./packages/my<span class="token punctuation">-</span>driver.deb</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><ul><li><strong>Package names</strong> (like <code>ffmpeg</code>) are installed with <code>apt-get install --no-install-recommends</code> inside the image during the build. Anything available in Debian works.</li><li><strong>Paths ending in <code>.deb</code></strong> are resolved relative to the project root, copied into the image, and installed with <code>dpkg -i</code> (with an automatic <code>apt-get install -f</code> to pull in their dependencies). Use this for vendor drivers or your own packaged software.</li></ul><p>A missing <code>.deb</code> file produces a warning and is skipped, so check the build output if a package doesn&#39;t show up on the device.</p><div class="hint-container tip"><p class="hint-container-title">BSP packages live in the BSP</p><p>Board-specific packages (firmware, hardware tools) belong in the BSP&#39;s <code>bsp.yaml</code>, not here — they&#39;re merged with your project list at build time. See the <a href="/bsp/reference/bsp-yaml.html" target="_blank" rel="noopener noreferrer">bsp.yaml reference</a>.</p></div><h2 id="hostname" tabindex="-1"><a class="header-anchor" href="#hostname"><span>Hostname</span></a></h2><div class="language-yaml line-numbers-mode" data-highlighter="prismjs" data-ext="yml"><pre><code><span class="line"><span class="token key atrule">hostname</span><span class="token punctuation">:</span> lobby<span class="token punctuation">-</span>kiosk</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>The hostname is written to <code>/etc/hostname</code> and <code>/etc/hosts</code> in the image. It&#39;s how the device identifies itself on the network — useful when you have a fleet and want <code>lobby-kiosk</code> and <code>cafe-kiosk</code> distinguishable in your router&#39;s client list.</p><p>Resolution order: <code>hostname</code> in <code>strux.yaml</code>, then <code>bsp.hostname</code> in the BSP&#39;s <code>bsp.yaml</code>, then the default <code>strux</code>.</p><h2 id="multi-monitor-displays" tabindex="-1"><a class="header-anchor" href="#multi-monitor-displays"><span>Multi-monitor displays</span></a></h2><p>A Strux device can drive several screens, each showing a different route of your app. This is one config block doing three jobs: which route goes where, how each screen is oriented, and which touchscreen controls which display.</p><div class="language-yaml line-numbers-mode" data-highlighter="prismjs" data-ext="yml"><pre><code><span class="line"><span class="token key atrule">display</span><span class="token punctuation">:</span></span>
<span class="line">  <span class="token key atrule">monitors</span><span class="token punctuation">:</span></span>
<span class="line">    <span class="token punctuation">-</span> <span class="token key atrule">path</span><span class="token punctuation">:</span> /</span>
<span class="line">      <span class="token key atrule">resolution</span><span class="token punctuation">:</span> 1920x1080</span>
<span class="line">      <span class="token key atrule">names</span><span class="token punctuation">:</span></span>
<span class="line">        <span class="token punctuation">-</span> DSI<span class="token punctuation">-</span><span class="token number">1</span></span>
<span class="line">        <span class="token punctuation">-</span> Virtual<span class="token punctuation">-</span><span class="token number">1</span></span>
<span class="line">      <span class="token key atrule">input_devices</span><span class="token punctuation">:</span></span>
<span class="line">        <span class="token punctuation">-</span> ILITEK</span>
<span class="line">    <span class="token punctuation">-</span> <span class="token key atrule">path</span><span class="token punctuation">:</span> /tv</span>
<span class="line">      <span class="token key atrule">resolution</span><span class="token punctuation">:</span> 1920x1080</span>
<span class="line">      <span class="token key atrule">transform</span><span class="token punctuation">:</span> <span class="token number">90</span></span>
<span class="line">      <span class="token key atrule">names</span><span class="token punctuation">:</span></span>
<span class="line">        <span class="token punctuation">-</span> HDMI<span class="token punctuation">-</span>A<span class="token punctuation">-</span><span class="token number">1</span></span>
<span class="line">        <span class="token punctuation">-</span> Virtual<span class="token punctuation">-</span><span class="token number">2</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><table><thead><tr><th>Key</th><th>Type</th><th>Default</th><th>Description</th></tr></thead><tbody><tr><td><code>path</code></td><td>string</td><td>—</td><td>The app route to show on this output. Appended to your frontend&#39;s base URL — <code>/tv</code> loads your app&#39;s <code>/tv</code> route.</td></tr><tr><td><code>resolution</code></td><td>string</td><td>—</td><td><code>WIDTHxHEIGHT</code>, e.g. <code>1920x1080</code>. Optional.</td></tr><tr><td><code>transform</code></td><td>string/number</td><td>—</td><td>Rotation/flip: <code>normal</code>, <code>90</code>, <code>180</code>, <code>270</code>, <code>flipped</code>, <code>flipped-90</code>, <code>flipped-180</code>, <code>flipped-270</code>. Optional.</td></tr><tr><td><code>names</code></td><td>string[]</td><td>—</td><td>Output connector names this entry applies to. Optional.</td></tr><tr><td><code>input_devices</code></td><td>string[]</td><td>—</td><td>Touch/pointer devices bound to this output. Optional.</td></tr></tbody></table><p>With this example, the built-in DSI panel shows your app&#39;s home route while the HDMI screen shows the <code>/tv</code> route, rotated 90 degrees — each in its own dedicated browser instance, confined to its own output.</p><p>A few details worth knowing:</p><ul><li><strong><code>names</code> are connector names</strong> like <code>HDMI-A-1</code>, <code>DSI-1</code>, or <code>eDP-1</code> — how Linux identifies each physical video output. List several names for the same entry so one config works on both real hardware and QEMU, which calls its outputs <code>Virtual-1</code>, <code>Virtual-2</code>, and so on.</li><li><strong><code>input_devices</code> map touchscreens to outputs.</strong> Each entry is a substring of the input device&#39;s name (e.g. <code>ILITEK</code> matches <code>ILITEK ILITEK-TP</code>). A matched device is bound to the first name in <code>names</code>, so touches land on the right screen — essential when two touchscreens are connected, and when a rotated display needs its touch coordinates rotated to match.</li><li><strong><code>transform</code> rotates the output</strong> — <code>90</code> is the usual choice for portrait kiosks. Touch input mapped to that output is transformed with it.</li><li><strong>Unconfigured outputs</strong> show a &quot;not configured&quot; page instead of stretching your app across them.</li><li><strong>No <code>display</code> section at all</strong> is fine: you get a single monitor at <code>/</code> using the BSP&#39;s display resolution.</li></ul><p>If you change <code>display</code> settings, rebuild — the config is baked into the image (the device reads it from <code>/strux/.display-config.json</code> at boot).</p><h2 id="where-to-go-next" tabindex="-1"><a class="header-anchor" href="#where-to-go-next"><span>Where to go next</span></a></h2><ul><li><a href="/guide/project-structure.html" target="_blank" rel="noopener noreferrer">Project Structure</a> — every file in your project, explained.</li><li><a href="/guide/building.html" target="_blank" rel="noopener noreferrer">Building</a> — the build command and its options.</li><li><a href="/concepts/display-stack.html" target="_blank" rel="noopener noreferrer">Display Stack</a> — how Cage and WPE WebKit put your app on screen.</li><li><a href="/guide/updates.html" target="_blank" rel="noopener noreferrer">Updates</a> — ship a new OS version to devices in the field.</li></ul>`,37)])])}const c=s(o,[["render",i]]),d=JSON.parse('{"path":"/guide/customizing-the-os.html","title":"Customizing the OS","lang":"en-US","frontmatter":{},"headers":[{"level":2,"title":"Boot splash","slug":"boot-splash","link":"#boot-splash","children":[]},{"level":2,"title":"The rootfs overlay","slug":"the-rootfs-overlay","link":"#the-rootfs-overlay","children":[]},{"level":2,"title":"Installing packages","slug":"installing-packages","link":"#installing-packages","children":[]},{"level":2,"title":"Hostname","slug":"hostname","link":"#hostname","children":[]},{"level":2,"title":"Multi-monitor displays","slug":"multi-monitor-displays","link":"#multi-monitor-displays","children":[]},{"level":2,"title":"Where to go next","slug":"where-to-go-next","link":"#where-to-go-next","children":[]}],"git":{"updatedTime":1781317241000,"contributors":[{"name":"Miguel Medeiros","username":"","email":"miguel@medeirosconsulting.ca","commits":1}],"changelog":[{"hash":"e32366d86296aed0c45cf8447d8ea8147c1d03da","time":1781317241000,"email":"miguel@medeirosconsulting.ca","author":"Miguel Medeiros","message":"add docs"}]},"filePathRelative":"guide/customizing-the-os.md"}');export{c as comp,d as data};
