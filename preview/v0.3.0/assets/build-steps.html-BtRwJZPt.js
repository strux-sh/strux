import{_ as t,c as o,a as d,o as r}from"./app-BxL53bLZ.js";const c={};function a(n,e){return r(),o("div",null,[...e[0]||(e[0]=[d('<h1 id="build-steps-lifecycle-hooks" tabindex="-1"><a class="header-anchor" href="#build-steps-lifecycle-hooks"><span>Build Steps &amp; Lifecycle Hooks</span></a></h1><p>The exact order of the build pipeline, what each step does, when conditional steps run, and the full list of <code>before_*</code> / <code>after_*</code> hook values your BSP scripts can attach to. For the mental model behind the pipeline, see <a href="/concepts/build-pipeline.html" target="_blank" rel="noopener noreferrer">Build Pipeline</a>; for how to write hook scripts, see the <a href="/bsp/guide/scripts.html" target="_blank" rel="noopener noreferrer">Scripts guide</a>.</p><h2 id="pipeline-order" tabindex="-1"><a class="header-anchor" href="#pipeline-order"><span>Pipeline order</span></a></h2><p>Steps run in this order on every <code>strux build</code> (and the build phase of <code>strux dev</code>). Each cacheable step is skipped when the <a href="/concepts/caching.html" target="_blank" rel="noopener noreferrer">build cache</a> detects no changes to its inputs; its surrounding hooks still run (subject to their own <a href="/bsp/concepts/lifecycle-scripts.html" target="_blank" rel="noopener noreferrer">script caching</a>).</p><table><thead><tr><th>#</th><th>Step</th><th>What it does</th><th>Runs when</th><th>Hooks around it</th></tr></thead><tbody><tr><td>1</td><td><code>frontend</code></td><td>Regenerates TypeScript API types, then compiles the web frontend (Vue/React/vanilla) with Vite.</td><td>Always</td><td><code>before_frontend</code> / <code>after_frontend</code></td></tr><tr><td>2</td><td><code>application</code></td><td>Compiles your <code>main.go</code> backend for the target architecture.</td><td>Always</td><td><code>before_application</code> / <code>after_application</code></td></tr><tr><td>3</td><td><code>cage</code></td><td>Compiles the Cage Wayland compositor.</td><td>Always</td><td><code>before_cage</code> / <code>after_cage</code></td></tr><tr><td>4</td><td><code>wpe</code></td><td>Compiles the WPE WebKit extension and the patched Cog browser launcher.</td><td>Always</td><td><code>before_wpe</code> / <code>after_wpe</code></td></tr><tr><td>5</td><td><code>screen</code></td><td>Compiles the screen capture daemon (<code>strux-screen</code>).</td><td>Always</td><td>none</td></tr><tr><td>6</td><td><code>client</code></td><td>Compiles the on-device Strux client binary; writes (dev) or removes (production) the dev environment config.</td><td>Always</td><td><code>before_client</code> / <code>after_client</code></td></tr><tr><td>7</td><td><code>kernel</code></td><td>Two phases: fetch + patch the kernel source (<code>extract</code>), then configure, compile, and install it.</td><td>Only when <code>boot.kernel.custom_kernel: true</code></td><td><code>before_kernel</code>, <code>after_kernel_extract</code> (between the phases), <code>after_kernel</code></td></tr><tr><td>8</td><td><code>bootloader</code></td><td>Builds the bootloader (U-Boot, GRUB, ...).</td><td>Only when <code>boot.bootloader.enabled: true</code>; the built-in build additionally requires <code>type</code> to be set and not <code>custom</code> or <code>none</code></td><td><code>before_bootloader</code> / <code>after_bootloader</code></td></tr><tr><td>9</td><td><code>rootfs-base</code></td><td>Creates the minimal Debian base root filesystem with debootstrap.</td><td>Always</td><td><code>before_rootfs</code> / <code>after_rootfs</code></td></tr><tr><td>10</td><td><code>rootfs-post</code></td><td>Post-processes the rootfs: installs your binaries, kernel, packages, overlays, splash, and display config into it. The display config is written just before this step.</td><td>Always</td><td>none (followed by <code>before_bundle</code>)</td></tr><tr><td>11</td><td><code>before_bundle</code></td><td>Hook-only stage between rootfs post-processing and image creation.</td><td>Always</td><td>—</td></tr><tr><td>12</td><td><code>make_image</code></td><td>Runs the BSP&#39;s <code>make_image</code> script(s) to produce the final disk image. There is no built-in image step — the BSP must provide this.</td><td>Always (if the BSP defines a <code>make_image</code> script)</td><td>—</td></tr><tr><td>13</td><td>update bundle</td><td>Generates the signed Strux rootfs update bundle from the built image.</td><td>Only when <code>strux.yaml</code> has <code>update.enabled: true</code> and <code>update.auto_bundle: true</code> — see <a href="/guide/updates.html" target="_blank" rel="noopener noreferrer">Updates</a></td><td>—</td></tr><tr><td>14</td><td><code>after_build</code></td><td>Hook-only stage after everything completes.</td><td>Always</td><td>—</td></tr></tbody></table><p><code>before_build</code> hooks run before step 1, right after configuration validation and artifact preparation.</p><div class="hint-container tip"><p class="hint-container-title">Conditional steps and caching</p><p>When <code>custom_kernel</code> is <code>false</code> or <code>bootloader.enabled</code> is <code>false</code>, the corresponding step is treated as intentionally skipped — it does not poison the cache for later steps. Setting <code>bootloader.type: custom</code> keeps the bootloader stage enabled (hooks and <code>custom_bootloader</code> scripts run) but skips Strux&#39;s built-in build.</p></div><h2 id="replacing-built-in-steps" tabindex="-1"><a class="header-anchor" href="#replacing-built-in-steps"><span>Replacing built-in steps</span></a></h2><p>Two hook values don&#39;t wrap a built-in step — they <strong>replace</strong> it:</p><table><thead><tr><th>Hook</th><th>Replaces</th><th>Behavior</th></tr></thead><tbody><tr><td><code>custom_kernel</code></td><td>The built-in kernel build (both the extract and build phases)</td><td>Your script runs instead. <code>before_kernel</code> and <code>after_kernel</code> still run around it; <code>after_kernel_extract</code> does <strong>not</strong> run, since the built-in extract phase is skipped. Requires <code>boot.kernel.custom_kernel: true</code>.</td></tr><tr><td><code>custom_bootloader</code></td><td>The built-in bootloader build</td><td>Your script runs instead. <code>before_bootloader</code> and <code>after_bootloader</code> still run around it. Requires <code>boot.bootloader.enabled: true</code>.</td></tr></tbody></table><h2 id="all-valid-hook-values" tabindex="-1"><a class="header-anchor" href="#all-valid-hook-values"><span>All valid hook values</span></a></h2><p>The complete set of values accepted by <code>scripts[].step</code> in <a href="/bsp/reference/bsp-yaml.html#bsp-scripts" target="_blank" rel="noopener noreferrer">bsp.yaml</a>:</p><table><thead><tr><th>Step value</th><th>Fires</th></tr></thead><tbody><tr><td><code>before_build</code></td><td>Very first, before any compilation.</td></tr><tr><td><code>before_frontend</code></td><td>Before frontend compilation.</td></tr><tr><td><code>after_frontend</code></td><td>After frontend compilation.</td></tr><tr><td><code>before_application</code></td><td>Before <code>main.go</code> compilation.</td></tr><tr><td><code>after_application</code></td><td>After <code>main.go</code> compilation.</td></tr><tr><td><code>before_cage</code></td><td>Before Cage compositor compilation.</td></tr><tr><td><code>after_cage</code></td><td>After Cage compilation.</td></tr><tr><td><code>before_wpe</code></td><td>Before WPE WebKit extension compilation.</td></tr><tr><td><code>after_wpe</code></td><td>After WPE extension compilation.</td></tr><tr><td><code>before_client</code></td><td>Before Strux client compilation.</td></tr><tr><td><code>after_client</code></td><td>After Strux client compilation.</td></tr><tr><td><code>before_kernel</code></td><td>Before kernel source fetch. Conditional: <code>custom_kernel: true</code>.</td></tr><tr><td><code>after_kernel_extract</code></td><td>After kernel source fetch + patching, before configuration and compilation — e.g. to install a boot logo into the source tree. Conditional: <code>custom_kernel: true</code> and no <code>custom_kernel</code> script.</td></tr><tr><td><code>custom_kernel</code></td><td>Replaces the built-in kernel build. Conditional: <code>custom_kernel: true</code>.</td></tr><tr><td><code>after_kernel</code></td><td>After the kernel build (built-in or custom). Conditional: <code>custom_kernel: true</code>.</td></tr><tr><td><code>before_bootloader</code></td><td>Before the bootloader build. Conditional: <code>bootloader.enabled: true</code>.</td></tr><tr><td><code>custom_bootloader</code></td><td>Replaces the built-in bootloader build. Conditional: <code>bootloader.enabled: true</code>.</td></tr><tr><td><code>after_bootloader</code></td><td>After the bootloader build (built-in or custom). Conditional: <code>bootloader.enabled: true</code>.</td></tr><tr><td><code>before_rootfs</code></td><td>Before base root filesystem creation.</td></tr><tr><td><code>after_rootfs</code></td><td>After base root filesystem creation.</td></tr><tr><td><code>before_bundle</code></td><td>After rootfs post-processing, before <code>make_image</code>.</td></tr><tr><td><code>make_image</code></td><td>Creates the final disk image for the target device.</td></tr><tr><td><code>after_build</code></td><td>Very last, after everything completes.</td></tr><tr><td><code>flash_script_tool</code></td><td>Not part of the build. Run on the <strong>host</strong> by <code>strux flash</code>, before <code>flash_script</code> — e.g. to download a vendor flashing tool. See <a href="/bsp/guide/flash-scripts.html" target="_blank" rel="noopener noreferrer">Flash Scripts</a>.</td></tr><tr><td><code>flash_script</code></td><td>Not part of the build. Run on the <strong>host</strong> by <code>strux flash</code> to write the image to a device. See <a href="/bsp/guide/flash-scripts.html" target="_blank" rel="noopener noreferrer">Flash Scripts</a>.</td></tr></tbody></table><p>Multiple scripts may share the same step; they run in the order they appear in <code>bsp.yaml</code>.</p><h2 id="where-scripts-run" tabindex="-1"><a class="header-anchor" href="#where-scripts-run"><span>Where scripts run</span></a></h2><p>All build-time scripts run inside the <code>strux-builder</code> Docker container with your project mounted at <code>/project</code>. The two <code>flash_*</code> steps run directly on your host machine. Either way, scripts get a standard set of environment variables — see <a href="/bsp/reference/environment-variables.html" target="_blank" rel="noopener noreferrer">Environment Variables</a>.</p>',16)])])}const s=t(c,[["render",a]]),l=JSON.parse('{"path":"/bsp/reference/build-steps.html","title":"Build Steps & Lifecycle Hooks","lang":"en-US","frontmatter":{},"headers":[{"level":2,"title":"Pipeline order","slug":"pipeline-order","link":"#pipeline-order","children":[]},{"level":2,"title":"Replacing built-in steps","slug":"replacing-built-in-steps","link":"#replacing-built-in-steps","children":[]},{"level":2,"title":"All valid hook values","slug":"all-valid-hook-values","link":"#all-valid-hook-values","children":[]},{"level":2,"title":"Where scripts run","slug":"where-scripts-run","link":"#where-scripts-run","children":[]}],"git":{"updatedTime":1781317241000,"contributors":[{"name":"Miguel Medeiros","username":"","email":"miguel@medeirosconsulting.ca","commits":1}],"changelog":[{"hash":"e32366d86296aed0c45cf8447d8ea8147c1d03da","time":1781317241000,"email":"miguel@medeirosconsulting.ca","author":"Miguel Medeiros","message":"add docs"}]},"filePathRelative":"bsp/reference/build-steps.md"}');export{s as comp,l as data};
